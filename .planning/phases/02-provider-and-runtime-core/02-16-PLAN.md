---
phase: 02-provider-and-runtime-core
plan: 16
type: execute
wave: 10
depends_on:
  - 02-08
  - 02-11
  - 02-15
files_modified:
  - pyproject.toml
  - uv.lock
  - aura_backend/Dockerfile
  - tests/test_python_dependency_contract.py
autonomous: true
requirements:
  - TEST-05
  - OPS-02
must_haves:
  truths:
    - "pyproject.toml plus uv.lock are the sole authoritative Python resolution path and the Docker/runtime commands consume that path."
    - "Only the 14 conditionally approved Python actions are applied: exact Ruff addition, four exact lane moves, and nine direct-declaration removals."
    - "The rejected pyzbar, faiss-cpu, faiss-gpu-cu12, and asyncio-mqtt declarations and their lock records remain untouched; the active persistence path retains Torch/Chroma/sentence-transformers."
    - "Evidence freshness, exact 16/4 human disposition, and pre-change authority digests pass independently before any manifest or lock edit; no broad upgrade or local sync/install occurs."
  artifacts:
    - path: "tests/test_python_dependency_contract.py"
      provides: "Authority/group/import/entrypoint/Docker contract"
    - path: "pyproject.toml"
      provides: "Exact approved Python extras/groups/removals plus Ruff and existing Pyright configuration authority"
    - path: "uv.lock"
      provides: "Single resolved Python lock with unrelated-version-churn gate"
    - path: "aura_backend/Dockerfile"
      provides: "Locked uv-based image build path"
  key_links:
    - from: "aura_backend/Dockerfile"
      to: "pyproject.toml"
      via: "uv sync --locked from copied project metadata"
      pattern: "uv sync.*--locked"
---

<objective>
Reconcile Python dependency authority and audited optional lanes per D-05 without crossing storage work or upgrading broadly.

Purpose: Remove the contradictory requirements path, keep the runnable base honest, and make only the approved lint/optional capabilities reproducible.
Output: Exact approved `pyproject.toml` grouping/removals, regenerated `uv.lock`, uv-based Docker path, and authority tests.
</objective>

## Artifacts this phase produces

- Python dependency authority/static entrypoint gate.
- Exact approved provider/MCP/Memvid/dev declarations and direct removals with all non-enumerated declarations preserved.
- Removal of `requirements.txt` from supported install consumers while leaving the legacy file unchanged, plus locked Docker usage.

<execution_context>
@/home/ty/.Codex/gsd-core/workflows/execute-plan.md
@/home/ty/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-provider-and-runtime-core/02-CONTEXT.md
@.planning/phases/02-provider-and-runtime-core/02-RESEARCH.md
@.planning/phases/02-provider-and-runtime-core/02-08-SUMMARY.md
@.planning/phases/02-provider-and-runtime-core/02-11-SUMMARY.md
@.planning/phases/02-provider-and-runtime-core/02-15-SUMMARY.md
@.planning/evidence/phase-02/package-legitimacy.json
@pyproject.toml
@uv.lock
@requirements.txt
@aura_backend/Dockerfile
</context>

<dependencies>
Needs: Import-safe supported entry points, canonical CLI, and explicit package approval.
Creates: Authoritative Python lanes consumed by CI.
Wave rationale: May run beside launcher work; no file overlap, but cannot precede app/CLI smoke seams or approval.
</dependencies>

<approval_contract>
The executor must treat this revision as a narrow authorization envelope, not a general dependency-cleanup approval.

- Exact human decision: `Approve only the 16 OK rows; reject the four SUS rows; revise Plans 02-16 and 02-17 before any manifest or lock changes.`
- Revised evidence SHA-256: `e3efba9197560d9559629448cfa088003e9239567c645c0204d4d9f994724bdb` for `.planning/evidence/phase-02/package-legitimacy.json`.
- Pre-change Python authority SHA-256 values: `pyproject.toml=d9459bd69739ff4891e641bbcdd00f13abd4d41bf241e09141d6c7ebc2feef0c`, `uv.lock=4b7df8a21291f380a42f4b4a5d360d194b8228a0632ca84cae7dcffe35fca74e`.
- Approved Python action set: add exact `ruff==0.12.7` to the dev group; move exact `google-genai==1.75.0` to `provider-gemini` after lazy-import proof; move exact `mcp==1.27.0` and `fastmcp==3.2.4` to `mcp`; move exact `memvid-sdk==2.0.159` to `memvid`; remove the direct declarations for `beautifulsoup4@4.13.4`, `ebooklib@0.19`, `opencv-python@4.11.0.86`, `pandas@2.2.3`, `pillow@12.2.0`, `pypdf@6.10.2`, `qrcode@8.2`, `anthropic@0.54.0`, and `websockets@15.0.1`.
- Rejected and immutable in Phase 2: `pyzbar@0.1.9`, `faiss-cpu@1.11.0`, `faiss-gpu-cu12@1.14.1.post1`, and `asyncio-mqtt@0.16.2`. Rejection does not authorize moving, removing, repinning, or otherwise editing their declarations or resolved lock records.

Any missing/stale/changed evidence, digest mismatch, decision-text mismatch, set widening, unexpected action, or authorization broader than this exact subset stops execution before `pyproject.toml`, `uv.lock`, or any other dependency authority is edited.
</approval_contract>

<artifacts_produced>
- `tests/test_python_dependency_contract.py` — Python authority/group/removal/lock/Docker gates.
- `pyproject.toml` and `uv.lock` — checkpoint-approved Python dependency authority and lock.
- `aura_backend/Dockerfile` — lock-faithful uv runtime path; unchanged `requirements.txt` ceases to be a consumed authority.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 02-16-01: Define Python authority, lane, and supported-entrypoint gates</name>
  <files>tests/test_python_dependency_contract.py</files>
  <read_first>
    @.planning/evidence/phase-02/package-legitimacy.json
    @pyproject.toml
    @uv.lock
    @requirements.txt
    @aura_backend/Dockerfile
    @tests/test_dependency_audit.py
  </read_first>
  <behavior>
    - A pre-change authorization gate independently validates the evidence freshness window, evidence SHA-256, exact decision text, exact 16 OK / 4 SUS partition, and the current pyproject/uv.lock SHA-256 values before edits are possible.
    - The plan's enumerated 14-action Python subset equals the Python portion of the conditionally approved rows; any omitted, added, changed, or rejected candidate fails closed.
    - One authoritative project+lock exists; no Docker/launcher/docs path installs from requirements or unlocked pip, while `requirements.txt` itself remains unchanged.
    - Base retains active conversation/persistence imports; exact Gemini/MCP/Memvid moves occur only after their supported import/entrypoint lanes are lazy and proven.
    - The four rejected declarations and their resolved lock records are identical before and after; all other lock changes are mechanical consequences of the exact approved manifest diff.
  </behavior>
  <action>Write the contract as RED against current manifests. Parse TOML/lock/Docker source structurally where possible. Encode the `<approval_contract>` as an explicit immutable action set and pre-change SHA-256 map, then cross-check it independently against the evidence file, `02-15-SUMMARY.md`, current UTC freshness, live file digests, package verdicts, row-level non-authorization, and exact human decision. Provide an opt-in pre-change gate (for example `AURA_DEPENDENCY_PRECHANGE_GATE=1` with a narrowly selected pytest case) that passes only while all preconditions match; normal post-change test runs must not mistake the expected manifest update for evidence drift. Include supported import-smoke definitions for `aura_backend.main`, runtime CLI, provider modules, Aura MCP server, companion MCP wrapper, and active Memvid entry point under their declared lanes. Prove the Gemini import is lazy before expecting its move. Require the rejected declarations, their lock package records, and `requirements.txt` to remain unchanged. Since the audit records uv 0.11.21 but no reliable container-image digest, assert the exact official source/version reference `ghcr.io/astral-sh/uv:0.11.21` in the Dockerfile and reject floating or different uv image references; do not claim digest immutability. The desired-state tests must be RED only for the 14 enumerated approved Python actions and Docker authority repair, never for a rejected row.</action>
  <acceptance_criteria>
    - Current contradictory manifest/Docker state fails for the intended reasons.
    - The opt-in pre-change authorization test passes before edits; synthetic stale/changed evidence, digest drift, action-set widening, rejected-row mutation, missing lane, orphaned entrypoint, duplicate authority, and unrelated lock churn fail closed.
    - Tests do not import/open real database roots or install packages.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/test_python_dependency_contract.py</automated>
  </verify>
  <done>The exact approved Python manifest/lock/Docker end state and a passing fail-closed pre-edit gate are defined before any dependency authority changes.</done>
</task>

<task type="auto">
  <name>Task 02-16-02: Apply the exact approved Python subset and regenerate one lock</name>
  <files>pyproject.toml, uv.lock, aura_backend/Dockerfile, tests/test_python_dependency_contract.py</files>
  <read_first>
    @tests/test_python_dependency_contract.py
    @.planning/evidence/phase-02/package-legitimacy.json
    @.planning/phases/02-provider-and-runtime-core/02-15-SUMMARY.md
    @pyproject.toml
    @uv.lock
    @requirements.txt
    @aura_backend/Dockerfile
  </read_first>
  <action>Immediately before editing, run the opt-in pre-change authorization gate from Task 02-16-01 and stop without touching any dependency file unless it passes. Apply exactly these approved Python changes and no others: add exact `ruff==0.12.7` to the dev group; after the lazy-import probe passes, move exact `google-genai==1.75.0` to `provider-gemini`; move exact `mcp==1.27.0` and `fastmcp==3.2.4` to `mcp`; move exact `memvid-sdk==2.0.159` to `memvid`; remove only the direct declarations for `beautifulsoup4@4.13.4`, `ebooklib@0.19`, `opencv-python@4.11.0.86`, `pandas@2.2.3`, `pillow@12.2.0`, `pypdf@6.10.2`, `qrcode@8.2`, `anthropic@0.54.0`, and `websockets@15.0.1`. Keep every non-enumerated direct declaration exactly as declared, including pytest/pytest-asyncio and active Torch/Chroma/sentence-transformers. Leave `pyzbar`, both FAISS declarations, and `asyncio-mqtt` byte-for-byte unchanged in `pyproject.toml`, preserve their resolved package records in `uv.lock`, and leave `requirements.txt` unchanged. Make Docker copy uv/uvx from the exact official source/version reference `ghcr.io/astral-sh/uv:0.11.21`, then consume `pyproject.toml`/`uv.lock` through locked uv with explicit groups so the legacy requirements file is no longer an install authority. This is an exact source/version pin, not an unsupported immutable-digest claim. Regenerate `uv.lock` once with `uv lock`; inspect the manifest and semantic lock diff, and abort if it contains anything beyond the enumerated actions, Docker authority repair, and mechanically generated lock consequences. Do not run `uv sync`, install packages, mutate Ty's environment, change data/storage behavior, or broaden versions.</action>
  <acceptance_criteria>
    - `uv lock --check` passes and the authority test proves exact approved lanes/entrypoints.
    - The manifest diff contains exactly the 14 approved Python actions; every non-enumerated direct declaration, the four rejected lock records, and `requirements.txt` are unchanged.
    - Lock comparison shows only mechanically generated consequences of the exact approved manifest changes and no unrelated churn.
    - Docker has one lock-faithful uv path pinned to exact official reference `ghcr.io/astral-sh/uv:0.11.21`, no pip requirements path, and a valid readiness endpoint.
    - Full deterministic/companion/persistence/import tests pass in the existing environment; no data/storage behavior changes.
  </acceptance_criteria>
  <verify>
    <automated>uv lock --check && uv run --locked --no-sync python -m pytest -q tests/test_python_dependency_contract.py tests/test_dependency_audit.py tests/runtime/test_import_safety.py tests/characterization/test_companion_contract.py tests/characterization/test_persistence_contract.py</automated>
  </verify>
  <done>Python has one approved lock authority and explicit capability lanes without a broad upgrade or implicit large install.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Manifest/lock/Docker → installed runtime | Drift/confusion can execute different or malicious packages and break alternate entry points. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-DP | Tampering | Python authority/lock | mitigate | Approved evidence, one lock, lock-check, unrelated-version-churn abort. |
| T-02-DE | Repudiation | optional lanes/entrypoints | mitigate | Exact approved action-set plus import/entrypoint tests per named lane. |
| T-02-CD | Denial of Service | heavy install | mitigate | No active-environment sync; keep required storage and all rejected declarations untouched. |
| T-02-SC | Tampering | Ruff/uv/container source | mitigate | Blocking approval, exact Ruff version, and a test-enforced official uv source/version pin `ghcr.io/astral-sh/uv:0.11.21`; no unrecorded digest claim. |
</threat_model>

<verification>
Run lock/audit/authority/import/companion/persistence gates without syncing the active environment. CI later owns the clean locked install. Confirm no storage path/data/Git history changed.
</verification>

<success_criteria>
- OPS-02 has one Python authority and only the evidence-approved provider/MCP/Memvid/dev lane changes.
- TEST-05 can invoke a reproducible Ruff lane.
- D-05 broad upgrades and opaque/large startup installs remain absent.
</success_criteria>

<output>
Create `.planning/phases/02-provider-and-runtime-core/02-16-SUMMARY.md` when done.
</output>
