---
phase: 02-provider-and-runtime-core
plan: 16
type: execute
wave: 10
depends_on:
  - 02-08
  - 02-11
  - 02-15
files_modified:
  - pyproject.toml
  - uv.lock
  - requirements.txt
  - aura_backend/Dockerfile
  - tests/test_python_dependency_contract.py
autonomous: true
requirements:
  - TEST-05
  - OPS-02
must_haves:
  truths:
    - "pyproject.toml plus uv.lock are the sole authoritative Python resolution path and the Docker/runtime commands consume that path."
    - "Base, provider-gemini, MCP, Memvid/media, GPU, and dev lanes match audited imports/entrypoints; the active persistence path retains Torch/Chroma/sentence-transformers."
    - "Ruff is added only at the approved exact version; no broad dependency upgrade or large local sync/install occurs implicitly."
  artifacts:
    - path: "tests/test_python_dependency_contract.py"
      provides: "Authority/group/import/entrypoint/Docker contract"
    - path: "pyproject.toml"
      provides: "Audited base, extras/groups, pytest, Ruff, and Pyright config authority"
    - path: "uv.lock"
      provides: "Single resolved Python lock with unrelated-version-churn gate"
    - path: "aura_backend/Dockerfile"
      provides: "Locked uv-based image build path"
  key_links:
    - from: "aura_backend/Dockerfile"
      to: "pyproject.toml"
      via: "uv sync --locked from copied project metadata"
      pattern: "uv sync.*--locked"
---

<objective>
Reconcile Python dependency authority and audited optional lanes per D-05 without crossing storage work or upgrading broadly.

Purpose: Remove the contradictory requirements path, keep the runnable base honest, and make lint/optional capabilities reproducible.
Output: Grouped `pyproject.toml`, regenerated `uv.lock`, uv-based Docker path, and authority tests.
</objective>

## Artifacts this phase produces

- Python dependency authority/static entrypoint gate.
- Audited base/extras/dev declarations and exact lock update.
- Removal of `requirements.txt` as a supported authority and locked Docker usage.

<execution_context>
@/home/ty/.Codex/gsd-core/workflows/execute-plan.md
@/home/ty/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-provider-and-runtime-core/02-CONTEXT.md
@.planning/phases/02-provider-and-runtime-core/02-RESEARCH.md
@.planning/phases/02-provider-and-runtime-core/02-08-SUMMARY.md
@.planning/phases/02-provider-and-runtime-core/02-11-SUMMARY.md
@.planning/phases/02-provider-and-runtime-core/02-15-SUMMARY.md
@.planning/evidence/phase-02/package-legitimacy.json
@pyproject.toml
@uv.lock
@requirements.txt
@aura_backend/Dockerfile
</context>

<dependencies>
Needs: Import-safe supported entry points, canonical CLI, and explicit package approval.
Creates: Authoritative Python lanes consumed by CI.
Wave rationale: May run beside launcher work; no file overlap, but cannot precede app/CLI smoke seams or approval.
</dependencies>

<artifacts_produced>
- `tests/test_python_dependency_contract.py` — Python authority/group/removal/lock/Docker gates.
- `pyproject.toml` and `uv.lock` — checkpoint-approved Python dependency authority and lock.
- `aura_backend/Dockerfile` — lock-faithful uv runtime path; `requirements.txt` ceases to be a competing authority.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 02-16-01: Define Python authority, lane, and supported-entrypoint gates</name>
  <files>tests/test_python_dependency_contract.py</files>
  <read_first>
    @.planning/evidence/phase-02/package-legitimacy.json
    @pyproject.toml
    @uv.lock
    @requirements.txt
    @aura_backend/Dockerfile
    @tests/test_dependency_audit.py
  </read_first>
  <behavior>
    - One authoritative project+lock exists; no Docker/launcher/docs path installs from requirements or unlocked pip.
    - Every audited package is in its approved base/extra/group/remove disposition and every supported entry point's required lane exists.
    - Base retains active conversation/persistence imports; optional Gemini/MCP/Memvid/media/GPU imports do not become eager local-runtime requirements.
    - Lock changes outside approved direct/group consequences fail an explicit before/after package-version comparison.
  </behavior>
  <action>Write the contract as RED against current manifests. Parse TOML/lock/Docker source structurally where possible; bind expectations to the approved evidence artifact rather than duplicating an unaudited package list. Include supported import-smoke definitions for `aura_backend.main`, runtime CLI, provider modules, Aura MCP server, companion MCP wrapper, and active Memvid entry point under their declared lanes. The test must fail if `requirements.txt` remains a supported authority, a dependency is orphaned, or the lock exhibits unrelated version churn.</action>
  <acceptance_criteria>
    - Current contradictory manifest/Docker state fails for the intended reasons.
    - Synthetic missing lane, orphaned entrypoint, duplicate authority, and unrelated lock-churn cases fail.
    - Tests do not import/open real database roots or install packages.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/test_python_dependency_contract.py</automated>
  </verify>
  <done>The exact Python manifest/lock/Docker end state is defined before any dependency file changes.</done>
</task>

<task type="auto">
  <name>Task 02-16-02: Apply approved Python grouping and regenerate one lock</name>
  <files>pyproject.toml, uv.lock, requirements.txt, aura_backend/Dockerfile, tests/test_python_dependency_contract.py</files>
  <read_first>
    @tests/test_python_dependency_contract.py
    @.planning/evidence/phase-02/package-legitimacy.json
    @.planning/phases/02-provider-and-runtime-core/02-15-SUMMARY.md
    @pyproject.toml
    @uv.lock
    @requirements.txt
    @aura_backend/Dockerfile
  </read_first>
  <action>Apply only the approved evidence dispositions: keep active FastAPI/provider-Ollama/persistence dependencies in base; place `google-genai` in `provider-gemini`, MCP/FastMCP in `mcp`, Memvid/media/FAISS in their audited extras, GPU-only FAISS in `gpu`, and pytest/pytest-asyncio plus approved `ruff==0.12.7` in the dev group. Retain Torch/Chroma/sentence-transformers because current persistence imports require them. Remove only approved no-consumer direct declarations. Remove `requirements.txt` as an authority and make Docker copy `pyproject.toml`/`uv.lock`, use a verified immutable uv 0.11.21 image digest from the audit, and run locked sync with explicit groups. Regenerate `uv.lock` once with uv; abort and restore the manifest edit if unrelated packages upgrade/downgrade. Do not run `uv sync` into Ty's active environment or install large packages; verification uses the existing locked environment, lock check, import probes, and later CI clean install.</action>
  <acceptance_criteria>
    - `uv lock --check` passes and the authority test proves exact approved lanes/entrypoints.
    - Lock comparison shows no unrelated broad version churn.
    - Docker has one lock-faithful uv path, no pip requirements path, and a valid readiness endpoint.
    - Full deterministic/companion/persistence/import tests pass in the existing environment; no data/storage behavior changes.
  </acceptance_criteria>
  <verify>
    <automated>uv lock --check && uv run --locked --no-sync python -m pytest -q tests/test_python_dependency_contract.py tests/test_dependency_audit.py tests/runtime/test_import_safety.py tests/characterization/test_companion_contract.py tests/characterization/test_persistence_contract.py</automated>
  </verify>
  <done>Python has one approved lock authority and explicit capability lanes without a broad upgrade or implicit large install.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Manifest/lock/Docker → installed runtime | Drift/confusion can execute different or malicious packages and break alternate entry points. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-DP | Tampering | Python authority/lock | mitigate | Approved evidence, one lock, lock-check, unrelated-version-churn abort. |
| T-02-DE | Repudiation | optional lanes/entrypoints | mitigate | Exact import/entrypoint tests per named lane. |
| T-02-CD | Denial of Service | heavy install | mitigate | No active-environment sync; keep required storage deps, move only audited optional/GPU packages. |
| T-02-SC | Tampering | Ruff/uv/container source | mitigate | Blocking approval, exact Ruff version, immutable verified uv image digest. |
</threat_model>

<verification>
Run lock/audit/authority/import/companion/persistence gates without syncing the active environment. CI later owns the clean locked install. Confirm no storage path/data/Git history changed.
</verification>

<success_criteria>
- OPS-02 has one Python authority and evidence-backed optional/GPU/dev lanes.
- TEST-05 can invoke a reproducible Ruff lane.
- D-05 broad upgrades and opaque/large startup installs remain absent.
</success_criteria>

<output>
Create `.planning/phases/02-provider-and-runtime-core/02-16-SUMMARY.md` when done.
</output>
