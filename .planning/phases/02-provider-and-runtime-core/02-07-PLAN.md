---
phase: 02-provider-and-runtime-core
plan: 07
type: tdd
wave: 5
depends_on:
  - 02-02
  - 02-06
files_modified:
  - aura_backend/runtime/__init__.py
  - aura_backend/runtime/config.py
  - aura_backend/runtime/app.py
  - tests/runtime/test_lifecycle.py
autonomous: true
requirements:
  - TEST-03
  - AI-03
  - OPS-01
must_haves:
  truths:
    - "One ApplicationRuntime owns initialization, readiness state, partial-start unwind, in-flight cancellation, and reverse-order shutdown."
    - "A failed startup closes only successfully started resources, exactly once, in reverse order."
    - "Runtime configuration remains pure, typed, loopback-first, and does not initialize providers, storage, MCP, models, or subprocesses."
  artifacts:
    - path: "aura_backend/runtime/app.py"
      provides: "ApplicationRuntime resource stack and lifecycle owner"
    - path: "aura_backend/runtime/config.py"
      provides: "Pure application runtime settings"
    - path: "tests/runtime/test_lifecycle.py"
      provides: "Ordered start, partial unwind, close, and cancellation tests"
  key_links:
    - from: "aura_backend/runtime/app.py"
      to: "aura_backend/providers/runtime.py"
      via: "ProviderRuntime starts before readiness and closes after in-flight cancellation"
      pattern: "ProviderRuntime"
---

<objective>
Create the explicit application lifecycle owner required by D-04.

Purpose: Make construction and teardown testable before the FastAPI composition root is switched away from globals.
Output: Pure runtime configuration, dependency-injected `ApplicationRuntime`, and lifecycle tests.
</objective>

## Artifacts this phase produces

- Import-light `aura_backend.runtime` package.
- `RuntimeSettings` and ordered `ApplicationRuntime` resource ownership.
- Deterministic partial-start/shutdown/cancellation tests.

<execution_context>
@/home/ty/.Codex/gsd-core/workflows/execute-plan.md
@/home/ty/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-provider-and-runtime-core/02-CONTEXT.md
@.planning/phases/02-provider-and-runtime-core/02-RESEARCH.md
@.planning/phases/02-provider-and-runtime-core/02-02-SUMMARY.md
@.planning/phases/02-provider-and-runtime-core/02-06-SUMMARY.md
@aura_backend/main.py
@aura_backend/runtime_security.py
</context>

<dependencies>
Needs: ProviderRuntime cancellation/close semantics and strict selected-provider factory.
Creates: Application lifecycle API consumed by FastAPI lifespan and CLI.
Wave rationale: The generic owner is implemented before touching `main.py`.
</dependencies>

<artifacts_produced>
- `aura_backend/runtime/config.py` — pure runtime settings assembly.
- `aura_backend/runtime/app.py` — ordered startup, reverse unwind, cancellation, and close ownership.
- `tests/runtime/test_lifecycle.py` — partial-start and shutdown lifecycle proof.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 02-07-01: Define pure runtime settings and ordered resource ownership</name>
  <files>aura_backend/runtime/__init__.py, aura_backend/runtime/config.py, aura_backend/runtime/app.py, tests/runtime/test_lifecycle.py</files>
  <read_first>
    @aura_backend/runtime_security.py
    @aura_backend/main.py
    @aura_backend/providers/config.py
    @aura_backend/providers/runtime.py
  </read_first>
  <behavior>
    - Importing runtime config/app constructs no resource and performs no network, process, model, database, or filesystem initialization.
    - Runtime settings preserve loopback default, explicit LAN override, explicit origins, selected provider settings, port/storage/preflight bounds, and recognized key names.
    - Resource factories start in declared dependency order and each successful resource registers one async close callback.
    - If stage N fails, stages 1..N-1 close once in strict reverse order and readiness remains false with a safe code.
  </behavior>
  <action>Write recording-resource and partial-failure tests first. Add frozen `RuntimeSettings` composed with `ProviderSettings`, and an `ApplicationRuntime` that accepts explicit async factories/callbacks rather than importing `main`. Use an `AsyncExitStack`-style ordered ownership model or an equivalently testable explicit stack. Keep configuration parsing pure; storage roots are validated but not created/opened here. Preserve `server_host()` and `allowed_browser_origins()` semantics per D-04 and Phase 1.</action>
  <acceptance_criteria>
    - Test matrices prove exact start/close order for complete and every partial-start boundary.
    - Double close is safe and each registered close runs at most once.
    - Invalid host/origin/provider/port/storage configuration fails without constructing a resource.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/runtime/test_lifecycle.py -k 'settings or order or partial or close'</automated>
  </verify>
  <done>Application resources have one deterministic owner with fail-closed readiness and complete partial-start unwind.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-07-02: Integrate provider cancellation into application shutdown</name>
  <files>aura_backend/runtime/app.py, tests/runtime/test_lifecycle.py</files>
  <read_first>
    @aura_backend/runtime/app.py
    @aura_backend/providers/runtime.py
    @tests/providers/fakes.py
    @tests/providers/test_streaming.py
  </read_first>
  <behavior>
    - Runtime startup stores the selected `ProviderRuntime` as a typed required resource before readiness becomes true.
    - Shutdown first rejects new provider work, cancels/awaits active streams, and then closes remaining started resources in reverse dependency order.
    - Cancellation during shutdown is not swallowed as successful completion and no task remains pending.
    - Optional subsystem absence is recorded distinctly from required-runtime failure.
  </behavior>
  <action>Add a blocked fake provider operation to the lifecycle RED tests. Wire `ProviderRuntime` into the application resource registry with explicit accept-new-work, cancel/await, and close phases. Distinguish required versus optional resource status without making unconfigured cloud/autonomic/MCP components a readiness failure for a valid local runtime. Do not construct concrete database/MCP/provider implementations in this module.</action>
  <acceptance_criteria>
    - Active fake streams are cancelled and awaited before provider/client close.
    - No `Completed`/fallback result appears during application shutdown.
    - Required failure blocks readiness; optional not-configured remains visible but does not lie about local readiness.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/runtime/test_lifecycle.py -k 'provider or cancel or shutdown or optional'</automated>
  </verify>
  <done>Application shutdown owns and proves prompt local provider cleanup without overstating upstream cancellation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Configuration → runtime factories | Invalid settings must fail before external/stateful construction. |
| Application shutdown → resources | Partial/start/active resources must all close deterministically. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-LC | Tampering / Repudiation | lifecycle state | mitigate | Explicit ordered state machine, readiness false until complete, exact partial-unwind tests. |
| T-02-CD | Denial of Service | active tasks/resources | mitigate | Reject new work, cancel/await in-flight, idempotent reverse close. |
| T-02-LH | Information Disclosure | host/origins | mitigate | Reuse tested loopback and explicit-origin validation; no auth added. |
| T-02-SC | Tampering | package supply chain | mitigate | Standard library and locked dependencies only. |
</threat_model>

<verification>
Run lifecycle and provider-stream suites, then Phase 1 runtime-security/local-boundary tests. No concrete production resource may be created by the focused suite.
</verification>

<success_criteria>
- OPS-01 has one typed runtime lifecycle owner.
- D-04 partial initialization and shutdown are exact, safe, and testable.
- AI-03 cancellation/non-success semantics survive application shutdown.
</success_criteria>

<output>
Create `.planning/phases/02-provider-and-runtime-core/02-07-SUMMARY.md` when done.
</output>
