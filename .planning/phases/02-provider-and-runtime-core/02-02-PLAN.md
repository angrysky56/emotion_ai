---
phase: 02-provider-and-runtime-core
plan: 02
type: tdd
wave: 2
depends_on:
  - 02-01
files_modified:
  - aura_backend/providers/runtime.py
  - tests/providers/fakes.py
  - tests/providers/test_fake.py
  - tests/providers/test_streaming.py
autonomous: true
requirements:
  - TEST-03
  - AI-02
  - AI-03
must_haves:
  truths:
    - "Deterministic fakes cover normal, incremental, malformed, timeout, missing-model, unavailable, authentication, resource-limit, midstream-failure, and cancellation behavior offline."
    - "A consumer observes the first text delta before the fake/provider is permitted to complete."
    - "Cancellation and shutdown close local stream resources, remove in-flight work, and never become fallback or completed success."
  artifacts:
    - path: "aura_backend/providers/runtime.py"
      provides: "Deadline, in-flight cancellation, terminal-state, and provider-close ownership"
    - path: "tests/providers/fakes.py"
      provides: "Event-controlled scripted provider and safe call recorder"
    - path: "tests/providers/test_streaming.py"
      provides: "Adversarial incremental/cancellation/terminal-state tests"
  key_links:
    - from: "aura_backend/providers/runtime.py"
      to: "aura_backend/providers/base.py"
      via: "ProviderRuntime consumes only the Provider protocol and typed events"
      pattern: "ProviderRuntime"
    - from: "tests/providers/test_streaming.py"
      to: "tests/providers/fakes.py"
      via: "asyncio.Event gates first delta and completion independently"
      pattern: "Event"
---

<objective>
Implement the deterministic runtime semantics behind D-02 and D-03 before connecting a real adapter.

Purpose: Prove true incrementality, bounded deadlines, local cancellation, and honest terminal states without any model or network dependency.
Output: `ProviderRuntime`, a reusable scripted fake, and exhaustive offline stream/cancellation tests.
</objective>

## Artifacts this phase produces

- `tests/providers/fakes.py` — scripted request/stream/failure provider controlled by explicit events.
- `tests/providers/test_fake.py` — required deterministic fake outcome matrix.
- `tests/providers/test_streaming.py` — incremental, partial-failure, timeout, cancellation, and shutdown tests.
- `aura_backend/providers/runtime.py` — provider operation owner and in-flight registry.

<execution_context>
@/home/ty/.Codex/gsd-core/workflows/execute-plan.md
@/home/ty/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-provider-and-runtime-core/02-CONTEXT.md
@.planning/phases/02-provider-and-runtime-core/02-RESEARCH.md
@.planning/phases/02-provider-and-runtime-core/02-01-SUMMARY.md
@aura_backend/providers/base.py
@aura_backend/providers/errors.py
</context>

<dependencies>
Needs: Plan 02-01 typed request/result/event/failure symbols.
Creates: Runtime/fake seams used by all adapter tests and application shutdown.
Wave rationale: Executes after the contract, independently of provider-specific modules.
</dependencies>

<artifacts_produced>
- `tests/providers/fakes.py` — deterministic event-controlled adapter fake.
- `aura_backend/providers/runtime.py` — deadline, in-flight task, cancellation, and shutdown ownership.
- `tests/providers/test_fake.py` and `tests/providers/test_streaming.py` — deterministic incremental-stream and false-terminal proof.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 02-02-01: Build the event-controlled deterministic provider fake</name>
  <files>tests/providers/fakes.py, tests/providers/test_fake.py</files>
  <read_first>
    @aura_backend/providers/base.py
    @aura_backend/providers/errors.py
    @tests/support/main_subprocess_probe.py
    @.planning/phases/02-provider-and-runtime-core/02-RESEARCH.md
  </read_first>
  <behavior>
    - Script steps emit deltas, terminal completion, or a named typed failure without sleeping or contacting a service.
    - One event releases the first delta and a separate event releases completion, proving the response was not pre-buffered.
    - Normal, malformed, timeout, missing-model, unavailable, authentication, resource-limit, midstream-failure, and cancellation scripts produce distinct observable outcomes.
    - Fake diagnostics retain counts/codes only and never retain prompt or response content by default.
  </behavior>
  <action>Start with the required outcome matrix as RED. Implement a typed `ScriptedProvider` and recorder in test support using `asyncio.Event`, async-generator `try/finally`, and the production `Provider` protocol. Each script must have exactly one terminal instruction; invalid scripts fail construction. Make cancelled iteration set a cleanup event and re-raise cancellation. Do not implement correctness with timed sleeps, split a completed string, retain sensitive request text in assertion output, or use Ollama/cloud.</action>
  <acceptance_criteria>
    - The first delta is received while the completion gate remains closed.
    - Every non-success case lacks `Completed`; invalid double-terminal scripts are rejected.
    - Cancellation runs generator cleanup and exposes only safe counts/codes.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/providers/test_fake.py</automated>
  </verify>
  <done>AI-03 has one reusable offline fake that distinguishes all required outcomes and cannot fake streaming by replay.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-02-02: Own deadlines, in-flight cancellation, and shutdown in ProviderRuntime</name>
  <files>aura_backend/providers/runtime.py, tests/providers/test_streaming.py</files>
  <read_first>
    @aura_backend/providers/base.py
    @aura_backend/providers/errors.py
    @aura_backend/providers/config.py
    @tests/providers/fakes.py
  </read_first>
  <behavior>
    - `generate()` and `stream()` register the current operation by session/correlation key and remove it in `finally` for success, failure, timeout, and cancellation.
    - One outer `asyncio.timeout()` owns the absolute deadline; no hidden runtime retry converts timeout/resource failure into success.
    - `stream()` emits `Completed` exactly once only when the adapter did; partial failure, malformed ordering, timeout, or cancellation closes without completion.
    - `aclose()` cancels and awaits in-flight operations, closes the provider once, and leaves an empty registry.
  </behavior>
  <action>Write RED tests that block the scripted provider midstream, cancel the consumer, and close the runtime during active work. Implement `ProviderRuntime` around one selected `Provider`, a bounded in-flight task registry, an absolute timeout, terminal-event validation, `clear_session`, safe health delegation, and idempotent `aclose`. Catch `asyncio.CancelledError` only to record safe terminal metadata, then re-raise after cleanup. State explicitly in the runtime health/metrics model that upstream compute cancellation is `unknown`; guarantee only Aura's local task, iterator, connection/client, and registry cleanup per D-03.</action>
  <acceptance_criteria>
    - Cancellation and shutdown complete inside deterministic event bounds and registry size returns to zero.
    - Midstream failure after one or more deltas has safe partial-count evidence but no content and no `Completed`.
    - Provider `aclose()` is called exactly once even when startup/operation failed.
    - No assertion or public field claims remote compute or billing was stopped.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/providers/test_streaming.py</automated>
  </verify>
  <done>AI-02/AI-03 runtime semantics are locally cancellable, deadline-owned, and incapable of reporting partial work as complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Provider async iterator → runtime | Malformed ordering or late errors can create false completed answers. |
| Caller/shutdown → in-flight provider task | Cancellation must release local resources without masking failure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-PS | Tampering / Repudiation | stream state machine | mitigate | Event-gated tests and exactly-one-terminal validation; only `Completed` licenses success. |
| T-02-CD | Denial of Service | in-flight tasks and clients | mitigate | Absolute deadline, registry cleanup, cancel/await, idempotent close. |
| T-02-PL | Information Disclosure | fake/runtime diagnostics | mitigate | Counts, codes, provider/model/correlation allowlist only; no content retention. |
| T-02-SC | Tampering | package supply chain | mitigate | Standard library plus already locked test dependencies only. |
</threat_model>

<verification>
Run both focused provider files and `uv run --locked --no-sync python -m pytest -q tests/providers/test_contract.py`. A socket monkeypatch may be added to prove the whole lane is offline.
</verification>

<success_criteria>
- AI-03 has deterministic coverage for every locked outcome.
- D-03 true-stream/cancellation rules are enforced at runtime, including adversarial partial streams.
- Upstream cancellation remains explicitly unknown rather than overstated.
</success_criteria>

<output>
Create `.planning/phases/02-provider-and-runtime-core/02-02-SUMMARY.md` when done.
</output>
