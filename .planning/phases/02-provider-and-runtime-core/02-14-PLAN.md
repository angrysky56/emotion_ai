---
phase: 02-provider-and-runtime-core
plan: 14
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/evidence/phase-02/package-legitimacy.json
  - tests/test_dependency_audit.py
autonomous: true
requirements:
  - TEST-05
  - OPS-02
must_haves:
  truths:
    - "Every proposed package addition, removal, or dependency-group move has current registry/source/maintainer/age/install-script/entrypoint and import/consumer evidence."
    - "Unassessed, suspicious, missing, mismatched, or stale package evidence cannot authorize a manifest change."
    - "Supported API, MCP, Memvid, Docker, frontend, and test entry points are inventoried before a direct dependency is removed or made optional."
  artifacts:
    - path: ".planning/evidence/phase-02/package-legitimacy.json"
      provides: "Current allowlisted package and import/entrypoint audit"
    - path: "tests/test_dependency_audit.py"
      provides: "Fail-closed schema/freshness/candidate/entrypoint evidence gate"
  key_links:
    - from: "tests/test_dependency_audit.py"
      to: ".planning/evidence/phase-02/package-legitimacy.json"
      via: "Exact candidate set, evidence fields, freshness, and dispositions"
      pattern: "package-legitimacy"
---

<objective>
Create the current package-legitimacy and dependency-consumer evidence required before any Phase 2 manifest change.

Purpose: Repair the broken GSD helper gap with explicit fail-closed evidence and give Ty a concrete approval artifact.
Output: Audited package/import/entrypoint JSON and its deterministic validator; no install, removal, lock edit, or Git operation.
</objective>

## Artifacts this phase produces

- Current evidence for Ruff/Pyright additions and every proposed removal/group move.
- Exact active/import/entrypoint inventory covering alternate supported commands.
- Tests that reject missing/stale/ambiguous/unsafe audit evidence.

<execution_context>
@/home/ty/.Codex/gsd-core/workflows/execute-plan.md
@/home/ty/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-provider-and-runtime-core/02-CONTEXT.md
@.planning/phases/02-provider-and-runtime-core/02-RESEARCH.md
@pyproject.toml
@uv.lock
@requirements.txt
@package.json
@package-lock.json
@aura_backend/Dockerfile
</context>

<dependencies>
Needs: Current repository/manifests and research candidate list only.
Creates: Blocking evidence consumed by Plan 02-15 and manifest Plans 02-16/02-17.
Wave rationale: Read-only audit can run beside provider contract work with no shared files.
</dependencies>

<artifacts_produced>
- `.planning/evidence/phase-02/package-legitimacy.json` — dated registry/source/maintainer/script/entrypoint and import-use dispositions.
- `tests/test_dependency_audit.py` — freshness, candidate-completeness, legitimacy, import, and supported-entrypoint gates.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 02-14-01: Record current legitimacy for every addition/removal candidate</name>
  <files>.planning/evidence/phase-02/package-legitimacy.json, tests/test_dependency_audit.py</files>
  <read_first>
    @.planning/phases/02-provider-and-runtime-core/02-RESEARCH.md
    @pyproject.toml
    @uv.lock
    @package.json
    @package-lock.json
  </read_first>
  <action>Write the evidence-schema/candidate-set test first. Query only authoritative PyPI/npm metadata and official source repositories for exact proposed `ruff==0.12.7` and `pyright==1.1.413`, recording package name/version, registry URL, official source/owner, release date/age, maintainers/publisher, license, yanked/deprecated status, install/postinstall scripts, declared CLI entrypoint, and a verdict. Also record existing-package identity for every direct dependency proposed for removal or movement: `google-genai`, `mcp`, `fastmcp`, `memvid-sdk`, media packages, `faiss-cpu`, `faiss-gpu-cu12`, `anthropic`, `asyncio-mqtt`, `websockets`, and frontend `@google/genai`. A registry hit alone is not approval. Mark any missing/mismatch/stale/ambiguous signal `UNASSESSED` or `SUS`; never convert it to OK. Do not install or execute package code.</action>
  <acceptance_criteria>
    - JSON contains the exact full candidate set and every required field with retrieval timestamp/source URL.
    - Validator rejects stale, omitted, duplicate, unexpected, malformed, unassessed-as-approved, and suspicious-as-approved variants.
    - Ruff/Pyright executable names and install-script facts are evidenced from authoritative metadata, not guessed.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/test_dependency_audit.py -k 'package or legitimacy or freshness or candidate'</automated>
  </verify>
  <done>Every manifest candidate has current, reviewable legitimacy evidence and non-OK evidence blocks later mutation.</done>
</task>

<task type="auto">
  <name>Task 02-14-02: Bind dependency moves/removals to actual imports and supported entry points</name>
  <files>.planning/evidence/phase-02/package-legitimacy.json, tests/test_dependency_audit.py</files>
  <read_first>
    @.planning/codebase/STACK.md
    @.planning/codebase/INTEGRATIONS.md
    @aura_backend/main.py
    @aura_backend/aura_server.py
    @aura_backend/aura_as_mcp_server.py
    @aura_backend/aura_real_memvid.py
    @index.tsx
    @src/services/auraApi.ts
  </read_first>
  <action>Inventory import consumers and smoke commands for the API/runtime CLI, standalone Aura MCP server, companion MCP wrapper, active Memvid integration, deterministic tests, TypeScript type-check, and frontend build. Store exact source paths/import symbols and an evidence-backed disposition for each direct dependency: keep-base, move-to-named-extra/group, remove-direct, or retain-pending. Prove `@google/genai` has no active TypeScript consumer before removal; prove Python removal candidates have no active/supported consumer; retain Torch/Chroma/sentence-transformers because the characterized persistence path still needs them. Tests must reject a move/removal when any active or supported entry point lacks a package lane. Do not import/open production databases, start MCP/provider services, inspect personal data, or change a manifest.</action>
  <acceptance_criteria>
    - Every supported entry point maps to the dependency lane that can execute it.
    - No direct package is removed/moved solely because it is large or absent from `main.py`.
    - Storage dependencies and Phase 3 roots remain unchanged; evidence contains paths/symbols/counts only.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/test_dependency_audit.py</automated>
  </verify>
  <done>Manifest changes are licensed only by complete import and supported-entrypoint evidence, not an incomplete grep or size judgment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Package registries/source metadata → manifest decision | Names and provenance may be deceptive, stale, or incomplete. |
| Source import scan → removal/group decision | Alternate entry points can be missed and silently broken. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-DP | Spoofing / Tampering | package identity | mitigate | Registry plus official-source/owner/age/script/entrypoint evidence and fail-closed verdict. |
| T-02-DE | Repudiation | dependency consumer audit | mitigate | Exact supported entrypoint/import mapping; synthetic omitted-consumer tests. |
| T-02-PL | Information Disclosure | audit evidence | mitigate | Record paths/symbols/metadata only; no `.env`, package execution, or personal data. |
| T-02-SC | Tampering | package supply chain | mitigate | This is the legitimacy gate itself; no install/change occurs. |
</threat_model>

<verification>
Run the audit validator against canonical evidence and adversarial mutated copies. Confirm Git diff contains only evidence/test files and no manifest, lock, installed artifact, or runtime-data change.
</verification>

<success_criteria>
- OPS-02 package decisions have current legitimacy/import/entrypoint evidence.
- D-05 package changes remain blocked until explicit approval.
- No large or optional package is implicitly installed by the audit.
</success_criteria>

<output>
Create `.planning/phases/02-provider-and-runtime-core/02-14-SUMMARY.md` when done.
</output>
