---
phase: 02-provider-and-runtime-core
plan: 08
type: tdd
wave: 6
depends_on:
  - 02-07
files_modified:
  - aura_backend/main.py
  - aura_backend/runtime/app.py
  - tests/runtime/test_import_safety.py
  - tests/runtime/test_app_lifespan.py
  - tests/support/main_subprocess_probe.py
autonomous: true
requirements:
  - TEST-03
  - OPS-01
must_haves:
  truths:
    - "Importing aura_backend.main or any provider/runtime module has zero client, service, model, process, database, or data-write side effects."
    - "create_app() remains import-compatible and resource-free; FastAPI lifespan constructs one ApplicationRuntime and closes it exactly once."
    - "Partial startup fails readiness and unwinds cleanly while Phase 1 loopback/CORS/no-auth behavior remains unchanged."
  artifacts:
    - path: "tests/runtime/test_import_safety.py"
      provides: "Bounded subprocess side-effect traps for main/provider/runtime imports"
    - path: "tests/runtime/test_app_lifespan.py"
      provides: "Context-managed TestClient lifecycle integration tests"
    - path: "aura_backend/main.py"
      provides: "Pure create_app/app plus lifespan-owned runtime composition"
  key_links:
    - from: "aura_backend/main.py"
      to: "aura_backend/runtime/app.py"
      via: "create_app runtime_builder and lifespan app.state.runtime ownership"
      pattern: "create_app|app.state.runtime"
---

<objective>
Move Aura's FastAPI composition onto the explicit runtime boundary required by D-04.

Purpose: Make imports safe and lifecycle behavior deterministic without yet changing the characterized conversation route.
Output: Pure application factory, lifespan wiring, import traps, and lifecycle integration tests.
</objective>

## Artifacts this phase produces

- Resource-free `create_app()` and compatibility `app = create_app()`.
- Concrete runtime builder called only inside lifespan.
- Subprocess import-safety and TestClient lifespan tests.

<execution_context>
@/home/ty/.Codex/gsd-core/workflows/execute-plan.md
@/home/ty/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-provider-and-runtime-core/02-CONTEXT.md
@.planning/phases/02-provider-and-runtime-core/02-RESEARCH.md
@.planning/phases/02-provider-and-runtime-core/02-PATTERNS.md
@.planning/phases/02-provider-and-runtime-core/02-07-SUMMARY.md
@.planning/phases/01-preservation-and-trusted-baseline/01-05-SUMMARY.md
@aura_backend/main.py
@tests/support/main_subprocess_probe.py
</context>

<dependencies>
Needs: ApplicationRuntime/resource stack.
Creates: Import-safe FastAPI composition consumed by conversation, health, and CLI plans.
Wave rationale: `main.py` is changed only after generic lifecycle behavior is green.
</dependencies>

<artifacts_produced>
- `aura_backend/main.py` — import-safe `create_app()` and lifespan integration.
- `tests/support/main_subprocess_probe.py` — strengthened import-side-effect counters and repository-write evidence.
- `tests/runtime/test_import_safety.py` and `tests/runtime/test_app_lifespan.py` — subprocess import and lifespan ownership proof.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 02-08-01: Prove main and provider imports have no runtime effects</name>
  <files>aura_backend/main.py, tests/runtime/test_import_safety.py, tests/support/main_subprocess_probe.py</files>
  <read_first>
    @aura_backend/main.py
    @aura_backend/providers/factory.py
    @aura_backend/shared_embedding_service.py
    @tests/support/main_subprocess_probe.py
    @tests/api/test_local_boundary.py
  </read_first>
  <behavior>
    - Bounded child imports `aura_backend.main`, provider factory, and every adapter with socket connects, subprocess starts, model/client construction, Chroma/database open, and repository/temp writes trapped.
    - Imports succeed when unselected optional provider modules are unavailable.
    - Provider factory and embedding/database/MCP/autonomic construction call counts remain zero.
    - Child crash, timeout, malformed/partial evidence, or any trapped effect is non-pass with bounded diagnostics.
  </behavior>
  <action>Write the inverted Phase 1 import probe first: production factory construction is now forbidden even without faking the factory itself. Move `load_dotenv`, environment logging, shared embedding access, provider/client construction, and resource-owning heavy imports into the explicit runtime build/start path. Use `TYPE_CHECKING` or local imports for annotations where needed. Keep top-level Pydantic route models, middleware, and `app = create_app()` available. Extend the subprocess probe with safe call counters and before/after repository-root metadata; never read `.env` values.</action>
  <acceptance_criteria>
    - Import probes report complete success with zero trapped calls/writes and no optional SDK requirement.
    - A deliberate effect/timeout/crash remains a distinct failing test.
    - Existing local-boundary probe still imports the real app and leaves data roots unchanged.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/runtime/test_import_safety.py tests/api/test_local_boundary.py -k 'import or probe or wildcard or host'</automated>
  </verify>
  <done>Importing Aura's app/providers is pure and fail-closed evidence proves no runtime work occurred.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-08-02: Wire create_app lifespan to one ApplicationRuntime</name>
  <files>aura_backend/main.py, aura_backend/runtime/app.py, tests/runtime/test_app_lifespan.py</files>
  <read_first>
    @aura_backend/runtime/app.py
    @aura_backend/main.py
    @tests/runtime/test_lifecycle.py
    @tests/api/test_filesystem_contract.py
  </read_first>
  <behavior>
    - `create_app(runtime_builder=...)` stores the builder, installs the existing local middleware/routes once, and performs no initialization.
    - Context-managed `TestClient` starts the fake runtime inside the context and closes it after; non-context construction does not pretend lifespan ran.
    - Startup failure yields no ready runtime and closes already started fake resources in reverse order.
    - CORS remains explicit with credentials disabled and the compatibility module-level `app` remains available.
  </behavior>
  <action>Extract an application factory and have its FastAPI lifespan call one runtime builder, `await runtime.start()`, assign `app.state.runtime`, then `await runtime.aclose()` in `finally`. The concrete builder may retain current service order and resources, but all construction must occur inside start; transitional aliases for untouched routes must reference lifespan-owned resources and must not construct anything themselves. Use context-managed TestClient. Preserve Phase 1 middleware, routes, storage paths, and no-auth behavior.</action>
  <acceptance_criteria>
    - Fake runtime start/close counts and ordering are exact for normal and failed startup.
    - `app = create_app()` imports successfully without entering lifespan.
    - Phase 1 local/filesystem tests remain green and no original/backup database is opened by focused tests.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/runtime/test_app_lifespan.py tests/api/test_local_boundary.py tests/api/test_filesystem_contract.py</automated>
  </verify>
  <done>FastAPI owns exactly one explicit runtime through lifespan while Aura remains import-compatible and local-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Python import → local host/data/services | Import must not create external or persistent side effects. |
| FastAPI lifespan → runtime resources | Startup/shutdown must pair even on partial failure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-IS | Tampering / Denial of Service | import path | mitigate | Subprocess traps for network/process/client/model/database/write effects and hard timeout. |
| T-02-LC | Repudiation | lifespan state | mitigate | Context-managed tests, explicit started/closed evidence, partial unwind. |
| T-02-LH | Information Disclosure | local middleware | mitigate | Preserve loopback, explicit origins, strict JSON, credentials off, no mandatory auth. |
| T-02-SC | Tampering | package supply chain | mitigate | No dependency changes. |
</threat_model>

<verification>
Run import/lifespan tests, all Phase 1 API/characterization tests, and the full deterministic suite. Snapshot repository data roots before/after; no live service/model is allowed.
</verification>

<success_criteria>
- OPS-01/D-04 have a pure app factory and lifespan-owned runtime.
- Imports prove zero side effects rather than merely avoiding lifespan.
- Phase 1 network, no-auth, filesystem, companion, and persistence contracts remain intact.
</success_criteria>

<output>
Create `.planning/phases/02-provider-and-runtime-core/02-08-SUMMARY.md` when done.
</output>
