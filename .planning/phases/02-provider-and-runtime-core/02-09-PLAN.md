---
phase: 02-provider-and-runtime-core
plan: 09
type: tdd
wave: 7
depends_on:
  - 02-03
  - 02-08
files_modified:
  - aura_backend/main.py
  - aura_backend/conversation/__init__.py
  - aura_backend/conversation/analysis.py
  - tests/conversation/test_analysis_transport.py
  - tests/api/test_provider_compatibility.py
  - tests/support/main_subprocess_probe.py
autonomous: true
requirements:
  - TEST-03
  - AI-01
  - AI-03
must_haves:
  truths:
    - "The complete conversation path, including three existing analysis calls, uses only the selected typed provider runtime and neutral tool catalog."
    - "The seven-field response, HTTP-200 provider fallback, one session clear, immediate persistence arguments, and persistence degradation behavior remain exactly characterized."
    - "Provider/source errors, credentials, prompts, conversation/tool content, raw SDK objects, and hidden reasoning do not enter public errors or default logs."
  artifacts:
    - path: "aura_backend/conversation/analysis.py"
      provides: "Provider-neutral transport for existing emotion/cognitive prompts and parsers"
    - path: "tests/api/test_provider_compatibility.py"
      provides: "Real-route selected-provider neutrality and compatibility tests"
    - path: "aura_backend/main.py"
      provides: "Runtime-owned provider/tool route wiring with preserved public response"
  key_links:
    - from: "aura_backend/main.py"
      to: "aura_backend/providers/runtime.py"
      via: "request.app.state.runtime.provider_runtime generate/clear calls"
      pattern: "app.state.runtime"
    - from: "aura_backend/conversation/analysis.py"
      to: "aura_backend/providers/base.py"
      via: "ProviderRequest and ProviderResult only"
      pattern: "ProviderRequest"
---

<objective>
Switch the complete non-streaming conversation path onto the typed runtime without changing Aura's Phase 1 public/persistence behavior.

Purpose: Make Ollama/Gemini/OpenRouter genuinely interchangeable in orchestration and close the route's raw-error/logging leaks.
Output: Provider-neutral analysis service, runtime-owned route, and compatibility/adversarial tests.
</objective>

## Artifacts this phase produces

- Provider-neutral emotion/cognitive analysis transport preserving current prompts/parsers/defaults.
- Real `/conversation` route integration through `app.state.runtime`.
- Extended subprocess/API evidence for success, failure, persistence, no-cloud, and redaction behavior.

<execution_context>
@/home/ty/.Codex/gsd-core/workflows/execute-plan.md
@/home/ty/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-provider-and-runtime-core/02-CONTEXT.md
@.planning/phases/02-provider-and-runtime-core/02-RESEARCH.md
@.planning/phases/02-provider-and-runtime-core/02-08-SUMMARY.md
@.planning/phases/01-preservation-and-trusted-baseline/01-07-SUMMARY.md
@aura_backend/main.py
@tests/characterization/test_companion_contract.py
@tests/characterization/test_persistence_contract.py
</context>

<dependencies>
Needs: Neutral tools, selected provider runtime, import-safe app/lifespan.
Creates: Complete provider-neutral non-stream conversation seam consumed by health/startup.
Wave rationale: `main.py` route wiring follows lifecycle conversion and is sequential on the shared file.
</dependencies>

<artifacts_produced>
- `aura_backend/conversation/analysis.py` — provider-neutral transport for existing emotion/cognitive analyses.
- `aura_backend/main.py` and `aura_backend/conversation/__init__.py` — selected-provider route wiring, safe conversation exports, and redaction.
- `tests/conversation/test_analysis_transport.py` and `tests/api/test_provider_compatibility.py` — analysis and seven-field/fallback/persistence compatibility proof.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 02-09-01: Route existing emotion/cognitive analyses through the selected provider</name>
  <files>aura_backend/conversation/__init__.py, aura_backend/conversation/analysis.py, aura_backend/main.py, tests/conversation/test_analysis_transport.py</files>
  <read_first>
    @aura_backend/main.py
    @aura_backend/providers/base.py
    @aura_backend/providers/runtime.py
    @tests/characterization/test_companion_contract.py
  </read_first>
  <behavior>
    - User emotion, Aura emotion, and cognitive focus each issue a typed provider request through one injected runtime with no provider-name or SDK branch.
    - Existing prompt content, parser mappings, defaults, and returned domain DTOs remain behaviorally equivalent; prompt-quality redesign is absent.
    - An Ollama-selected fake services answer plus analyses while Google/OpenAI constructors are forbidden.
    - Analysis failure returns the existing safe default/None behavior without logging source content or raw provider failure.
  </behavior>
  <action>Extract the three `detect_*` transport/parsing functions into `conversation/analysis.py`, injecting a provider-runtime callable and retaining current mappings/prompts/parsers/defaults. Adapt the main route to invoke this service rather than global `client.models`. Tests should use a recording selected-provider fake and tripwire all SDK constructors. Do not revise emotion labels, claims, prompt wording, response schema, or psychological quality; Phase 4 owns those decisions.</action>
  <acceptance_criteria>
    - One fake sees the primary answer and analysis requests with typed DTOs; no global client exists in the path.
    - Existing safe fallback values remain exact for malformed/unavailable analysis output.
    - Tests assert no prompt/response/provider-error sentinel is emitted to public/default diagnostics.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/conversation/test_analysis_transport.py</automated>
  </verify>
  <done>The complete analysis path follows the selected provider without changing the Phase 4-owned product semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-09-02: Preserve the seven-field route and persistence/session contract</name>
  <files>aura_backend/main.py, tests/api/test_provider_compatibility.py, tests/support/main_subprocess_probe.py</files>
  <read_first>
    @aura_backend/main.py
    @aura_backend/runtime/app.py
    @aura_backend/providers/runtime.py
    @aura_backend/providers/tools.py
    @tests/support/main_subprocess_probe.py
    @tests/characterization/test_companion_contract.py
    @tests/characterization/test_persistence_contract.py
  </read_first>
  <behavior>
    - The route obtains provider runtime/tool catalog from `request.app.state.runtime` and contains no provider-name, SDK-response, private bridge-map, or raw-response branch.
    - Success emits exactly the seven characterized JSON keys/types and persists one exchange with `update_profile=True` and the configured timeout.
    - Every typed provider failure/empty invalid success maps to the same nonempty HTTP-200 fallback, exactly one session clear, zero persistence calls, and no source error exposure.
    - Cancellation propagates and does not return HTTP-200 fallback; persistence degradation preserves the visible answer and one bounded background attempt as characterized.
  </behavior>
  <action>Write route tests for every `ProviderErrorCode`, cancellation, and provider-neutral tool catalog before changing the handler. Replace module-global provider/bridge usage with lifespan runtime access while preserving the one-message-plus-system-instruction input, response fields, immediate persistence exchange/options, fallback text/status, session cleanup, and local/no-auth boundary. Remove raw answer/thought previews, raw exception/session logging, raw SDK inspection, and private `_tool_mapping` access. Map source failures to safe internal codes/correlation IDs, then at the public route edge preserve the Phase 1 fallback per D-02.</action>
  <acceptance_criteria>
    - Phase 1 companion/persistence/local tests pass unchanged or are adapted only to inject the new production seam, never to relax assertions.
    - All typed provider failures are adversarially checked for identical public fallback/session/persistence behavior and source redaction.
    - Cancellation raises to the ASGI task and leaves no in-flight/session registry entry or persistence call.
    - No provider/SDK/private MCP branching remains in the conversation/analysis call path.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/api/test_provider_compatibility.py tests/characterization/test_companion_contract.py tests/characterization/test_persistence_contract.py tests/api/test_local_boundary.py</automated>
  </verify>
  <done>Aura's non-streaming conversation is adapter-neutral internally and byte/schema compatible at the public and persistence boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local browser request → conversation runtime | User identifiers/content enter model, analysis, tools, and persistence. |
| Provider/tool/source failure → public response/log | Sensitive/raw failures must not escape while compatibility fallback remains. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-PV | Tampering / Repudiation | route success/failure mapping | mitigate | Typed provider failures, exact fallback/session/persistence matrix, cancellation propagation. |
| T-02-PL | Information Disclosure | logs/public errors | mitigate | Remove previews/raw errors/SDK objects; safe codes/correlation IDs and sentinel tests. |
| T-02-LH | Spoofing / Information Disclosure | local route | mitigate | Preserve explicit origins, strict JSON, loopback, no credentials, no auth expansion. |
| T-02-SC | Tampering | package supply chain | mitigate | No dependency changes. |
</threat_model>

<verification>
Run focused analysis/compatibility tests, all Phase 1 API/characterization tests, and full deterministic pytest. Provider/SDK/network constructors are forbidden in the focused route tests.
</verification>

<success_criteria>
- AI-01 is true for the complete conversation path, not just adapters.
- TEST-03 covers translation, every failure, cancellation, session cleanup, persistence, and redaction.
- D-02 retains the seven-field HTTP-200 fallback and D-03 keeps cancellation from becoming success.
</success_criteria>

<output>
Create `.planning/phases/02-provider-and-runtime-core/02-09-SUMMARY.md` when done.
</output>
