---
phase: 02-provider-and-runtime-core
plan: 13
type: execute
wave: 11
depends_on:
  - 02-12
files_modified:
  - README.md
  - aura_backend/STARTUP_GUIDE.md
  - .env.example
  - tests/runtime/test_startup_docs.py
autonomous: true
requirements:
  - TEST-03
  - OPS-01
must_haves:
  truths:
    - "The documented startup/preflight commands are executable, cross-platform, locked/no-sync, and match the wrapper/runtime behavior."
    - "The example configuration is local-first Ollama, loopback-only by default, contains no real secret, and makes cloud providers explicit."
    - "Documentation does not claim installers, automatic model downloads, hidden cloud fallback, remote cancellation guarantees, auth, or readiness that executable evidence does not support."
  artifacts:
    - path: "README.md"
      provides: "Canonical Phase 2 startup/preflight instructions"
    - path: ".env.example"
      provides: "Non-secret local-first provider/runtime example"
    - path: "tests/runtime/test_startup_docs.py"
      provides: "Documentation/command/config drift gate"
  key_links:
    - from: "README.md"
      to: "aura_backend/runtime/__main__.py"
      via: "Exact canonical preflight/serve commands"
      pattern: "python -m aura_backend.runtime"
---

<objective>
Publish only the startup/provider claims now supported by executable Phase 2 behavior, per D-01, D-05, and D-06.

Purpose: Prevent users from following unsafe legacy setup instructions or assuming a cloud/model/cancellation guarantee.
Output: Scoped README/startup-guide/example-config corrections and a drift test.
</objective>

## Artifacts this phase produces

- Canonical setup/preflight/serve instructions.
- Local-first non-secret `.env.example`.
- Static drift tests tied to actual runtime/wrappers.

<execution_context>
@/home/ty/.Codex/gsd-core/workflows/execute-plan.md
@/home/ty/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-provider-and-runtime-core/02-CONTEXT.md
@.planning/phases/02-provider-and-runtime-core/02-RESEARCH.md
@.planning/phases/02-provider-and-runtime-core/02-12-SUMMARY.md
@README.md
@aura_backend/STARTUP_GUIDE.md
@.env.example
</context>

<dependencies>
Needs: Final canonical CLI and wrapper commands.
Creates: User-facing startup/config truth consumed by later operations/release work.
Wave rationale: Documentation follows executable behavior, never precedes it.
</dependencies>

<artifacts_produced>
- `README.md`, `aura_backend/STARTUP_GUIDE.md`, and `.env.example` — canonical local-first startup, preflight, provider, and truth-boundary guidance.
- `tests/runtime/test_startup_docs.py` — executable documentation/config drift checks.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 02-13-01: Document canonical preflight, serve, and truth boundaries</name>
  <files>README.md, aura_backend/STARTUP_GUIDE.md, tests/runtime/test_startup_docs.py</files>
  <read_first>
    @README.md
    @aura_backend/STARTUP_GUIDE.md
    @aura_backend/runtime/cli.py
    @tests/runtime/test_startup_entrypoints.py
  </read_first>
  <action>Write a static drift test that extracts documented commands and compares them to the canonical locked/no-sync CLI/wrapper contract. Replace only startup/provider/runtime passages: explicit setup is `uv sync --locked` and `npm ci`; normal execution is `uv run --locked --no-sync python -m aura_backend.runtime preflight` then `... serve`; wrappers are delegates. Explain exact preflight statuses, loopback/no-sign-in boundary, explicit LAN warning, selected-model requirement, optional cloud credentials, marked live checks, and local-only cancellation guarantee. Remove claims that startup installs/kills/downloads or that remote compute/billing cancellation is guaranteed. Leave Phase 3-6 documentation areas scoped for their owning phases rather than rewriting them.</action>
  <acceptance_criteria>
    - Every documented command is present in/accepted by the runtime CLI or wrapper contract.
    - No obsolete direct `python main.py`, install-on-start, kill-port, reload, wildcard/LAN-default, or silent-cloud instruction remains in the supported startup sections.
    - Limitations and environment-blocked/live results are named honestly.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/runtime/test_startup_docs.py tests/runtime/test_startup_entrypoints.py</automated>
  </verify>
  <done>Ty has one concise, executable startup path and accurate statements about provider/live/cancellation limits.</done>
</task>

<task type="auto">
  <name>Task 02-13-02: Make the example runtime configuration local-first and non-secret</name>
  <files>.env.example, tests/runtime/test_startup_docs.py</files>
  <read_first>
    @.env.example
    @aura_backend/providers/config.py
    @aura_backend/runtime/config.py
    @aura_backend/runtime_security.py
  </read_first>
  <action>Consolidate duplicate provider/model declarations in `.env.example`, set `AURA_DEFAULT_PROVIDER=ollama`, use a documented explicit example model without reserving `ornith:latest` as the normal default, and keep `AURA_HOST=127.0.0.1`. Retain recognized key names. Put Gemini/OpenRouter credentials and autonomic cloud behavior in commented explicit sections with obvious non-secret sentinels; do not include active keys, read `.env`, rename/migrate real environment state, or make cloud/autonomic implicit. Add tests for duplicate keys, secret-like values, loopback default, local provider, and agreement with settings parsing.</action>
  <acceptance_criteria>
    - Each active provider/runtime key appears once and parsing selects Ollama/loopback without a cloud key.
    - Secret scanners/sentinel assertions find no real credential material.
    - Ornith remains documented only for the opt-in bounded live lane.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/runtime/test_startup_docs.py -k 'env or provider or loopback or secret'</automated>
  </verify>
  <done>The checked-in example starts from Aura's private local trust model and cannot create a silent cloud dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Documentation/example config → operator action | Stale instructions can trigger unsafe installs, exposure, or cloud cost. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-ST | Tampering / Elevation | startup docs | mitigate | Exact executable command tests; setup separated from run; no mutating startup claims. |
| T-02-PC | Spoofing / Cost | example provider config | mitigate | Ollama default, cloud sections commented/explicit, unknown selection fails. |
| T-02-PL | Information Disclosure | example secrets/claims | mitigate | Non-secret sentinels, duplicate/secret tests, no `.env` reads. |
| T-02-SC | Tampering | package supply chain | mitigate | Docs name locked setup only; no package changes in this plan. |
</threat_model>

<verification>
Run startup-doc/entrypoint/CLI tests and inspect only the scoped documentation diff. Do not start services or read the real `.env`.
</verification>

<success_criteria>
- OPS-01 startup and preflight are documented exactly.
- D-01 local-first provider and D-06 live/cancellation truth are clear.
- Documentation drift cannot silently reintroduce unsafe launcher claims.
</success_criteria>

<output>
Create `.planning/phases/02-provider-and-runtime-core/02-13-SUMMARY.md` when done.
</output>
