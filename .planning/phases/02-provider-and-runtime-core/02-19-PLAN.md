---
phase: 02-provider-and-runtime-core
plan: 19
type: execute
wave: 13
depends_on: [02-18]
files_modified:
  - aura_backend/main.py
  - aura_backend/mcp_system.py
  - aura_backend/runtime/config.py
  - tests/runtime/test_base_install_startup.py
  - tests/runtime/test_optional_integrations.py
  - tests/support/main_subprocess_probe.py
  - tests/test_python_dependency_contract.py
autonomous: true
gap_closure: true
requirements: [TEST-03, AI-01, OPS-01, OPS-02]
must_haves:
  truths:
    - "The documented base uv installation starts default loopback Ollama preflight, serve delegation, and FastAPI lifespan without Gemini, MCP/FastMCP, or Memvid extras."
    - "MCP, the Gemini bridge, Memvid, and autonomic behavior are four distinct optional lifecycle stages that start only after explicit enablement/selection and close exactly once in reverse order."
    - "An explicitly enabled but unavailable optional stage is visible through a fixed redacted status and cannot make an otherwise valid base Ollama runtime unready."
    - "When its declared lane is installed and explicitly selected, each real optional import and application start seam receives bounded no-network/no-user-data smoke evidence; an absent lane is recorded as conditional not_run, never pass."
    - "Provider-neutral MCP tools, async Gemini behavior, Memvid capability, autonomic provider neutrality, Phase 1 compatibility/data, loopback, and no-sign-in behavior remain intact."
  artifacts:
    - path: "tests/runtime/test_base_install_startup.py"
      provides: "Missing-extra import, preflight, serve, lifespan, status, and cleanup proof"
    - path: "tests/runtime/test_optional_integrations.py"
      provides: "Four-stage enablement, success/failure/cleanup, and conditional real-extra smoke contracts"
    - path: "aura_backend/main.py"
      provides: "Required base composition separated from four optional lifecycle stages"
    - path: "aura_backend/mcp_system.py"
      provides: "Provider-neutral MCP lifecycle with separately lazy Gemini bridge construction"
  key_links:
    - from: "aura_backend/main.py"
      to: "aura_backend/runtime/app.py"
      via: "required base services plus four required=False ResourceFactory stages"
      pattern: "ResourceFactory|required=False"
    - from: "aura_backend/mcp_system.py"
      to: "aura_backend/mcp_to_gemini_bridge.py"
      via: "local bridge import only after MCP enablement and explicit Gemini selection"
      pattern: "MCPGeminiBridge"
    - from: "aura_backend/runtime/config.py"
      to: "aura_backend/main.py"
      via: "strict AURA_MCP_ENABLED, AURA_MEMVID_ENABLED, and AUTONOMIC_ENABLED false-default stage selection"
      pattern: "AURA_MCP_ENABLED|AURA_MEMVID_ENABLED|AUTONOMIC_ENABLED"
---

<objective>
Close the locally reproducible base-install/runtime blocker without changing dependency authority or pretending remote CI ran.

Purpose: Make base-only Ollama genuinely startable while retaining every optional capability behind an explicit, tested, lifecycle-owned boundary.
Output: Missing-extra TDD evidence, four optional-stage contracts, lazy runtime composition, safe status/remediation, and installed-lane preservation proof.
</objective>

## Artifacts this plan produces

- A subprocess-backed base-install contract that blocks every optional module root and proves import, preflight, serve delegation, lifespan readiness, and shutdown without network or data access.
- Explicit MCP, Gemini-bridge, Memvid, and autonomic lifecycle stages with deterministic enabled-success, unavailable, partial-start, and cleanup cases.
- Conditional real declared-extra import/start smoke results that remain `not_run` when a lane is absent.
- Base/optional entrypoint drift gates without any manifest or lock edit.

<execution_context>
@/home/ty/.Codex/gsd-core/workflows/execute-plan.md
@/home/ty/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-provider-and-runtime-core/02-CONTEXT.md
@.planning/phases/02-provider-and-runtime-core/02-RESEARCH.md
@.planning/phases/02-provider-and-runtime-core/02-PATTERNS.md
@.planning/phases/02-provider-and-runtime-core/02-VERIFICATION.md
@.planning/phases/02-provider-and-runtime-core/02-07-SUMMARY.md
@.planning/phases/02-provider-and-runtime-core/02-08-SUMMARY.md
@.planning/phases/02-provider-and-runtime-core/02-13-SUMMARY.md
@.planning/phases/02-provider-and-runtime-core/02-16-SUMMARY.md
@.planning/phases/02-provider-and-runtime-core/02-18-SUMMARY.md
@aura_backend/main.py
@aura_backend/runtime/app.py
@aura_backend/runtime/config.py
@aura_backend/runtime/health.py
@aura_backend/mcp_system.py
@aura_backend/mcp_to_gemini_bridge.py
@tests/support/main_subprocess_probe.py
</context>

<dependencies>
Needs: Completed lifecycle/provider/tool boundaries and declared optional dependency lanes from Plans 02-01 through 02-18.
Creates: Local base-startup closure consumed by the final command-contract gap plan.
Wave rationale: Runs after 02-18 because independent verification discovered the all-extras-only startup defect.
</dependencies>

<scope_fence>
- Preserve Ty's exact 16-approved / 4-rejected dependency boundary. Do not edit `pyproject.toml`, `uv.lock`, `package.json`, `package-lock.json`, `requirements.txt`, package evidence, or any rejected declaration/lock record.
- Do not run environment sync/install commands, a live provider/model, MCP subprocess, or user-data operation. Use only the existing locked environment with `uv run --locked --no-sync`.
- Preserve Phase 1's seven response fields, HTTP-200 provider fallback, session/persistence behavior, data-root bytes, loopback/no-sign-in boundary, and absence of mandatory authentication.
- Do not address or mark complete the remote GitHub/Pyright blocker. A local import/static smoke or installed package is not a completed clean-CI typing result.
- Do not broaden into npm audit findings, storage migration, prompt redesign, frontend refactor, live Ornith, auth, deployment, or Git-history work.
</scope_fence>

<tasks>

<task type="auto" tdd="true">
  <name>Task 02-19-01: Specify base-only startup and four optional-stage contracts</name>
  <files>tests/runtime/test_base_install_startup.py, tests/runtime/test_optional_integrations.py, tests/support/main_subprocess_probe.py, tests/test_python_dependency_contract.py</files>
  <read_first>
    @.planning/phases/02-provider-and-runtime-core/02-VERIFICATION.md
    @pyproject.toml
    @aura_backend/main.py
    @aura_backend/runtime/app.py
    @aura_backend/runtime/cli.py
    @aura_backend/mcp_system.py
    @aura_backend/mcp_to_gemini_bridge.py
    @aura_backend/memvid_archival_service.py
    @aura_backend/aura_autonomic_system.py
    @tests/support/main_subprocess_probe.py
    @tests/test_python_dependency_contract.py
  </read_first>
  <behavior>
    - A bounded child blocks imports of `mcp`, `fastmcp`, `google.genai`, and `memvid_sdk`, traps network/process/database/repository writes, and still imports the public app/runtime under default Ollama.
    - With injected base-resource/provider/process probes, that child obtains passing preflight, exercises exact serve delegation/exit propagation, enters production FastAPI lifespan, reaches ready state, and closes every started fake once in reverse order.
    - Missing/partial child evidence, timeout, swallowed import error, optional import attempt, side effect, or claimed readiness before completed startup is non-pass.
    - Strict explicit enablement covers four named stages independently: MCP client/tools, Gemini bridge, Memvid archival, and autonomic system. Each has disabled, enabled-success, enabled-missing-extra, partial-start, and exactly-once cleanup cases.
    - MCP enabled with Ollama never imports/constructs Google; Gemini bridge success requires both MCP enablement and explicit Gemini selection. Memvid and autonomic enablement are independent of MCP.
    - Enabled-but-missing optional extras use a fixed safe optional failure code, preserve overall local readiness, and expose no exception/module path, secret, prompt, tool payload, response, or file content.
    - Conditional real-lane smoke runs only when the exact declared extra is installed and explicitly selected: import the real external module and enter/close the real application integration seam with clients/processes/storage replaced by no-I/O fakes. Missing lanes yield structured `not_run: declared_extra_not_installed`; skipped/not_run is never summarized as pass.
    - The real-lane matrix is explicit: MCP imports both `mcp` and `fastmcp`; Gemini bridge imports real `google.genai.types` and converts fake tool definitions; Memvid imports `memvid_sdk` and starts/closes its injected no-storage facade; autonomic imports the real application module and starts/closes with an injected provider/bridge without a model call.
    - Base `aura_backend.main`/`aura_backend.runtime` require only the base lane; explicit Gemini, standalone MCP, and Memvid entry points retain their declared extras.
  </behavior>
  <action>Write the complete RED contract before production edits. Add a dedicated child scenario rather than uninstalling packages. Reuse repository data-root/effect traps and inject deterministic base resource/provider/serve probes while leaving the production app builder and lifecycle sequencing under test. Record complete structured evidence: blocked roots, attempted optional imports, selected provider, loopback host, preflight checks, serve argv/exit, resource states/codes, start/close events, readiness, and unchanged data-root digest. In `test_optional_integrations.py`, use recording stage fakes to specify exact four-stage order and cleanup, then add conditional real-extra smoke helpers that first inspect the authoritative installed distribution/module, emit a typed pass/not_run outcome, and forbid network, subprocess, database, repository/user-data reads or writes. For real installed lanes, exercise only import-safe public integration constructors/converters with injected clients and temporary in-memory/no-op resources: no service connection, model request, MCP child, archive creation, or personal content. Add explicit assertions against the existing provider Gemini, MCP parameter/result formatting, autonomic-provider, and new Memvid seam contracts. Strengthen dependency-entrypoint assertions for base-only truth and exact optional lanes. This implements D-01, D-04, D-05, OPS-01, and OPS-02.</action>
  <acceptance_criteria>
    - RED fails on current unconditional MCP/Gemini imports and missing explicit optional stages, never because a live service or user-data fixture is absent.
    - Base child proves import, preflight, serve delegation, lifespan readiness, and cleanup with all optional roots unavailable.
    - All four optional stages have deterministic explicit-enable success/failure/order/cleanup proof.
    - Real declared-extra smoke is bounded and no-I/O when installed, and explicitly `not_run` rather than passed when absent.
    - Manifests, locks, evidence, data roots, installed environments, and remote state are unchanged.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/runtime/test_base_install_startup.py tests/runtime/test_optional_integrations.py tests/test_python_dependency_contract.py -rs</automated>
  </verify>
  <done>A fail-closed RED suite defines one complete base runtime and four separately enabled, preserved, and truthfully evidenced optional integrations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-19-02: Implement lazy four-stage optional lifecycle composition</name>
  <files>aura_backend/main.py, aura_backend/mcp_system.py, aura_backend/runtime/config.py</files>
  <read_first>
    @tests/runtime/test_base_install_startup.py
    @tests/runtime/test_optional_integrations.py
    @aura_backend/main.py
    @aura_backend/runtime/app.py
    @aura_backend/runtime/config.py
    @aura_backend/runtime/health.py
    @aura_backend/mcp_system.py
    @aura_backend/providers/factory.py
    @aura_backend/providers/tools.py
    @aura_backend/memvid_archival_service.py
    @aura_backend/aura_autonomic_system.py
  </read_first>
  <behavior>
    - Required base services build the internal-only neutral tool executor and selected Ollama without importing any optional integration/package.
    - `AURA_MCP_ENABLED`, `AURA_MEMVID_ENABLED`, and `AUTONOMIC_ENABLED` are strict typed booleans defaulting false; malformed values fail before any resource construction.
    - Runtime declares separate optional MCP, Gemini-bridge, Memvid, and autonomic stages. Disabled stages return not-configured; enabled missing dependencies fail safely as optional and do not block required base readiness.
    - MCP startup no longer constructs Gemini bridge. The bridge has its own stage and local import, requiring successful MCP plus `ProviderKind.GEMINI`; Ollama/OpenRouter paths never import Google.
    - Explicitly enabled installed stages retain existing tool conversion/execution, Gemini bridge conversion, Memvid facade, and autonomic provider-neutral behavior, and each registers one cleanup before potentially failing initialization.
  </behavior>
  <action>Make optionality structural. Extend pure `RuntimeSettings` with exact strict false-default feature booleans and no ambient reads. Refactor main composition into one required base resource and four `required=False` stages named for MCP, Gemini bridge, Memvid, and autonomic behavior; retain existing storage constructors/paths and selected provider-last ownership. Build the internal tool catalog/executor in the base stage, augment it after optional MCP discovery, and pass only the neutral executor to the provider. In `mcp_system.py`, move MCP client/integration and Gemini bridge imports into their owning start functions, separate client/tool startup from bridge conversion, and provide idempotent stage-specific cleanup so a bridge failure does not leak or double-close the MCP client. Catch missing optional modules only at their feature boundary and let the existing `ApplicationRuntime` expose the allowlisted `optional_resource_failed` state. Do not catch required selected-provider failure as optional. Preserve D-02/D-03 result, streaming, cancellation, and public API behavior.</action>
  <acceptance_criteria>
    - Task 02-19-01 is GREEN, including all four explicit-enable success/order/cleanup cases and conditional real-extra outcomes.
    - Base default reaches readiness with all optional imports blocked, zero forbidden effects, and unchanged Phase 1 data-root digest.
    - Existing Gemini adapter, MCP characterization, autonomic-provider, Memvid, companion, persistence, health, import, and local-boundary tests remain green.
    - Feature activation is explicit; no optional dependency moves into base and no manifest/lock/environment changes occur.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/runtime/test_base_install_startup.py tests/runtime/test_optional_integrations.py tests/providers/test_gemini.py tests/characterization/test_mcp_parameters.py tests/characterization/test_mcp_result_formatting.py tests/runtime/test_autonomic_provider.py tests/runtime/test_lifecycle.py tests/runtime/test_app_lifespan.py tests/runtime/test_health.py tests/runtime/test_import_safety.py tests/characterization/test_companion_contract.py tests/characterization/test_persistence_contract.py tests/api/test_local_boundary.py -rs</automated>
  </verify>
  <done>Base-only Ollama is complete, while MCP, Gemini bridge, Memvid, and autonomic integrations remain explicit, independently owned, preserved when installed, and honestly unavailable when absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Base installation → lifespan | Missing optional packages must not become required imports or false readiness. |
| Feature configuration → optional code/process/storage | Only strict explicit enablement may enter each optional integration stage. |
| Optional dependency/exception → health/logs | Failures must become fixed safe resource/status metadata without source details. |
| Conditional installed-lane smoke → host/data | Real imports must remain bounded and use injected no-network/no-process/no-user-data seams. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-19-01 | Denial of Service | base lifespan | mitigate | Missing-extra child plus required/optional split; default Ollama reaches readiness without extras. |
| T-02-19-02 | Information Disclosure | optional errors/health | mitigate | Existing allowlisted optional resource code, safe names, and adversarial raw-detail tests. |
| T-02-19-03 | Tampering / Spoofing | feature activation | mitigate | Strict false-default booleans, selected-provider guard, and zero-construction malformed/disabled tests. |
| T-02-19-04 | Denial of Service / Tampering | optional cleanup | mitigate | Register cleanup before initialization, exact reverse-order events, idempotent close, and partial-start cases for all four stages. |
| T-02-19-05 | Information Disclosure | real extra smoke | mitigate | Synthetic content, injected no-I/O collaborators, hard subprocess timeout, data-root digest, conditional not_run. |
| T-02-19-SC | Tampering | package supply chain | mitigate | No package/manifest/lock/install mutation; smoke only exact already-declared installed extras. |
</threat_model>

<verification>
- Capture Task 02-19-01 RED before production edits, then run both exact task commands GREEN.
- Run `uv run --locked --no-sync python -m pytest tests -q -m "not live"`, active-code Ruff, frontend typing/build, `uv lock --check`, and `git diff --check` without installing or syncing.
- Record each optional real-lane result separately as pass or `not_run: declared_extra_not_installed`; do not aggregate not_run as success.
- Confirm manifest/lock/evidence diffs are empty and Phase 1 data-root digest is unchanged.
- Keep GitHub CI and Python typing pending unless a separately authorized exact-SHA remote run completes green.
</verification>

<source_coverage>
## Multi-Source Coverage Audit

| Source | Item | Coverage | Status |
|---|---|---|---|
| GOAL | Reliable Ollama/cloud provider runtime | Base startup plus installed explicit optional/provider preservation | COVERED |
| REQ TEST-03 | Provider/runtime automated behavior | Missing-extra, four-stage lifecycle, existing integration, and compatibility suites | COVERED |
| REQ AI-01 | Provider-neutral selected runtime/tools | Internal/MCP neutral executor and Gemini-only bridge stage | COVERED |
| REQ OPS-01 | Supported documented startup | Base preflight/serve/lifespan plus optional remediation | COVERED |
| REQ OPS-02 | One authority with optional lanes | Existing lanes consumed lazily; no authority edit | COVERED |
| CONTEXT D-01 | Complete Ollama; cloud explicit | Default blocked-extra Ollama and selected-Gemini preservation | COVERED |
| CONTEXT D-04 | Import-safe lifespan and honest optional health | Four optional ResourceFactory stages and cleanup proof | COVERED |
| CONTEXT D-05 | Base excludes optional extras; no broad upgrade | False-default feature settings and unchanged authorities | COVERED |
| CONTEXT D-06 | Offline/conditional evidence is honest | Fakes plus separate pass/not_run real-extra smoke | COVERED |
| RESEARCH | Avoid eager MCP/Gemini imports and health-time work | Local feature imports and cached lifecycle status | COVERED |

Excluded: remote CI/Pyright requires authorized publication/external execution; npm audit requires separate package authority. Storage migration, prompt work, frontend architecture, live Ornith, auth, data, and Git history remain out of scope.
</source_coverage>

<success_criteria>
- Base `uv sync --locked` is sufficient for tested loopback Ollama import, preflight, serve delegation, lifespan readiness, and shutdown with optional roots absent.
- MCP, Gemini bridge, Memvid, and autonomic stages each have deterministic explicit-enable success/failure/order/cleanup proof and conditional no-I/O real-extra smoke.
- Optional absence is safe and visible; installed explicit behavior is preserved; no optional dependency enters base.
- Full offline/active-lint/frontend/lock gates pass with unchanged manifests, locks, evidence, data, auth, and local boundary.
- Remote CI/Pyright remains pending and unclaimed.
</success_criteria>

<output>
Create `.planning/phases/02-provider-and-runtime-core/02-19-SUMMARY.md` when done.
</output>
