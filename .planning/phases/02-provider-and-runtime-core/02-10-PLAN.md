---
phase: 02-provider-and-runtime-core
plan: 10
type: tdd
wave: 8
depends_on:
  - 02-09
files_modified:
  - aura_backend/runtime/health.py
  - aura_backend/main.py
  - tests/runtime/test_health.py
autonomous: true
requirements:
  - TEST-03
  - OPS-01
must_haves:
  truths:
    - "Liveness, application readiness, and selected/optional provider availability are distinct typed states."
    - "Health never initializes a resource, generates model text, downloads/scans a model, opens a database, or turns optional cloud absence into local-runtime failure."
    - "Health/public error payloads are allowlisted and redact source exceptions, credentials, URLs, prompts, conversation/tool content, SDK objects, and tracebacks."
  artifacts:
    - path: "aura_backend/runtime/health.py"
      provides: "Typed liveness/readiness/provider health DTOs and safe serializers"
    - path: "tests/runtime/test_health.py"
      provides: "Truth-table, no-side-effect, status-code, and redaction tests"
    - path: "aura_backend/main.py"
      provides: "`/live`, `/ready`, `/health/providers`, and compatibility `/health` routes"
  key_links:
    - from: "aura_backend/main.py"
      to: "aura_backend/runtime/health.py"
      via: "Health routes serialize cached app.state.runtime status"
      pattern: "/live|/ready|/health/providers"
---

<objective>
Replace Aura's always-operational/raw-error health response with honest layered health per D-02 and D-04.

Purpose: Give startup and operators truthful, side-effect-free evidence without making optional cloud absence a local failure.
Output: Typed health models, four compatible routes, and adversarial tests.
</objective>

## Artifacts this phase produces

- Typed cached health snapshots and safe serialization.
- `/live`, `/ready`, `/health/providers`, and compatibility `/health` semantics.
- Tests for every required/optional/blocked/redaction state.

<execution_context>
@/home/ty/.Codex/gsd-core/workflows/execute-plan.md
@/home/ty/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-provider-and-runtime-core/02-CONTEXT.md
@.planning/phases/02-provider-and-runtime-core/02-RESEARCH.md
@.planning/phases/02-provider-and-runtime-core/02-09-SUMMARY.md
@aura_backend/main.py
@aura_backend/runtime/app.py
@aura_backend/preservation/manifest.py
</context>

<dependencies>
Needs: Lifespan-owned runtime and selected provider/route integration.
Creates: Health/readiness contract consumed by preflight/serve.
Wave rationale: Health derives from real runtime state after route/runtime wiring is stable.
</dependencies>

<artifacts_produced>
- `aura_backend/runtime/health.py` — typed liveness/readiness/provider snapshots with safe metadata.
- `aura_backend/main.py` — side-effect-free health routes.
- `tests/runtime/test_health.py` — status aggregation, no-probe, adversarial failure, and redaction checks.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 02-10-01: Define truthful typed health snapshots and redaction</name>
  <files>aura_backend/runtime/health.py, tests/runtime/test_health.py</files>
  <read_first>
    @aura_backend/runtime/app.py
    @aura_backend/providers/base.py
    @aura_backend/preservation/manifest.py
    @aura_backend/main.py
  </read_first>
  <behavior>
    - Liveness is process-local; readiness is true only after all required runtime resources and selected provider/model are ready.
    - Unselected cloud is `not_configured` and does not fail a valid Ollama runtime; selected unavailable/missing model makes readiness false.
    - Blocked, unknown, partial, unavailable, and not-run states remain distinct from pass/ready.
    - Public serialization includes only safe code, provider/model where safe, retryability, timestamp/age, and correlation ID.
  </behavior>
  <action>Write a complete required/optional/provider truth table first, including adversarial inconsistent snapshots. Implement frozen health DTOs and one aggregation function that derives readiness fail-closed and always emits every status category. Build public payloads from an allowlist. Never store/serialize exception text, headers, credential-bearing URLs, prompts, responses, tool data, or traceback.</action>
  <acceptance_criteria>
    - Truth table rejects vacuous/contradictory ready states and optional-cloud absence remains visible/non-fatal.
    - Secret/source/content sentinels are absent recursively from public JSON and logs.
    - Health modules import without resource construction or network.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/runtime/test_health.py -k 'model or snapshot or redact or aggregate'</automated>
  </verify>
  <done>Health state is typed, fail-closed for required resources, and privacy-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-10-02: Expose side-effect-free liveness, readiness, and provider routes</name>
  <files>aura_backend/main.py, tests/runtime/test_health.py</files>
  <read_first>
    @aura_backend/main.py
    @aura_backend/runtime/health.py
    @aura_backend/runtime/app.py
    @tests/runtime/test_app_lifespan.py
  </read_first>
  <behavior>
    - `/live` performs no network/resource work and returns 200 when the process serves.
    - `/ready` reads cached runtime status and returns 200 ready or 503 non-ready.
    - `/health/providers` returns 200 diagnostic provider states; optional unselected cloud is not configured, not failed.
    - Existing `/health` remains a compatibility composite derived from the same state, never `str(exception)` or object-existence-as-connectivity.
  </behavior>
  <action>Add the three explicit endpoints and replace the current `/health` body with a compatibility serialization of the same cached runtime snapshot. Provider refresh, if exposed, must be separately explicit and bounded to metadata/model listing only; default polling must not contact services. Add constructor/network/generation/database-open tripwires around every endpoint. Keep local/no-auth/CORS rules unchanged.</action>
  <acceptance_criteria>
    - Required-ready/unready status codes and payloads are exact and independently tested.
    - Repeated health polling causes zero provider generations, model downloads, runtime initialization, database opens, or source writes.
    - Raw exception/secret/content sentinels never appear in response or captured default log.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/runtime/test_health.py</automated>
  </verify>
  <done>Health endpoints tell the truth about process, application, and provider state without causing work or leakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Health request → runtime/provider | Untrusted polling must not trigger expensive/stateful work. |
| Internal failures → public health JSON | Source details may contain secrets/content/system paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-HF | Spoofing / Repudiation | readiness aggregation | mitigate | Derived fail-closed truth table; required/optional states explicit. |
| T-02-CD | Denial of Service | health polling | mitigate | Cached status, no generation/init/database work, bounded explicit metadata refresh only. |
| T-02-PL | Information Disclosure | public health/logs | mitigate | Allowlists and recursive sentinel redaction tests. |
| T-02-SC | Tampering | package supply chain | mitigate | No dependency changes. |
</threat_model>

<verification>
Run health, lifecycle, import-safety, local-boundary, and companion tests. Capture call counters proving health causes no external/stateful work.
</verification>

<success_criteria>
- OPS-01 has honest health/preflight inputs.
- D-04 liveness/readiness/provider semantics are distinct.
- D-02 public/source error redaction is adversarially proven.
</success_criteria>

<output>
Create `.planning/phases/02-provider-and-runtime-core/02-10-SUMMARY.md` when done.
</output>
