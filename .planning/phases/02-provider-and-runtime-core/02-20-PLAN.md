---
phase: 02-provider-and-runtime-core
plan: 20
type: execute
wave: 14
depends_on: [02-19]
files_modified:
  - .planning/phases/02-provider-and-runtime-core/02-17-PLAN.md
  - .env.example
  - aura_backend/STARTUP_GUIDE.md
  - tests/runtime/test_startup_docs.py
  - tests/test_ci_contract.py
autonomous: true
gap_closure: true
requirements: [TEST-03, TEST-05, OPS-01, OPS-02]
must_haves:
  truths:
    - "All three executable pytest commands in Plan 02-17 invoke pytest only as the Python module, with their targets, flags, and chained frontend gates unchanged."
    - "One fail-closed contract scans executable shell segments from all 20 Phase 2 plans, the Phase 2 validation matrix, and every CI run block."
    - "The command parser is independent of uv options or any other prefix: every executable token equal to pytest is valid only when immediately preceded by python and -m in the same shell segment."
    - "Standalone, uv-wrapped, chained, semicolon-separated, and multiline console-script forms are rejected while explanatory prose is ignored."
    - "Missing or empty expected plan, validation, workflow, job, or executable-command surfaces fail rather than producing a vacuous pass."
    - "The startup guide and example environment keep base `uv sync --locked` as the sufficient default path while documenting exact, explicit activation and remediation for MCP, Gemini bridge, Memvid, and autonomic stages."
  artifacts:
    - path: ".planning/phases/02-provider-and-runtime-core/02-17-PLAN.md"
      provides: "Three corrected import-safe executable commands"
    - path: "tests/test_ci_contract.py"
      provides: "Parsed all-plan/validation/CI executable-token regression contract"
    - path: "tests/runtime/test_startup_docs.py"
      provides: "Executable contract for base setup, inactive defaults, optional extras, and redacted remediation"
  key_links:
    - from: "tests/test_ci_contract.py"
      to: ".planning/phases/02-provider-and-runtime-core/02-*-PLAN.md"
      via: "complete expected-file discovery plus parsed executable sections"
      pattern: "02-.*-PLAN"
    - from: "tests/test_ci_contract.py"
      to: ".github/workflows/ci.yml"
      via: "safe YAML parsing of every non-empty run block"
      pattern: "run"
    - from: "aura_backend/STARTUP_GUIDE.md"
      to: "aura_backend/runtime/config.py"
      via: "exact optional-stage enable keys and failure-remediation names"
      pattern: "AURA_MCP_ENABLED|AURA_MEMVID_ENABLED|AUTONOMIC_ENABLED"
---

<objective>
Close the remaining local verification-command and startup-documentation defects after the runtime gap is repaired.

Purpose: Ensure no Phase 2 plan, validation row, or CI step can silently reintroduce the import-breaking pytest console entry point, and make the repaired optional-runtime boundary operationally explicit without widening the base install.
Output: Exactly three Plan 02-17 corrections, a complete parsed executable-token regression contract, and executable startup documentation for base and optional paths.
</objective>

## Artifacts this plan produces

- Correct module-mode commands in both Plan 02-17 task verifications and its plan-level verification list.
- A shell-segment/token parser for executable pytest invocations across all Phase 2 planning and CI truth surfaces.
- Adversarial coverage for prefixes, chaining, semicolons, multiline commands, missing surfaces, and prose immunity.
- Startup documentation and example configuration aligned to the repaired four-stage optional lifecycle.

<execution_context>
@/home/ty/.Codex/gsd-core/workflows/execute-plan.md
@/home/ty/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-provider-and-runtime-core/02-CONTEXT.md
@.planning/phases/02-provider-and-runtime-core/02-VERIFICATION.md
@.planning/phases/02-provider-and-runtime-core/02-17-PLAN.md
@.planning/phases/02-provider-and-runtime-core/02-18-PLAN.md
@.planning/phases/02-provider-and-runtime-core/02-19-SUMMARY.md
@.planning/phases/02-provider-and-runtime-core/02-VALIDATION.md
@.env.example
@aura_backend/STARTUP_GUIDE.md
@aura_backend/runtime/config.py
@tests/runtime/test_startup_docs.py
@tests/test_ci_contract.py
@.github/workflows/ci.yml
</context>

<dependencies>
Needs: Plan 02-19's base-runtime repair and final 20-plan phase inventory.
Creates: Complete local command-contract and startup-documentation closure consumed by phase re-verification.
Wave rationale: Runs after 02-19 so its expected surface set and final local regression include the gap-runtime plan.
</dependencies>

<scope_fence>
- Change only the three known Plan 02-17 command occurrences, the CI contract test, and the bounded startup-documentation surfaces named by Task 02-20-02. Do not alter Plan 02-17 dependency authorization, arguments, targets, npm checks, or outcome claims.
- Do not edit any source, manifest, lock, evidence, workflow, data, STATE, or `.trunk/` path.
- Do not execute or claim Pyright/GitHub success. Remote clean CI remains `not_run`/pending.
- Preserve the exact 16-approved / 4-rejected package boundary, local loopback/no-sign-in behavior, and every Phase 1 contract.
</scope_fence>

<tasks>

<task type="auto" tdd="true">
  <name>Task 02-20-01: Parse every Phase 2 executable pytest command</name>
  <files>.planning/phases/02-provider-and-runtime-core/02-17-PLAN.md, tests/test_ci_contract.py</files>
  <behavior>
    - RED fixture 1: a standalone pytest command in a Plan automated field is rejected.
    - RED fixture 2: uv run followed directly by pytest is rejected with no dependency on which uv flags are present.
    - RED fixture 3: invalid pytest commands after `&amp;&amp;`, raw `&&`, semicolon, newline, or YAML multiline boundaries are each detected.
    - RED fixture 4: a valid Python module invocation after any safe prefix passes; a literal argument or filename supplied to another executable does not become a pytest command merely because its value contains or equals pytest.
    - RED fixture 5: explanatory prose mentioning pytest is ignored because only parsed executable fields/code cells/run blocks are examined.
    - RED fixture 6: missing Plan 01-through-20 inventory member, validation file/matrix, workflow/jobs, run block collection, or empty extracted plan command surface fails closed.
    - GREEN 1: Plan 02-17 Task 01 automated command equals `uv run --locked --no-sync python -m pytest tests/test_node_dependency_contract.py -q` after HTML decoding.
    - GREEN 2: Plan 02-17 Task 02 command begins with that same module invocation and retains the frontend typecheck/build chain exactly.
    - GREEN 3: Plan 02-17's plan-level verification list contains the same module invocation exactly once; the document has exactly three targeted valid invocations and no executable console-script occurrence.
  </behavior>
  <action>Start with RED adversarial tests around pure extraction/token-validation helpers. Discover and sort the exact `02-01-PLAN.md` through `02-20-PLAN.md` set; do not use a single hard-coded Plan 18 path. Parse only executable surfaces: each plan's non-empty automated verification bodies plus command-form code cells/list entries inside its plan-level verification section, every backtick command in the validation matrix's automated-command column, and every non-empty CI YAML `run` scalar. Decode XML entities, join escaped line continuations, retain YAML newlines, then split shell operators outside quotes into segments and tokenize segments with standard-library shell lexical rules. Resolve the executable position in each segment, including leading environment assignments and an `uv run` wrapper with any option set rather than matching one approved prefix. If the resolved executable is pytest, accept it only when the resolved invocation is instead Python with adjacent `-m`, `pytest` module tokens; reject console-script execution with or without wrappers. Do not treat an argument to another executable as a command. Treat parse error, unbalanced quoting, absent expected surface, empty command extraction, duplicate/missing plan, or missing CI job/run collection as failure. Add adversarial documents for standalone, multiple uv flag combinations, chained, semicolon, multiline, exact-argument, and prose-only cases. Then edit only the three known Plan 02-17 executable surfaces by inserting `python -m` before the pytest token, preserving all remaining text. Avoid scanning ordinary objective/action prose so this plan's explanation cannot self-trigger.</action>
  <acceptance_criteria>
    - Exact Plan 01-through-20, validation, workflow, job, run-block, and non-empty command-surface assertions fail closed.
    - Token validation rejects every adversarial prefix/chaining/multiline variant and ignores ordinary prose.
    - Plan 02-17 has exactly three targeted valid module invocations with unchanged remaining command text.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest tests/test_ci_contract.py -q</automated>
  </verify>
  <done>Every executable Phase 2 pytest token is parsed and import-safe, with complete non-vacuous plan/validation/CI coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-20-02: Document base setup and explicit optional activation</name>
  <files>.env.example, aura_backend/STARTUP_GUIDE.md, tests/runtime/test_startup_docs.py</files>
  <behavior>
    - RED 1: `.env.example` keeps MCP, Memvid, and autonomic inactive with the exact non-secret values `AURA_MCP_ENABLED=false`, `AURA_MEMVID_ENABLED=false`, and `AUTONOMIC_ENABLED=false`, with no duplicate key.
    - RED 2: the canonical default setup remains exactly `uv sync --locked`; the guide does not require optional extras for local-first Ollama preflight, serve, or lifespan startup.
    - RED 3: opt-in setup names the exact locked commands `uv sync --locked --extra mcp`, `uv sync --locked --extra provider-gemini`, and `uv sync --locked --extra memvid`, then identifies the matching explicit feature-selection keys; Gemini also requires explicit provider selection and credentials.
    - RED 4: unavailable selected integrations point operators to the matching extra through the redacted `optional_resource_failed` status without exposing exception text, credentials, endpoints, prompts, tool payloads, or response bodies.
    - RED 5: documentation never instructs startup to install/sync dependencies, silently enables a cloud provider, adds sign-in, or broadens the local loopback boundary.
  </behavior>
  <action>Write the RED documentation contract first, then update only the example environment and startup guide. Keep the base command authoritative and sufficient for default Ollama; present each optional stage as an explicit opt-in whose enable key, declared extra, and redacted remediation agree with Plan 02-19's implemented runtime/config identifiers. Document autonomic as explicit runtime enablement that preserves the selected provider rather than as a new dependency extra. Keep example values non-secret, preserve localhost/no-sign-in operation and Phase 1 data compatibility, and state that preflight/serve never mutate the environment. Do not edit manifests, locks, source, workflows, or runtime tests owned by Plan 02-19.</action>
  <acceptance_criteria>
    - The docs test proves exact inactive defaults, base setup, three declared-extra commands, four explicit enable/selection paths, and safe failure remediation.
    - Default local-first startup remains base-only and no text implies an install, cloud fallback, authentication, or non-loopback requirement.
    - Documentation uses the implemented config/status names and contains no secrets or raw failure content.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/runtime/test_startup_docs.py</automated>
  </verify>
  <done>Operators can follow one tested base path or deliberately activate any optional stage with exact, safe, non-mutating instructions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Planning/validation text → executable verification | Prose and commands must be distinguished without omitting executable surfaces. |
| Shell/YAML/XML representation → token contract | Entities, quoting, chaining, and multiline forms can hide an invalid console entry point. |
| Scan inventory → clean claim | Missing documents or empty extraction must not be treated as success. |
| Runtime configuration → operator documentation | Stale setup text can make a safe optional boundary unavailable or accidentally enabled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-20-01 | Tampering | command encoding/chaining | mitigate | Entity decoding, quote-aware operator segmentation, shlex tokenization, and adversarial variants. |
| T-02-20-02 | Repudiation | scan coverage | mitigate | Exact Plan 01-through-20 set, required validation/workflow/jobs, non-empty surfaces, fail-closed parse errors. |
| T-02-20-03 | Spoofing | prose as command | mitigate | Extract only schema-defined executable fields/code cells/run blocks; explicit prose-immunity tests. |
| T-02-20-04 | Information Disclosure | optional failure guidance | mitigate | Document only redacted status/resource/remediation fields and prohibit raw exceptions, secrets, endpoints, prompts, tools, or bodies. |
| T-02-20-05 | Elevation of Privilege | optional activation defaults | mitigate | Exact false defaults, explicit selection, localhost/no-sign-in preservation, and executable docs assertions. |
| T-02-20-SC | Tampering | dependency/runtime state | mitigate | No install, source, manifest, lock, workflow, evidence, or environment change. |
</threat_model>

<verification>
- RED: `uv run --locked --no-sync python -m pytest tests/test_ci_contract.py -q -k 'phase2 or pytest or command'` fails on the three current Plan 02-17 occurrences and passes each adversarial detector expectation.
- GREEN: `uv run --locked --no-sync python -m pytest tests/test_ci_contract.py -q` passes after exactly three Plan 02-17 corrections.
- DOCS GREEN: `uv run --locked --no-sync python -m pytest -q tests/runtime/test_startup_docs.py` passes after the bounded guide/example update.
- Run `uv run --locked --no-sync python -m pytest tests -q -m "not live"`, `git diff --check`, and a scoped diff proving no source/manifest/lock/evidence/workflow change.
- Keep remote GitHub/Pyright explicitly pending.
</verification>

<source_coverage>
## Multi-Source Coverage Audit

| Source | Item | Coverage | Status |
|---|---|---|---|
| GOAL | Reliable testable provider/runtime boundary | Verification commands can reliably import the repository under test | COVERED |
| REQ TEST-05 | Honest CI/build/lint/type checks | Complete executable-token scan across plan/validation/CI | COVERED |
| REQ TEST-03 | Automated runtime integration coverage | Startup instructions and inactive defaults are executable contracts | COVERED |
| REQ OPS-01 | Supported executable runtime workflow | Replayed plan commands use the import-safe module entry point | COVERED |
| REQ OPS-02 | Single dependency declaration/lock model | Base and optional commands use the existing locked declarations without widening them | COVERED |
| CONTEXT D-05 | Evidence-gated dependency/startup repair | Guide and example config mirror the repaired optional boundary without manifest or lock edits | COVERED |
| CONTEXT D-06 | Distinct truthful verification lanes | Invalid/missing command evidence is non-pass; remote typing remains pending | COVERED |
| VERIFICATION GAP 3 | Three bad Plan 02-17 commands and narrow scan | Exact three corrections plus parsed all-surface regression | COVERED |

Excluded: the GitHub/Pyright external execution gap, npm audit warning, dependency changes, runtime/source behavior, live models, auth, storage, frontend work, and Git history.
</source_coverage>

<success_criteria>
- Plan 02-17 has exactly three corrected module-mode pytest invocations with unchanged targets and frontend gates.
- All 20 Phase 2 plans, validation matrix, and every CI run block are present, non-empty where required, parsed, segmented, and token-validated.
- Standalone, any uv-wrapped console form, chained, semicolon, and multiline defects fail; prose does not false-positive.
- The tested startup guide and example environment keep base Ollama sufficient by default and document exact explicit activation/remediation for all four optional stages.
- Full offline regression passes with no source, dependency, evidence, workflow, data, auth, or environment change.
- Remote clean CI/Pyright remains pending and unclaimed.
</success_criteria>

<output>
Create `.planning/phases/02-provider-and-runtime-core/02-20-SUMMARY.md` when done.
</output>
