---
phase: 02-provider-and-runtime-core
plan: 17
type: execute
wave: 11
depends_on: [02-12, 02-15]
files_modified:
  - package.json
  - package-lock.json
  - tests/test_node_dependency_contract.py
autonomous: true
requirements: [TEST-03, TEST-05, OPS-01, OPS-02]
must_haves:
  truths:
    - "The Node manifest/lock statically defines separate Python-typing, frontend-typing, and frontend-build commands; Plan 02-18 proves Pyright after an isolated clean install."
    - "Only exact pyright@1.1.413 addition and direct @google/genai@1.51.0 removal enter package.json/package-lock.json after the fail-closed pre-edit gate passes."
    - "Removing the unused frontend Google SDK cannot silently break the frontend build or supported entry points."
    - "The four rejected Python rows remain outside this plan and no Python manifest, lock, or declaration is touched."
  artifacts:
    - path: "package.json"
      provides: "Exact approved Pyright development dependency and explicit verification scripts"
      contains: "typecheck:python"
    - path: "package-lock.json"
      provides: "Lock-faithful Node dependency graph matching package.json"
    - path: "tests/test_node_dependency_contract.py"
      provides: "Static manifest, source-import, exact-version, script, and lock agreement gates"
  key_links:
    - from: "package.json"
      to: "package-lock.json"
      via: "npm package-lock-only regeneration and npm lock validation"
      pattern: "pyright"
    - from: "package.json"
      to: "pyproject.toml"
      via: "typecheck:python invokes the exact locked Pyright CLI against the configured Python source set"
      pattern: "typecheck:python"
---

<objective>
Make the Node manifest and lock an evidence-gated authority for frontend verification and the official Pyright CLI, per D-05 and D-06.

Purpose: OPS-01, OPS-02, TEST-03, and TEST-05 require reproducible dependency and truth lanes without broad upgrades or hidden package churn.
Output: A test-first Node dependency contract plus approved manifest/lock changes and named typing/build entry points.
</objective>

<execution_context>
@/home/ty/.Codex/gsd-core/workflows/execute-plan.md
@/home/ty/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-provider-and-runtime-core/02-CONTEXT.md
@.planning/phases/02-provider-and-runtime-core/02-RESEARCH.md
@.planning/phases/02-provider-and-runtime-core/02-PATTERNS.md
@.planning/phases/02-provider-and-runtime-core/02-15-SUMMARY.md
@package.json
@package-lock.json
@pyproject.toml
@index.tsx
@vite.config.ts
</context>

<dependencies>
Needs: Stable supported startup entry points plus explicit package-legitimacy approval.
Creates: Authoritative Node typing/build scripts and lock consumed by final CI.
Wave rationale: Runs beside documentation after wrapper ownership is fixed; follows the human gate and shares no Wave 11 files.
</dependencies>

<approval_contract>
The executor must treat this revision as a narrow authorization envelope, not a general Node upgrade or dependency-cleanup approval.

- Exact human decision: `Approve only the 16 OK rows; reject the four SUS rows; revise Plans 02-16 and 02-17 before any manifest or lock changes.`
- Revised evidence SHA-256: `e3efba9197560d9559629448cfa088003e9239567c645c0204d4d9f994724bdb` for `.planning/evidence/phase-02/package-legitimacy.json`.
- Pre-change Node authority SHA-256 values: `package.json=305b5f54910fd39499493d5a06e2b5f756ad65cf18e02c8134e1037bd17160db`, `package-lock.json=e7dc3835f15dfcc00f5b4c19a73c0dee207a9dd915aa4551df8faab1190be302`.
- Approved Node action set: add exact `pyright@1.1.413` as a development dependency and remove the direct frontend declaration `@google/genai@1.51.0`.
- Rejected and immutable in Phase 2: `pyzbar@0.1.9`, `faiss-cpu@1.11.0`, `faiss-gpu-cu12@1.14.1.post1`, and `asyncio-mqtt@0.16.2`. They are absent from this plan's file ownership and action set; rejection is not removal or movement authority.

Any missing/stale/changed evidence, digest mismatch, decision-text mismatch, set widening, unexpected action, or authorization broader than these exact two Node actions stops execution before `package.json` or `package-lock.json` is edited.
</approval_contract>

<artifacts_produced>
- `tests/test_node_dependency_contract.py` — executable authority, exact-version, source-import, script, and lock checks.
- `package.json` — approved exact development tool plus named verification commands.
- `package-lock.json` — package-lock-only reconciliation with the reviewed manifest.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>02-17-01: Specify the Node authority and verification-script contract</name>
  <files>tests/test_node_dependency_contract.py</files>
  <read_first>
    - package.json
    - package-lock.json
    - pyproject.toml
    - index.tsx
    - vite.config.ts
    - .planning/phases/02-provider-and-runtime-core/02-15-SUMMARY.md
    - .planning/phases/02-provider-and-runtime-core/02-RESEARCH.md
  </read_first>
  <behavior>
    - Test 0: an opt-in pre-change gate independently validates evidence freshness, evidence SHA-256, exact decision text, exact 16 OK / 4 SUS partition, the two-action Node subset, and current package manifest/lock SHA-256 values.
    - Test 1: package.json and package-lock.json agree on every direct dependency and exact approved Pyright version.
    - Test 2: named scripts invoke locked local binaries for Python typing, frontend typing, and frontend build; no global executable is required.
    - Test 3: a source/import scan proves `marked` remains active while the frontend Google SDK has no supported TypeScript import before it can be removed.
    - Test 4: no unapproved dependency addition, major-version change, or second Node lockfile is accepted.
  </behavior>
  <action>
Create focused static contract tests before editing either manifest. Encode the `<approval_contract>` as an explicit immutable two-action set and pre-change SHA-256 map, then independently cross-check it against the evidence file, `02-15-SUMMARY.md`, current UTC freshness, live file digests, package verdicts, row-level non-authorization, and exact human decision. Provide an opt-in pre-change gate (for example `AURA_DEPENDENCY_PRECHANGE_GATE=1` with a narrowly selected pytest case) that passes only while all preconditions match; normal post-change runs must not mistake the expected manifest update for evidence drift. Parse JSON rather than matching formatting. Assert exact command surfaces `typecheck:python`, `typecheck:frontend`, and `build`; the first must resolve the project-local official Pyright CLI and the second must run the project-local TypeScript compiler with `--noEmit`. Inventory active `.ts`/`.tsx` imports while excluding archives, generated output, and dependency directories. Assert that the plan's action set is exactly add `pyright@1.1.413` as a dev dependency and remove direct `@google/genai@1.51.0`, with no Python or rejected-row action. Record initial RED only for these two approved manifest differences and missing scripts. This implements D-05/D-06 without installing into Ty's active `node_modules`.
  </action>
  <acceptance_criteria>
    - Tests parse both manifests and reject lock drift, floated Pyright, global-tool scripts, and unapproved direct dependency changes.
    - The opt-in pre-change authorization test passes before edits; synthetic stale/changed evidence, digest drift, action-set widening, and any rejected-row action fail closed.
    - The import assertion distinguishes supported application source from archive/generated/vendor trees.
    - The test is RED for precisely the approved manifest differences, not for unrelated dependency versions.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest tests/test_node_dependency_contract.py -q</automated>
  </verify>
  <done>The RED contract precisely identifies the two reviewed Node changes and supplies a passing fail-closed gate that blocks edits on any evidence, digest, decision, or scope mismatch.</done>
</task>

<task type="auto" tdd="true">
  <name>02-17-02: Apply approved Node changes and reconcile the lock without broad upgrades</name>
  <files>package.json, package-lock.json, tests/test_node_dependency_contract.py</files>
  <read_first>
    - tests/test_node_dependency_contract.py
    - package.json
    - package-lock.json
    - .planning/phases/02-provider-and-runtime-core/02-15-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: exact approved Pyright is a development dependency, the lock contains that exact package, and named scripts statically reference project-local binaries.
    - Test 2: the approved unused frontend SDK declaration is absent while active `marked` remains.
    - Test 3: parsed manifest/lock and captured diff contain no unrelated direct-dependency or major-version churn.
    - Test 4: the existing installed TypeScript/Vite tree executes frontend type checking and build; local Pyright execution is not claimed before Plan 02-18's isolated clean install.
  </behavior>
  <action>
Immediately before editing, run the opt-in pre-change authorization gate from Task 02-17-01 and stop without touching either Node authority unless it passes. Then make exactly two dependency changes: add `pyright` as the exact development dependency string `1.1.413`, and remove the direct frontend dependency `@google/genai` whose reviewed candidate is `1.51.0`. Retain active `marked`; add `typecheck:python` and `typecheck:frontend` scripts using local executable paths. Regenerate only `package-lock.json` with `npm install --package-lock-only --ignore-scripts`; inspect the semantic manifest/lock diff and abort unless every change is an approved direct action, a named script, or a mechanically generated lock consequence of those exact actions. Do not touch `pyproject.toml`, `uv.lock`, `requirements.txt`, any rejected Python declaration/lock record, Vite/TypeScript versions, or other direct dependencies. Do not run `npm ci`, install/sync into Ty's active environment, add a provider SDK, or broaden any version. Because the current `node_modules` does not contain Pyright, do not execute `npm run typecheck:python` locally and do not report it as passing; Plan 02-18 owns its first real execution after an isolated clean lock-faithful CI install. Locally run only the static dependency contract, existing frontend TypeScript check, and frontend build through the current installed tree.
  </action>
  <acceptance_criteria>
    - The dependency manifest diff is exactly add development `pyright@1.1.413` and remove direct `@google/genai@1.51.0`; named script additions are explicit and no other direct package changes.
    - package-lock.json agrees with package.json and contains only mechanically generated consequences of those two dependency actions.
    - No Python authority, rejected declaration, rejected lock record, installed environment, or unrelated version is changed.
    - The static Pyright manifest/lock/script contract, existing frontend typing, and frontend build pass independently; actual Pyright execution remains explicitly pending Plan 02-18's isolated clean CI install.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest tests/test_node_dependency_contract.py -q &amp;&amp; npm run typecheck:frontend &amp;&amp; npm run build</automated>
  </verify>
  <done>The reviewed Node graph is lock-faithful, the unused SDK removal is proven safe, frontend typing/build pass locally, and Pyright execution is truthfully deferred to Plan 02-18's isolated clean CI install.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Registry metadata to repository lock | External package identity and dependency metadata may be misleading or mutable. |
| Package scripts to developer/CI host | Install or task scripts can execute code with workspace permissions. |
| Manifest to verification claim | A declared command can falsely imply a tool ran if it resolves globally or skips work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17-01 | Spoofing | Pyright package identity | mitigate | Require exact 02-15 human approval plus official-package evidence before declaration. |
| T-02-17-02 | Tampering | package-lock.json | mitigate | Digest-bound pre-edit gate, package-lock-only regeneration, parsed agreement tests, and abort outside the exact two-action closure. |
| T-02-17-03 | Repudiation | typing/build scripts | mitigate | Static local-binary command contracts here; independent Pyright exit status only after Plan 02-18's isolated clean CI install. |
| T-02-17-SC | Tampering | npm installs | mitigate | Human legitimacy checkpoint precedes changes; package-lock generation suppresses lifecycle scripts and CI owns clean install. |
</threat_model>

<verification>
- `uv run --locked --no-sync python -m pytest tests/test_node_dependency_contract.py -q`
- `npm run typecheck:frontend`
- `npm run build`
- Confirm `npm run typecheck:python` is not executed or claimed locally; Plan 02-18 owns that proof after isolated `npm ci`.
- Review the manifest/lock diff and confirm only approved direct-package changes.
</verification>

<success_criteria>
- Node dependencies have one authoritative manifest/lock pair with no broad upgrade.
- Required Python/frontend typing and frontend build commands are explicit; frontend lanes pass locally and actual Pyright execution is reserved for Plan 02-18's isolated clean CI install.
- No package change bypasses the legitimacy checkpoint, active imports, or entry-point proof.
</success_criteria>

<output>
Create `.planning/phases/02-provider-and-runtime-core/02-17-SUMMARY.md` when done.
</output>
