---
phase: 02-provider-and-runtime-core
plan: 17
type: execute
wave: 11
depends_on: [02-12, 02-15]
files_modified:
  - package.json
  - package-lock.json
  - tests/test_node_dependency_contract.py
autonomous: true
requirements: [TEST-03, TEST-05, OPS-01, OPS-02]
must_haves:
  truths:
    - "A clean Node install has one lock-faithful dependency authority and exposes separate Python-typing, frontend-typing, and frontend-build commands."
    - "Only package changes explicitly approved by the Phase 2 legitimacy checkpoint enter package.json/package-lock.json."
    - "Removing the unused frontend Google SDK cannot silently break the frontend build or supported entry points."
  artifacts:
    - path: "package.json"
      provides: "Exact approved Pyright development dependency and explicit verification scripts"
      contains: "typecheck:python"
    - path: "package-lock.json"
      provides: "Lock-faithful Node dependency graph matching package.json"
    - path: "tests/test_node_dependency_contract.py"
      provides: "Static manifest, source-import, exact-version, script, and lock agreement gates"
  key_links:
    - from: "package.json"
      to: "package-lock.json"
      via: "npm package-lock-only regeneration and npm lock validation"
      pattern: "pyright"
    - from: "package.json"
      to: "pyproject.toml"
      via: "typecheck:python invokes the exact locked Pyright CLI against the configured Python source set"
      pattern: "typecheck:python"
---

<objective>
Make the Node manifest and lock an evidence-gated authority for frontend verification and the official Pyright CLI, per D-05 and D-06.

Purpose: OPS-01, OPS-02, TEST-03, and TEST-05 require reproducible dependency and truth lanes without broad upgrades or hidden package churn.
Output: A test-first Node dependency contract plus approved manifest/lock changes and named typing/build entry points.
</objective>

<execution_context>
@/home/ty/.Codex/gsd-core/workflows/execute-plan.md
@/home/ty/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-provider-and-runtime-core/02-CONTEXT.md
@.planning/phases/02-provider-and-runtime-core/02-RESEARCH.md
@.planning/phases/02-provider-and-runtime-core/02-PATTERNS.md
@.planning/phases/02-provider-and-runtime-core/02-15-SUMMARY.md
@package.json
@package-lock.json
@pyproject.toml
@index.tsx
@vite.config.ts
</context>

<dependencies>
Needs: Stable supported startup entry points plus explicit package-legitimacy approval.
Creates: Authoritative Node typing/build scripts and lock consumed by final CI.
Wave rationale: Runs beside documentation after wrapper ownership is fixed; follows the human gate and shares no Wave 11 files.
</dependencies>

<artifacts_produced>
- `tests/test_node_dependency_contract.py` — executable authority, exact-version, source-import, script, and lock checks.
- `package.json` — approved exact development tool plus named verification commands.
- `package-lock.json` — package-lock-only reconciliation with the reviewed manifest.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>02-17-01: Specify the Node authority and verification-script contract</name>
  <files>tests/test_node_dependency_contract.py</files>
  <read_first>
    - package.json
    - package-lock.json
    - pyproject.toml
    - index.tsx
    - vite.config.ts
    - .planning/phases/02-provider-and-runtime-core/02-15-SUMMARY.md
    - .planning/phases/02-provider-and-runtime-core/02-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: package.json and package-lock.json agree on every direct dependency and exact approved Pyright version.
    - Test 2: named scripts invoke locked local binaries for Python typing, frontend typing, and frontend build; no global executable is required.
    - Test 3: a source/import scan proves `marked` remains active while the frontend Google SDK has no supported TypeScript import before it can be removed.
    - Test 4: no unapproved dependency addition, major-version change, or second Node lockfile is accepted.
  </behavior>
  <action>
Create focused static contract tests before editing either manifest. Read the signed 02-15 checkpoint summary and encode only its approved Ruff/Pyright/removal decisions; if the checkpoint is not explicitly approved, fail this task without changing package files. Parse JSON rather than matching formatting. Assert exact command surfaces `typecheck:python`, `typecheck:frontend`, and `build`; the first must resolve the project-local official Pyright CLI and the second must run the project-local TypeScript compiler with `--noEmit`. Inventory active `.ts`/`.tsx` imports while excluding archives, generated output, and dependency directories. Record the initial RED failures caused by the missing scripts/tool and any approved unused dependency. This implements D-05/D-06 without installing into Ty's active `node_modules`.
  </action>
  <acceptance_criteria>
    - Tests parse both manifests and reject lock drift, floated Pyright, global-tool scripts, and unapproved direct dependency changes.
    - The import assertion distinguishes supported application source from archive/generated/vendor trees.
    - The test is RED for precisely the approved manifest differences, not for unrelated dependency versions.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync pytest tests/test_node_dependency_contract.py -q</automated>
  </verify>
  <done>The RED contract precisely identifies the reviewed Node changes and blocks manifest edits when checkpoint evidence is absent or non-approving.</done>
</task>

<task type="auto" tdd="true">
  <name>02-17-02: Apply approved Node changes and reconcile the lock without broad upgrades</name>
  <files>package.json, package-lock.json, tests/test_node_dependency_contract.py</files>
  <read_first>
    - tests/test_node_dependency_contract.py
    - package.json
    - package-lock.json
    - .planning/phases/02-provider-and-runtime-core/02-15-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: exact approved Pyright is a development dependency and all named scripts resolve local locked binaries.
    - Test 2: the approved unused frontend SDK declaration is absent while active `marked` remains.
    - Test 3: `npm install --package-lock-only --ignore-scripts` produces no unrelated direct-dependency or major-version churn.
    - Test 4: the existing frontend compiles and builds using the new named commands.
  </behavior>
  <action>
After 02-17-01 is RED for the expected reasons, edit `package.json` according to the explicit 02-15 approval: add `pyright` at exact `1.1.413` only if approved, remove `@google/genai` only if its audit disposition is approved, retain active `marked`, and add `typecheck:python` and `typecheck:frontend` scripts using local executables. Regenerate only `package-lock.json` with `npm install --package-lock-only --ignore-scripts`; inspect the diff and abort on unrelated direct-dependency or major-version movement. Do not run `npm ci` against Ty's active dependency tree; CI will perform the clean-install proof. Run the local scripts using the existing locked tree, triage real Pyright/TypeScript findings rather than weakening configs, and keep changes within Phase 2-owned source. Do not add a new provider SDK or change Vite/TypeScript versions.
  </action>
  <acceptance_criteria>
    - The manifest contains only checkpoint-approved changes and exact Pyright 1.1.413 when approved.
    - package-lock.json agrees with package.json and its diff contains no broad dependency upgrade.
    - Python typing, frontend typing, frontend build, and the static dependency contract pass independently.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync pytest tests/test_node_dependency_contract.py -q &amp;&amp; npm run typecheck:python &amp;&amp; npm run typecheck:frontend &amp;&amp; npm run build &amp;&amp; npm install --package-lock-only --ignore-scripts --dry-run</automated>
  </verify>
  <done>The reviewed Node graph is lock-faithful, the unused SDK removal is proven safe, and separate local typing/build commands pass without a large implicit install.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Registry metadata to repository lock | External package identity and dependency metadata may be misleading or mutable. |
| Package scripts to developer/CI host | Install or task scripts can execute code with workspace permissions. |
| Manifest to verification claim | A declared command can falsely imply a tool ran if it resolves globally or skips work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17-01 | Spoofing | Pyright package identity | mitigate | Require exact 02-15 human approval plus official-package evidence before declaration. |
| T-02-17-02 | Tampering | package-lock.json | mitigate | Package-lock-only regeneration, parsed agreement tests, and abort on unrelated churn. |
| T-02-17-03 | Repudiation | typing/build scripts | mitigate | Named local-binary commands with independent exit statuses and contract tests. |
| T-02-17-SC | Tampering | npm installs | mitigate | Human legitimacy checkpoint precedes changes; package-lock generation suppresses lifecycle scripts and CI owns clean install. |
</threat_model>

<verification>
- `uv run --locked --no-sync pytest tests/test_node_dependency_contract.py -q`
- `npm run typecheck:python`
- `npm run typecheck:frontend`
- `npm run build`
- Review the manifest/lock diff and confirm only approved direct-package changes.
</verification>

<success_criteria>
- Node dependencies have one authoritative manifest/lock pair with no broad upgrade.
- Required Python/frontend typing and frontend build commands are explicit and independently runnable.
- No package change bypasses the legitimacy checkpoint, active imports, or entry-point proof.
</success_criteria>

<output>
Create `.planning/phases/02-provider-and-runtime-core/02-17-SUMMARY.md` when done.
</output>
