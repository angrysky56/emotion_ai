---
phase: 02-provider-and-runtime-core
plan: 05
type: tdd
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified:
  - aura_backend/providers/gemini.py
  - aura_backend/thinking_processor.py
  - tests/providers/test_gemini.py
autonomous: true
requirements:
  - TEST-03
  - AI-01
  - AI-03
user_setup:
  - service: gemini
    why: "Optional explicitly selected Gemini execution"
    env_vars:
      - name: GEMINI_API_KEY
        source: "Google AI Studio API key; not required for Ollama or deterministic tests"
must_haves:
  truths:
    - "Gemini uses the official asynchronous client surface for generation, streaming, tool follow-ups, and close."
    - "Gemini retains no hidden provider chat-history owner and returns no raw SDK/thought object through the stable result."
    - "Gemini failures, malformed data, timeouts, partial streams, and cancellation obey the same typed terminal contract."
  artifacts:
    - path: "aura_backend/providers/gemini.py"
      provides: "Lazy, async, stateless Gemini adapter"
    - path: "aura_backend/thinking_processor.py"
      provides: "Awaited async processing compatibility with safe normalized summary"
    - path: "tests/providers/test_gemini.py"
      provides: "Async-only, stream, failure, close, and no-hidden-session tests"
  key_links:
    - from: "aura_backend/providers/gemini.py"
      to: "aura_backend/providers/base.py"
      via: "Google responses become only ProviderResult/StreamEvent values"
      pattern: "ProviderResult|TextDelta|Completed"
---

<objective>
Bring Gemini behind the same async, stateless, typed contract required by D-01 through D-03.

Purpose: Preserve explicit cloud support without blocking the event loop, retaining hidden SDK sessions, or leaking raw thinking/provider data.
Output: Async Gemini adapter, async-compatible thinking processor, and injected-client tests.
</objective>

## Artifacts this phase produces

- `aura_backend/providers/gemini.py` — explicit optional Gemini adapter using `client.aio`.
- `aura_backend/thinking_processor.py` — awaited async calls and normalized user-facing reflection summary only.
- `tests/providers/test_gemini.py` — fake async client/chat/stream lifecycle suite.

<execution_context>
@/home/ty/.Codex/gsd-core/workflows/execute-plan.md
@/home/ty/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-provider-and-runtime-core/02-CONTEXT.md
@.planning/phases/02-provider-and-runtime-core/02-RESEARCH.md
@.planning/phases/02-provider-and-runtime-core/02-02-SUMMARY.md
@.planning/phases/02-provider-and-runtime-core/02-03-SUMMARY.md
@aura_backend/providers/gemini.py
@aura_backend/thinking_processor.py
</context>

<dependencies>
Needs: Typed runtime/terminal contract and provider-neutral tools.
Creates: Gemini `Provider` implementation consumed by the lazy factory.
Wave rationale: Runs beside OpenAI-compatible adapters with no shared files.
</dependencies>

<artifacts_produced>
- `aura_backend/providers/gemini.py` — lazy stateless async Gemini adapter.
- `aura_backend/thinking_processor.py` — asynchronous provider-neutral thinking path.
- `tests/providers/test_gemini.py` — async, streaming, cancellation, failure, and close checks.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 02-05-01: Implement a lazy stateless Gemini async adapter</name>
  <files>aura_backend/providers/gemini.py, tests/providers/test_gemini.py</files>
  <read_first>
    @aura_backend/providers/base.py
    @aura_backend/providers/errors.py
    @aura_backend/providers/tools.py
    @aura_backend/providers/gemini.py
    @tests/providers/fakes.py
  </read_first>
  <behavior>
    - Module import does not create a Google client; adapter construction is injected/testable and occurs only after explicit Gemini selection and credential validation.
    - Generation and streaming await `client.aio` methods; a sync-method tripwire fake is never called.
    - Each request carries its explicit messages; no `_chat_sessions` or SDK-owned hidden conversation history remains.
    - Normal, malformed, auth, rate limit, timeout, unavailable, resource-limit, midstream failure, and cancellation outcomes map safely; `aio.aclose()` runs once.
  </behavior>
  <action>Write async-only tripwire tests first, then refactor Gemini to lazy-import the SDK inside its selected construction path and use `client.aio` for model/chat/stream/close. Convert neutral messages/tools at the adapter edge, normalize usage/reflection fields, and drop raw SDK objects and hidden chat-session history from the stable path. Preserve current model/thinking configuration keys but do not expose raw hidden reasoning or change prompts/quality in this phase.</action>
  <acceptance_criteria>
    - Sync Google fake methods raise if touched and the complete suite remains green.
    - Repeated requests are self-contained and no adapter session cache exists.
    - Cancellation re-raises, closes the async iterator/client locally, and emits no completion.
    - Sentinel key/prompt/response/exception text is absent from public/default output.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/providers/test_gemini.py -k 'adapter or stream or failure or close'</automated>
  </verify>
  <done>Gemini is an explicit, async, stateless implementation of the same provider contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-05-02: Remove synchronous sends from ThinkingProcessor paths</name>
  <files>aura_backend/thinking_processor.py, tests/providers/test_gemini.py</files>
  <read_first>
    @aura_backend/thinking_processor.py
    @aura_backend/providers/gemini.py
    @tests/providers/test_gemini.py
  </read_first>
  <behavior>
    - All async processing functions await an async chat/model send for initial and tool-follow-up responses.
    - Empty candidates/parts and malformed function-call results become typed adapter failures rather than answer content.
    - Only a concise normalized reflection summary may be returned; raw thought chunks are neither required nor exposed.
    - Cancellation is cleaned up and re-raised without a fallback success.
  </behavior>
  <action>Convert the three synchronous `send_message` call sites reached from async methods to injected awaited callables compatible with Gemini `aio`. Retain parser/prompt meaning and the existing public absence of hidden reasoning; remove raw exception/tool-result text from default logs. If the adapter no longer needs a legacy processor path, keep its public import behavior safe for untouched callers and cover that behavior rather than deleting it speculatively.</action>
  <acceptance_criteria>
    - Event-loop tripwire tests prove no synchronous network method is called from async processing.
    - Malformed/tool/cancel cases preserve the typed terminal rules.
    - No prompt-quality or emotion-analysis redesign is introduced.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/providers/test_gemini.py -k 'thinking or async or cancel'</automated>
  </verify>
  <done>All Gemini work reachable from async Aura paths is awaitable, cancellable, and privacy-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Aura → Gemini cloud | Explicit credentialed cloud traffic may expose content or block/cost unexpectedly. |
| Google SDK response/thoughts → Aura | SDK types and hidden reasoning must not cross stable/public boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-PC | Spoofing / Cost | Gemini selection | mitigate | Explicit provider plus credential before lazy import/client construction. |
| T-02-CD | Denial of Service | synchronous/timeout paths | mitigate | `client.aio`, one runtime deadline, cancellation propagation, async close. |
| T-02-PL | Information Disclosure | thoughts/errors/SDK objects | mitigate | Normalized allowlisted fields only; sentinel redaction and no traceback/raw response. |
| T-02-SC | Tampering | package supply chain | mitigate | Retain already locked `google-genai`; no package change. |
</threat_model>

<verification>
Run `tests/providers/test_gemini.py`, provider contract/stream tests, and Phase 1 companion characterization with Google modules replaceable by fakes and no network.
</verification>

<success_criteria>
- AI-01 supports Gemini without provider-specific orchestration.
- AI-03 covers Gemini normal/stream/failure/cancel behavior deterministically.
- D-01 explicit cloud selection and D-02/D-03 truth/privacy rules hold.
</success_criteria>

<output>
Create `.planning/phases/02-provider-and-runtime-core/02-05-SUMMARY.md` when done.
</output>
