---
phase: 02-provider-and-runtime-core
plan: 15
type: execute
wave: 2
depends_on:
  - 02-14
files_modified: []
autonomous: false
requirements:
  - TEST-05
  - OPS-02
must_haves:
  truths:
    - "Ty explicitly reviews and approves or rejects the current Ruff/Pyright and dependency-lane evidence before any manifest/lock mutation."
    - "Any UNASSESSED, SUS, missing, stale, or mismatched candidate blocks approval and installation/removal."
  artifacts:
    - path: ".planning/evidence/phase-02/package-legitimacy.json"
      provides: "Human-reviewed package and dependency decision evidence"
  key_links:
    - from: ".planning/evidence/phase-02/package-legitimacy.json"
      to: "02-16 and 02-17"
      via: "blocking human approval before manifest/lock edits"
      pattern: "verdict"
---

<objective>
Obtain the mandatory package-legitimacy approval required by D-05 and the supply-chain gate.

Purpose: Prevent unreviewed Ruff/Pyright additions or evidence-unsupported package removal/movement.
Output: A blocking approval signal only; no files, packages, locks, installed artifacts, or Git history are changed.
</objective>

## Artifacts this phase produces

- No new file. The executor records Ty's approval/rejection in the plan summary and does not mutate manifests here.

<execution_context>
@/home/ty/.Codex/gsd-core/workflows/execute-plan.md
@/home/ty/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-provider-and-runtime-core/02-CONTEXT.md
@.planning/phases/02-provider-and-runtime-core/02-RESEARCH.md
@.planning/phases/02-provider-and-runtime-core/02-14-SUMMARY.md
@.planning/evidence/phase-02/package-legitimacy.json
</context>

<dependencies>
Needs: Passing current audit evidence from Plan 02-14.
Creates: Blocking approval required by Python/Node authority plans.
Wave rationale: Runs immediately after audit; other provider work can continue independently.
</dependencies>

<artifacts_produced>
- `.planning/phases/02-provider-and-runtime-core/02-15-SUMMARY.md` — Ty's explicit approved/rejected disposition for every gated addition, move, and removal.
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 02-15-01: Approve exact package and dependency-lane dispositions</name>
  <files>None</files>
  <read_first>
    @.planning/evidence/phase-02/package-legitimacy.json
    @.planning/phases/02-provider-and-runtime-core/02-14-SUMMARY.md
    @.planning/phases/02-provider-and-runtime-core/02-RESEARCH.md
  </read_first>
  <action>First run the automated evidence validator below. Present Ty a concise table for every candidate, emphasizing exact Ruff/Pyright versions, official registry/source owner, release age, install/postinstall behavior, CLI entrypoint, and the import/entrypoint-backed disposition for every moved/removed dependency. Link Ruff to `pypi.org/project/ruff/` and Pyright to `npmjs.com/package/pyright`, plus their official source repositories. Do not run a package manager, edit a manifest/lock, or treat silence/auto-advance as approval.</action>
  <acceptance_criteria>
    - The canonical evidence validator passes and contains no non-OK candidate presented as approved.
    - Ty approves the exact versions and dependency dispositions, or rejects/names changes.
    - Rejection or any ambiguous/stale/SUS/UNASSESSED evidence stops Plans 02-16/02-17 for revision.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/test_dependency_audit.py</automated>
    <human-check>Review the evidence table and official registry/source links; confirm that exact additions and moves/removals are acceptable.</human-check>
  </verify>
  <what-built>Current package legitimacy, import, and supported-entrypoint evidence; no software was installed.</what-built>
  <how-to-verify>Confirm each evidence row has the correct official owner/source, exact version, install-script/CLI facts, and a supported-entrypoint-safe disposition.</how-to-verify>
  <resume-signal>Type `approved` to authorize only the listed manifest changes, or describe the rejected/changed rows.</resume-signal>
  <done>An explicit human approval exists for the exact audited package changes; otherwise dependency plans remain blocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Audit evidence → human authorization | Incomplete or misleading evidence could authorize supply-chain changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-SC | Spoofing / Tampering | package approval | mitigate | Blocking human review of exact official identity/version/script/entrypoint evidence; no auto-approval. |
| T-02-DE | Repudiation | approval scope | mitigate | Approval names exact candidate rows; summary records decision without broadening scope. |
</threat_model>

<verification>
Only the deterministic evidence test and human review occur. Verify `git diff --check`; no manifest/lock/package/data/Git-history mutation is allowed.
</verification>

<success_criteria>
- The package gate is explicitly approved or truthfully blocked.
- No package addition/removal/group move occurs without authorization.
</success_criteria>

<output>
Create `.planning/phases/02-provider-and-runtime-core/02-15-SUMMARY.md` recording the exact approval/rejection signal.
</output>
