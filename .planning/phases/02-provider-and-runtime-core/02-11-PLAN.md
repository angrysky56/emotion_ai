---
phase: 02-provider-and-runtime-core
plan: 11
type: tdd
wave: 9
depends_on:
  - 02-10
files_modified:
  - aura_backend/runtime/cli.py
  - aura_backend/runtime/__main__.py
  - tests/runtime/test_cli.py
autonomous: true
requirements:
  - TEST-03
  - OPS-01
must_haves:
  truths:
    - "One cross-platform Python entry point reports dependency, lock, provider/model, port, storage, and health readiness with exact truthful statuses."
    - "Preflight never installs/syncs/downloads software or models, writes configuration, changes permissions, kills a process, opens production databases, or starts Aura."
    - "Serve runs the preflight gate, binds loopback by default, owns child/service shutdown, and performs no implicit dependency mutation."
  artifacts:
    - path: "aura_backend/runtime/cli.py"
      provides: "Argparse preflight/serve commands, safe result model, and exit codes"
    - path: "aura_backend/runtime/__main__.py"
      provides: "`python -m aura_backend.runtime` entry point"
    - path: "tests/runtime/test_cli.py"
      provides: "Non-mutating check matrix, serve wiring, redaction, and false-success tests"
  key_links:
    - from: "aura_backend/runtime/cli.py"
      to: "aura_backend/runtime/health.py"
      via: "Preflight/serve consume typed readiness/provider status"
      pattern: "Preflight|Readiness"
---

<objective>
Implement the canonical non-mutating startup/preflight boundary required by D-05.

Purpose: Replace divergent installer/port-killer behavior with one testable Linux/Windows Python command.
Output: `preflight` and `serve` commands with truthful exit codes and no hidden mutation.
</objective>

## Artifacts this phase produces

- `python -m aura_backend.runtime preflight` — report-only prerequisite/readiness checks.
- `python -m aura_backend.runtime serve` — preflight-gated local API/UI process ownership.
- Deterministic CLI subprocess tests.

<execution_context>
@/home/ty/.Codex/gsd-core/workflows/execute-plan.md
@/home/ty/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-provider-and-runtime-core/02-CONTEXT.md
@.planning/phases/02-provider-and-runtime-core/02-RESEARCH.md
@.planning/phases/02-provider-and-runtime-core/02-PATTERNS.md
@.planning/phases/02-provider-and-runtime-core/02-10-SUMMARY.md
@aura_backend/preservation/cli.py
@start_full_system.sh
@start_full_system.bat
</context>

<dependencies>
Needs: Import-safe app factory and honest health/readiness.
Creates: Canonical commands consumed by all wrapper/documentation plans.
Wave rationale: Startup checks can be truthful only after runtime and health semantics exist.
</dependencies>

<artifacts_produced>
- `aura_backend/runtime/cli.py` and `aura_backend/runtime/__main__.py` — canonical non-mutating preflight and loopback serve commands.
- `tests/runtime/test_cli.py` — exact status, redaction, signal, child-cleanup, and no-mutation checks.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 02-11-01: Build non-mutating preflight checks with exact statuses</name>
  <files>aura_backend/runtime/cli.py, aura_backend/runtime/__main__.py, tests/runtime/test_cli.py</files>
  <read_first>
    @aura_backend/preservation/cli.py
    @aura_backend/runtime/config.py
    @aura_backend/runtime/health.py
    @aura_backend/providers/config.py
    @.planning/phases/02-provider-and-runtime-core/02-RESEARCH.md
  </read_first>
  <behavior>
    - Preflight emits one structured check each for Python/uv/Node/npm, `uv.lock`/`package-lock.json` freshness, selected-provider config, port, configured storage paths/writability, selected service/model metadata, and app readiness.
    - Missing, failed, blocked, not-run, optional/not-applicable, and pass are distinct; required non-pass yields a nonzero documented exit.
    - Busy port names the safe numeric port and remediation but never kills/attaches to a process; missing model never downloads/pulls.
    - Secret/path/content/source exception sentinels are absent from stdout/stderr/public JSON.
  </behavior>
  <action>Write pure check and subprocess CLI tests first. Follow the existing preservation `argparse`/integer-exit pattern. Use injected command/socket/filesystem/provider-health probes for deterministic tests and bounded metadata calls for a selected live provider. Report actionable commands as text only. The check registry must derive the aggregate status from the full required set so omitted/partial checks cannot pass. Do not run `uv sync`, `npm install`, curl, model pulls, chmod, `.env` writes, process kills, database clients, or generation requests.</action>
  <acceptance_criteria>
    - Complete success and every individual required failure produce exact check/aggregate/exit outcomes.
    - Empty/omitted/duplicate/contradictory check evidence is rejected as non-pass.
    - Mutation tripwires and secret/source/content redaction assertions pass.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/runtime/test_cli.py -k 'preflight or check or redaction or mutation'</automated>
  </verify>
  <done>Preflight truthfully identifies every required startup blocker without changing the machine or project.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-11-02: Serve Aura through the canonical loopback runtime command</name>
  <files>aura_backend/runtime/cli.py, aura_backend/runtime/__main__.py, tests/runtime/test_cli.py</files>
  <read_first>
    @aura_backend/runtime/cli.py
    @aura_backend/runtime/app.py
    @aura_backend/main.py
    @aura_backend/runtime_security.py
  </read_first>
  <behavior>
    - `serve` refuses to start after any required preflight non-pass and propagates its exit/status.
    - Successful serve starts the import-safe FastAPI factory on validated loopback by default and optionally owns the existing frontend process without installing packages.
    - Explicit LAN host requires explicit configuration and emits a clear warning; no auth is introduced.
    - SIGINT/SIGTERM/child failure stops owned API/frontend processes and waits for application lifespan shutdown; unrelated processes are untouched.
  </behavior>
  <action>Add injected server/process-runner tests before implementation. Implement `serve` using Uvicorn's factory/lifespan support and standard-library process/signal handling for the optional existing frontend command; include explicit `--backend-only` and `--frontend-only` compatibility modes used by thin wrappers. Invoke only already-installed locked commands (`uv run --locked --no-sync` is the outer documented command and npm execution may not install). Do not enable reload, discover terminal emulators, fork detached processes, kill occupied ports, or claim ready until `/ready` is true.</action>
  <acceptance_criteria>
    - Fake-runner tests prove preflight-before-start, exact loopback args, ready gate, child-failure propagation, and reverse shutdown.
    - No installer/sync/download/kill/write command appears in invoked command arrays.
    - Explicit LAN and normal no-auth behavior match Phase 1 runtime-security tests.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/runtime/test_cli.py -k 'serve or signal or child or loopback'</automated>
  </verify>
  <done>Aura has one cross-platform, preflight-gated startup command that is non-mutating and cleanup-owned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| CLI → operating system | Startup can inspect dependencies/ports/storage and launch owned processes. |
| CLI diagnostics → terminal/log | Paths, credentials, provider errors, and content must remain private. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-ST | Tampering / Elevation | preflight/serve commands | mitigate | Report only; no installer, sync, chmod, kill, env write, download, or reload. |
| T-02-HF | Repudiation | aggregate preflight result | mitigate | Exact required check set, derived statuses, adversarial incomplete/contradictory tests. |
| T-02-PL | Information Disclosure | CLI output | mitigate | Safe code/remediation allowlist and recursive sentinel tests. |
| T-02-LH | Information Disclosure | bind host | mitigate | Loopback default, explicit LAN warning, existing CORS/no-auth model. |
| T-02-SC | Tampering | package supply chain | mitigate | Run locked/no-sync only; no package mutation. |
</threat_model>

<verification>
Run CLI, health, lifecycle, import-safety, and local-boundary suites. Subprocess tests must intercept every external command and never start a real server or mutate Ty's active environment.
</verification>

<success_criteria>
- OPS-01 has one canonical cross-platform preflight/serve implementation.
- D-05 missing dependencies/model/port/storage/health produce actionable redacted non-pass outcomes.
- Startup remains loopback-first and requires no account.
</success_criteria>

<output>
Create `.planning/phases/02-provider-and-runtime-core/02-11-SUMMARY.md` when done.
</output>
