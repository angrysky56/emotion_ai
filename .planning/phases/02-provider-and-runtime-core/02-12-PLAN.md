---
phase: 02-provider-and-runtime-core
plan: 12
type: tdd
wave: 10
depends_on:
  - 02-11
files_modified:
  - start_full_system.sh
  - start_full_system.bat
  - aura_backend/start.sh
  - aura_backend/start_api.sh
  - aura_backend/start_all.sh
  - aura_backend/start_frontend.sh
  - aura_backend/start_mcp.sh
  - tests/runtime/test_startup_entrypoints.py
autonomous: true
requirements:
  - TEST-03
  - OPS-01
must_haves:
  truths:
    - "Every supported shell/batch launcher delegates to the canonical Python runtime command or an explicit standalone MCP module."
    - "No launcher installs/syncs/downloads, creates `.env`, changes permissions, kills ports/processes, enables reload, or silently binds LAN."
    - "Linux and Windows wrappers expose the same loopback-first behavior and propagate canonical exit status."
  artifacts:
    - path: "tests/runtime/test_startup_entrypoints.py"
      provides: "Static/command-spy contract for every supported launcher"
    - path: "start_full_system.sh"
      provides: "POSIX delegate to canonical full serve"
    - path: "start_full_system.bat"
      provides: "Windows delegate to canonical full serve"
  key_links:
    - from: "start_full_system.sh"
      to: "aura_backend/runtime/__main__.py"
      via: "uv run --locked --no-sync python -m aura_backend.runtime serve"
      pattern: "aura_backend.runtime serve"
---

<objective>
Make every supported launcher a thin, non-mutating delegate to the canonical runtime per D-05.

Purpose: Remove Linux/Windows drift and unsafe startup side effects without deleting unrelated stop/legacy artifacts.
Output: Thin root/backend wrappers and an exact entry-point contract test.
</objective>

## Artifacts this phase produces

- Cross-platform root launchers that execute one canonical command.
- Backend API/full/frontend delegates and a corrected explicit MCP delegate.
- Static and command-spy gates banning mutating startup behavior.

<execution_context>
@/home/ty/.Codex/gsd-core/workflows/execute-plan.md
@/home/ty/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-provider-and-runtime-core/02-CONTEXT.md
@.planning/phases/02-provider-and-runtime-core/02-RESEARCH.md
@.planning/phases/02-provider-and-runtime-core/02-11-SUMMARY.md
@start_full_system.sh
@start_full_system.bat
@aura_backend/start.sh
@aura_backend/start_api.sh
@aura_backend/start_all.sh
@aura_backend/start_frontend.sh
@aura_backend/start_mcp.sh
</context>

<dependencies>
Needs: Canonical preflight/serve CLI.
Creates: All supported shell/batch entry points aligned with that CLI.
Wave rationale: Wrapper behavior is changed only after the delegated command is tested.
</dependencies>

<artifacts_produced>
- `start_full_system.sh`, `start_full_system.bat`, `aura_backend/start.sh`, `aura_backend/start_api.sh`, `aura_backend/start_all.sh`, `aura_backend/start_frontend.sh`, and `aura_backend/start_mcp.sh` — exact non-installing CLI delegates.
- `tests/runtime/test_startup_entrypoints.py` — structural delegation, loopback, no-install, no-process-kill, and argument-forwarding proof.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 02-12-01: Replace root Linux/Windows launchers with exact delegates</name>
  <files>start_full_system.sh, start_full_system.bat, tests/runtime/test_startup_entrypoints.py</files>
  <read_first>
    @start_full_system.sh
    @start_full_system.bat
    @aura_backend/runtime/cli.py
    @tests/runtime/test_cli.py
  </read_first>
  <behavior>
    - Both root wrappers resolve the repository root safely and invoke `uv run --locked --no-sync python -m aura_backend.runtime serve` with forwarded arguments.
    - Missing uv is reported with remediation and nonzero exit; no install attempt occurs.
    - Child/canonical exit status is propagated and no terminal window, detached process, port kill, reload, package install, permission change, or `.env` write occurs.
  </behavior>
  <action>Write static token bans and injected command-spy tests first. Reduce both wrappers to cross-platform argument forwarding and the exact canonical locked/no-sync command. Keep output concise and safe. The batch script must not bind `0.0.0.0`; host choice remains inside validated runtime settings. Do not touch stop scripts or perform Git/data cleanup.</action>
  <acceptance_criteria>
    - Linux and Windows wrapper command arrays/exit propagation match.
    - Mutation/terminal/port-kill/reload tokens are absent from executable regions.
    - Wrapper tests never launch real services or modify the active environment.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/runtime/test_startup_entrypoints.py -k 'root or windows or linux'</automated>
  </verify>
  <done>The advertised root launchers are safe delegates with identical platform semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-12-02: Delegate backend API/full wrappers without environment activation</name>
  <files>aura_backend/start.sh, aura_backend/start_api.sh, aura_backend/start_all.sh, tests/runtime/test_startup_entrypoints.py</files>
  <read_first>
    @aura_backend/start.sh
    @aura_backend/start_api.sh
    @aura_backend/start_all.sh
    @aura_backend/runtime/cli.py
  </read_first>
  <behavior>
    - `start.sh`/`start_all.sh` delegate full `serve`; `start_api.sh` delegates `serve --backend-only` from the repository root.
    - Wrappers neither source/activate environments nor run direct `python main.py`/Uvicorn commands.
    - Signals and exit status are owned by/delegated to the canonical CLI.
  </behavior>
  <action>Replace the three backend orchestration scripts with small root-resolving delegates to the exact locked/no-sync runtime command. Remove custom process traps, manual venv activation, health polling, sleeps, and direct Python/Uvicorn starts. Preserve executable intent only; no chmod is performed by runtime or wrapper.</action>
  <acceptance_criteria>
    - Each script has one canonical delegate with the correct mode and forwarded args.
    - No duplicate process lifecycle/health logic remains.
    - Static/spy tests cover all three files and command failure propagation.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/runtime/test_startup_entrypoints.py -k 'backend or api or all'</automated>
  </verify>
  <done>Backend wrappers cannot drift from the tested runtime lifecycle or mutate dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-12-03: Make frontend and MCP wrappers explicit non-installing delegates</name>
  <files>aura_backend/start_frontend.sh, aura_backend/start_mcp.sh, tests/runtime/test_startup_entrypoints.py</files>
  <read_first>
    @aura_backend/start_frontend.sh
    @aura_backend/start_mcp.sh
    @aura_backend/runtime/cli.py
    @aura_backend/aura_server.py
  </read_first>
  <behavior>
    - Frontend wrapper delegates `serve --frontend-only` and never runs npm installation.
    - MCP wrapper explicitly invokes the existing supported `aura_backend.aura_server` module through locked/no-sync uv and is not represented as part of normal Aura readiness.
    - Missing optional MCP dependencies fail truthfully rather than falling through to a nonexistent script or success message.
  </behavior>
  <action>Replace frontend install/start logic with the canonical frontend-only mode. Point MCP startup at the actual `aura_backend.aura_server` module and label it as a separate explicit optional entry point, not normal startup. Add command-spy/static tests and exact nonzero propagation. Do not consolidate MCP implementations or change MCP protocol/tool behavior in this phase.</action>
  <acceptance_criteria>
    - Neither wrapper installs/syncs packages or claims readiness before its delegated command succeeds.
    - MCP no longer names a missing `mcp_server.py` file.
    - All seven wrapper files pass the complete static contract.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/runtime/test_startup_entrypoints.py</automated>
  </verify>
  <done>Specialized wrappers are explicit delegates and cannot silently install or claim unavailable services.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| User-invoked script → shell/OS | Legacy launchers currently install, kill, chmod, and bind services. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-ST | Tampering / Elevation | launch scripts | mitigate | Exact delegates only; static bans for install/sync/download/chmod/kill/reload/env writes. |
| T-02-LH | Information Disclosure | Windows/Linux bind behavior | mitigate | Host resolved only by validated runtime, loopback default on every platform. |
| T-02-HF | Repudiation | wrapper success | mitigate | Propagate canonical process exit; no early readiness messages. |
| T-02-SC | Tampering | package supply chain | mitigate | `--locked --no-sync`; no package command in wrappers. |
</threat_model>

<verification>
Run startup-entrypoint and CLI tests, then shell syntax-check the POSIX files and inspect batch command-spy evidence. Do not execute a real server/install/kill operation.
</verification>

<success_criteria>
- OPS-01 has one implementation across Linux and Windows.
- D-05 startup side effects are removed from every supported wrapper.
- Optional MCP remains explicit and truthful without crossing into consolidation.
</success_criteria>

<output>
Create `.planning/phases/02-provider-and-runtime-core/02-12-SUMMARY.md` when done.
</output>
