---
phase: 02-provider-and-runtime-core
plan: 06
type: tdd
wave: 4
depends_on:
  - 02-04
  - 02-05
files_modified:
  - aura_backend/providers/factory.py
  - aura_backend/aura_autonomic_system.py
  - tests/providers/test_factory.py
  - tests/runtime/test_autonomic_provider.py
autonomous: true
requirements:
  - TEST-03
  - AI-01
  - AI-03
must_haves:
  truths:
    - "The factory imports and constructs only the explicitly selected adapter, defaults to Ollama, and never silently falls back to Gemini."
    - "An Ollama-selected runtime can import/start without Google modules or credentials."
    - "Autonomic work uses an injected compatible provider or reports a truthful disabled/unavailable state; it never creates Gemini implicitly."
  artifacts:
    - path: "aura_backend/providers/factory.py"
      provides: "Validated lazy selected-adapter construction"
    - path: "aura_backend/aura_autonomic_system.py"
      provides: "Injected provider path or truthful optional-disable behavior"
    - path: "tests/providers/test_factory.py"
      provides: "Lazy import/selection/credential tests"
  key_links:
    - from: "aura_backend/providers/factory.py"
      to: "aura_backend/providers/config.py"
      via: "Factory branches only on validated ProviderKind"
      pattern: "ProviderKind"
    - from: "aura_backend/aura_autonomic_system.py"
      to: "aura_backend/providers/runtime.py"
      via: "Injected provider-neutral generation rather than Google client construction"
      pattern: "ProviderRuntime|ProviderRequest"
---

<objective>
Make provider selection and optional autonomic behavior genuinely local-first per D-01 and D-04.

Purpose: Eliminate eager imports, unknown-provider cloud fallback, and the hidden Gemini autonomic side channel.
Output: Lazy factory, provider-neutral/disabled autonomic path, and offline tripwire tests.
</objective>

## Artifacts this phase produces

- Lazy adapter construction in `providers/factory.py`.
- Explicit injected/disabled autonomic model behavior.
- Tests that forbid unselected SDK imports/client construction/network.

<execution_context>
@/home/ty/.Codex/gsd-core/workflows/execute-plan.md
@/home/ty/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-provider-and-runtime-core/02-CONTEXT.md
@.planning/phases/02-provider-and-runtime-core/02-RESEARCH.md
@.planning/phases/02-provider-and-runtime-core/02-04-SUMMARY.md
@.planning/phases/02-provider-and-runtime-core/02-05-SUMMARY.md
@aura_backend/providers/factory.py
@aura_backend/aura_autonomic_system.py
</context>

<dependencies>
Needs: All three provider adapters and validated settings.
Creates: Selected-provider construction seam consumed by ApplicationRuntime.
Wave rationale: Selection cannot be finalized before the adapters implement one contract.
</dependencies>

<artifacts_produced>
- `aura_backend/providers/factory.py` — explicit lazy provider selection with no cloud fallback.
- `aura_backend/aura_autonomic_system.py` — selected-provider injection instead of implicit Gemini construction.
- `tests/providers/test_factory.py` and `tests/runtime/test_autonomic_provider.py` — selection and no-implicit-provider contracts.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 02-06-01: Select one adapter lazily and fail unknown configuration</name>
  <files>aura_backend/providers/factory.py, tests/providers/test_factory.py</files>
  <read_first>
    @aura_backend/providers/config.py
    @aura_backend/providers/factory.py
    @aura_backend/providers/ollama.py
    @aura_backend/providers/openrouter.py
    @aura_backend/providers/gemini.py
  </read_first>
  <behavior>
    - Default settings construct Ollama only; Gemini/OpenRouter modules remain absent from `sys.modules` when unselected.
    - Explicit Gemini/OpenRouter settings construct only that adapter after credential validation.
    - Unknown provider and missing selected-cloud credential raise `configuration` without importing/constructing any adapter.
    - Factory logging exposes only safe provider/model identifiers and no secret/base-URL query/header content.
  </behavior>
  <action>Write module-import and constructor tripwire tests, then replace top-level concrete imports with imports inside exact validated branches. Accept an explicit `ProviderSettings` and neutral tool executor; retain a compatibility `get_provider()` wrapper for existing callers while making it delegate to the new strict constructor. Remove the unknown-name Gemini fallback. Do not read `.env` or install/initialize every provider to discover availability.</action>
  <acceptance_criteria>
    - Ollama-only test succeeds with Google modules made unavailable and no cloud keys.
    - Unknown/missing-credential tests observe zero adapter constructions.
    - Only the selected module/client is imported and no secret sentinel reaches logs/errors.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/providers/test_factory.py</automated>
  </verify>
  <done>The selected adapter is explicit, local-first, lazily imported, and incapable of silent cloud fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 02-06-02: Remove the autonomic subsystem's implicit Gemini client</name>
  <files>aura_backend/aura_autonomic_system.py, tests/runtime/test_autonomic_provider.py</files>
  <read_first>
    @aura_backend/aura_autonomic_system.py
    @aura_backend/providers/base.py
    @aura_backend/providers/runtime.py
    @tests/providers/fakes.py
  </read_first>
  <behavior>
    - Autonomic initialization with no compatible injected provider returns a stable disabled/not-configured status and starts no worker/model client.
    - With an injected fake provider, autonomic generation uses `ProviderRequest` and typed results without provider-name or SDK branching.
    - Timeout, cancellation, unavailable, and resource-limit outcomes remain failures and update safe counts/status only.
    - No Google import/client is performed for an Ollama-selected or disabled autonomic configuration.
  </behavior>
  <action>Inject a provider-neutral generation callable/runtime into `AutonomicProcessor` and initialization. Replace `_call_autonomic_model`'s local Google import/client/synchronous call with the injected boundary. If the selected provider cannot safely support this optional subsystem under current behavior, keep it truthfully disabled with a typed reason rather than silently constructing Gemini. Preserve queue/task/status APIs and do not redesign autonomic prompts, thresholds, persistence, or quality.</action>
  <acceptance_criteria>
    - Google import/client tripwires remain untouched in Ollama/disabled tests.
    - Injected fake success and every non-success terminal state are assertion-tested.
    - Default logs/status contain no task payload, prompt, result, exception, or credential.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/runtime/test_autonomic_provider.py</automated>
  </verify>
  <done>Optional autonomic work cannot create a silent cloud dependency and is honest when not configured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Provider configuration → adapter import/client | A typo or ambient key must not select an unintended paid/cloud provider. |
| Conversation-derived autonomic task → model | Sensitive task payload and cost/resource use require explicit provider ownership. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-PC | Spoofing / Information Disclosure / Cost | provider factory | mitigate | Validated exact branch, lazy import, no unknown fallback, explicit cloud key. |
| T-02-PL | Information Disclosure | autonomic payload/logs | mitigate | Provider-neutral typed calls and safe count/status logging only. |
| T-02-CD | Denial of Service | autonomic tasks | mitigate | Existing queue/rate limits plus runtime timeout/cancellation; non-success remains non-success. |
| T-02-SC | Tampering | package supply chain | mitigate | No package changes; adapters already locked. |
</threat_model>

<verification>
Run both focused files and the provider adapter suites with Google modules blocked for Ollama cases. Re-run Phase 1 companion/local tests to prove default behavior remains available.
</verification>

<success_criteria>
- AI-01 selection and autonomic use are adapter-neutral.
- D-01 has no silent cloud path.
- AI-03 covers selection/autonomic failures and D-02 redaction remains intact.
</success_criteria>

<output>
Create `.planning/phases/02-provider-and-runtime-core/02-06-SUMMARY.md` when done.
</output>
