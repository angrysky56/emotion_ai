---
phase: 03-memory-integrity-and-data-lifecycle
plan: 07
type: execute
wave: 5
depends_on: [03-06]
files_modified:
  - .planning/evidence/phase-03/inventory-proposal.json
  - .planning/evidence/phase-03/inventory-summary.json
  - .planning/evidence/phase-03/quiescence-summary.json
  - tests/storage/test_phase3_evidence.py
autonomous: false
requirements: [PRES-04, TEST-03, DATA-01, DATA-02]
must_haves:
  truths:
    - "Before any real root is resolved, statted, hashed, opened, inventoried, or preflighted, Ty approves an exact content-free root/path/role and outside-Git evidence proposal proven only against synthetic fixtures."
    - "A fresh content-safe inventory covers both historical Chroma roots, both data roots, all prior backup/test/archive roots, and the preserved Memvid v2 archive root without reading conversation content."
    - "Phase 1's public preservation hashes/evidence remain immutable and are compared rather than replaced or rewritten."
    - "No backup or real-data import begins until writer/open-handle/space/path checks pass and Ty approves the exact source set and quiescence."
  artifacts:
    - path: ".planning/evidence/phase-03/inventory-proposal.json"
      provides: "Exact content-free repo-relative root/role/run/evidence-path proposal requiring pre-inventory approval"
    - path: ".planning/evidence/phase-03/inventory-summary.json"
      provides: "Sanitized fresh metadata/root-role/fingerprint evidence bound to private details outside Git"
    - path: ".planning/evidence/phase-03/quiescence-summary.json"
      provides: "Sanitized short-lived source-set/preflight ticket"
    - path: "tests/storage/test_phase3_evidence.py"
      provides: "Exact root, privacy, Phase 1 immutability, and evidence-binding verifier"
  key_links:
    - from: ".planning/evidence/phase-03/inventory-proposal.json"
      to: ".planning/evidence/phase-03/inventory-summary.json"
      via: "pre-inventory human approval and exact proposal digest binding"
      pattern: "proposal_sha256|approved_root_alias|approved_role"
    - from: ".planning/evidence/phase-03/inventory-summary.json"
      to: ".planning/evidence/phase-01/inventory-summary.json"
      via: "content-free comparison retaining Phase 1 hashes and declared anomalies"
      pattern: "source_set_sha256|private_artifact_sha256"
    - from: ".planning/evidence/phase-03/quiescence-summary.json"
      to: ".planning/evidence/phase-03/inventory-summary.json"
      via: "inventory digest and exact source-set binding"
      pattern: "inventory_summary_sha256|source_set_sha256"
---

<objective>
Prove the inventory tooling on synthetic fixtures, obtain approval before touching real root metadata, then inventory/preflight and pause again for backup authorization.

Purpose: Ensure the preserved source set is current—including Memvid v2 and both divergent roots—before copying, restoring, reading, migrating, or switching anything.
Output: Content-free proposal, two explicit human gates, public-safe inventory/preflight evidence, private details outside Git, and a truthful blocked result for any non-pass.
</objective>

## Artifacts this plan produces

- `/backup/aura-phase-03/phase-03-storage-gate-01/inventory.private.json` with mode-restricted per-file details.
- `/backup/aura-phase-03/phase-03-storage-gate-01/quiescence*.private.json` with process/open-handle/space details.
- Public allowlisted summaries containing only aliases, roles, counts, bytes, digests, database-check aggregates, and safe statuses.
- A public proposal containing only fixed repo-relative roots, roles, run/evidence paths, operation scope, and no observed real metadata.
- No backup copy, Chroma public-API read, import, read switch, repair, deletion, or source mutation.

<execution_context>
@/home/ty/.Codex/gsd-core/workflows/execute-plan.md
@/home/ty/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-memory-integrity-and-data-lifecycle/03-CONTEXT.md
@.planning/phases/03-memory-integrity-and-data-lifecycle/03-RESEARCH.md
@.planning/phases/01-preservation-and-trusted-baseline/01-VERIFICATION.md
@.planning/evidence/phase-01/inventory-summary.json
@.planning/evidence/phase-01/restore-drill-summary.json
@aura_backend/preservation/cli.py
@aura_backend/preservation/inventory.py
@tests/preservation/test_inventory.py
</context>

<dependencies>
Needs: Plan 03-06's finalized storage paths/owner and the complete Phase 1/2 regression baseline.
Creates: Pre-inventory approval plus a separately human-approved current source-set ticket required by Plan 03-08.
Wave rationale: Real metadata work begins only after all ordinary implementation is complete, synthetic readiness is green, and Ty approves the exact metadata-only proposal.
</dependencies>

<scope_fence>
- This is a non-autonomous real-data checkpoint plan. D-04 requires both active Chroma roots and all historical stores to remain immutable.
- Before Task 03-07-02 approval, do not resolve, stat, glob, list, hash, open, inventory, or preflight any real historical root. Synthetic `tmp_path` fixtures and the fixed content-free proposal are the only allowed inputs.
- After Task 03-07-02 approval, metadata/hash/read-only SQLite checks are allowed only through the exact proposal and proven preservation inventory. Do not instantiate Chroma/Memvid, read documents/metadata values, print private paths/content, repair FK anomalies, or choose a canonical root.
- Preserve the exact Phase 1 evidence files/hashes; create Phase 3 evidence separately. No backup, import, read switch, deletion, retention, tracked-data removal, or Git-history change occurs here.
- If any required root/check, writer/open handle, path containment, free-space, privacy, or binding check is non-pass, stop and retain evidence. Never omit a root or stop/kill a process to manufacture pass.
</scope_fence>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-07-01: Prove synthetic readiness and publish the content-free inventory proposal</name>
  <files>.planning/evidence/phase-03/inventory-proposal.json, tests/storage/test_phase3_evidence.py</files>
  <read_first>
    @.planning/phases/03-memory-integrity-and-data-lifecycle/03-CONTEXT.md
    @.planning/evidence/phase-01/inventory-summary.json
    @.planning/evidence/phase-01/restore-drill-summary.json
    @aura_backend/preservation/cli.py
    @aura_backend/preservation/inventory.py
    @tests/preservation/test_inventory.py
  </read_first>
  <behavior>
    - Proposal names the six Phase 1 active roots, three backup roots, two test roots, three archive roots, `aura_backend/memvid_videos`, conditional `aura_data_v2`, roles, run ID, public/private evidence destinations, backup target, operation `metadata_inventory_preflight_only`, and explicit prohibitions without resolving any root.
    - Proposal is content-free and observation-free: no existence result, stat, size, count, timestamp, hash, filename, open-handle/process fact, document, embedding, metadata value, secret, or conversation text.
    - Synthetic tests exercise inventory, quiescence, privacy, evidence binding, missing roots, writers, handles, capacity, and Phase 1 hash invariance entirely under pytest `tmp_path` fixtures.
    - Any tool/readiness failure or proposal mismatch is non-pass and grants no real-root authority.
  </behavior>
  <action>Use an evidence tracer. RED: extend `test_phase3_evidence.py` with absent/incomplete/widened proposal cases and synthetic inventory/preflight failures for exact roots/roles, privacy, Phase 1 hashes, source-set/ticket binding, writer/handle/capacity/path failures, and content leakage. GREEN: create `inventory-proposal.json` from locked Phase 1/Phase 3 declarations using fixed repo-relative roots only; do not resolve, stat, glob, list, hash, open, inventory, or preflight those real paths. Exercise `aura_backend.preservation.cli` inventory/preflight behavior only through pytest-created synthetic directories and databases, and require the exact pytest verifier. Refactor verifier helpers only while green. Do not run any preservation command against `/home/ty/Repositories/ai_workspace/emotion_ai` or `/backup/aura-phase-03`, instantiate Chroma/Memvid, or start backup/import/switch work.</action>
  <acceptance_criteria>
    - Synthetic inventory/preflight, failure, privacy, and binding tests pass.
    - Proposal contains the exact root/path/role/run/evidence/backup scope and no observed real metadata.
    - No real historical root or outside-Git backup destination has been resolved, statted, listed, hashed, opened, inventoried, or preflighted.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/storage/test_phase3_evidence.py -k 'proposal or synthetic_readiness or phase1'</automated>
  </verify>
  <done>The exact content-free proposal and synthetic readiness proof exist, while real root metadata remains untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 03-07-02: Authorize the exact metadata-only inventory and preflight</name>
  <read_first>
    @.planning/evidence/phase-03/inventory-proposal.json
    @.planning/evidence/phase-01/inventory-summary.json
    @.planning/evidence/phase-01/restore-drill-summary.json
  </read_first>
  <action>Run the synthetic readiness/proposal verifier immediately before pausing. Ty reviews the exact fixed repo-relative aliases, roles, run/evidence paths, conditional `aura_data_v2` rule, and `/backup/aura-phase-03` target. Approval authorizes only metadata/hash/read-only SQLite inventory and writer/open-handle/space/path preflight for this proposal; it does not authorize Chroma/Memvid opens, backup, restore, import, switch, repair, deletion, or content/metadata-value reads.</action>
  <what-built>A content-free proposal and synthetic proof of the metadata inventory/preflight tooling; no real path has been observed.</what-built>
  <how-to-verify>1. Confirm the proposal lists both Chroma roots, all Phase 1 roles, `aura_backend/memvid_videos`, and the conditional new ledger root. 2. Confirm operation scope is metadata inventory/preflight only. 3. Confirm public/private evidence paths and backup target are exact. 4. Confirm the proposal contains no observed metadata. 5. Approve only this real metadata inventory/preflight.</how-to-verify>
  <acceptance_criteria>
    - Synthetic readiness and exact proposal verification pass.
    - Ty explicitly approves only the listed real metadata inventory/preflight scope.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/storage/test_phase3_evidence.py -k 'proposal or synthetic_readiness or phase1'</automated>
    <human-check>Ty reviews the exact proposal and replies `approved` for metadata inventory/preflight only.</human-check>
  </verify>
  <resume-signal>Type `approved` to authorize only the exact metadata inventory/preflight proposal; otherwise describe the proposal concern.</resume-signal>
  <done>Human and automated gates authorize the exact real metadata inventory/preflight and no backup or data read.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 03-07-03: Run approved inventory/preflight and separately authorize the backup</name>
  <read_first>
    @.planning/evidence/phase-03/inventory-proposal.json
    @.planning/evidence/phase-01/inventory-summary.json
    @.planning/evidence/phase-01/restore-drill-summary.json
    @aura_backend/preservation/cli.py
    @tests/storage/test_phase3_evidence.py
  </read_first>
  <action>Only after Task 03-07-02 approval, extend the evidence tracer with absent/incomplete/forged real-summary cases, then run the preservation inventory with repository root `/home/ty/Repositories/ai_workspace/emotion_ai`, backup root `/backup/aura-phase-03`, run ID `phase-03-storage-gate-01`, the proposal's fixed private/public outputs, and its exact declared roots/roles. Include `aura_data_v2` only if the approved conditional rule observes it exists and record that fact explicitly. Run preflight with the new inventory, quiescence summary, exact backup target, and 900-second TTL. Require content-safe public fields, private mode-restricted evidence, both historical roots kept separate, Phase 1 evidence before/after hashes unchanged, no Chroma/Memvid instantiation, and the exact automated verifier. Then pause for a separate human backup approval. If evidence is blocked or expires, rerun only preflight within the already approved proposal; any root/scope/path change requires returning to Task 03-07-02.</action>
  <what-built>A fresh approved metadata-only inventory and current quiescence/source-set ticket; no backup or historical content read/import has begun.</what-built>
  <how-to-verify>1. Confirm Aura is stopped and no maintenance/import/export is running. 2. Confirm the public root-role set exactly matches the approved proposal, including both Chroma roots and Memvid v2. 3. Confirm all checks pass, Phase 1 evidence hashes are unchanged, private details remain outside Git, and the ticket is current. 4. Approve only one backup of this exact source set to `/backup/aura-phase-03`.</how-to-verify>
  <acceptance_criteria>
    - Inventory/preflight ran only after Task 03-07-02 approval and exactly matches that proposal.
    - The exact evidence verifier passes on a current ticket with unchanged Phase 1 evidence.
    - Ty separately approves one exact outside-Git backup operation and nothing else.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/storage/test_phase3_evidence.py -k 'inventory or quiescence or phase1'</automated>
    <human-check>Ty confirms the fresh evidence and replies `approved` for one exact outside-Git backup.</human-check>
  </verify>
  <resume-signal>Type `approved` only while the ticket is current and exact; otherwise describe the inventory/writer/handle/space discrepancy.</resume-signal>
  <done>A separate human and automated gate authorizes one exact outside-Git preservation run and nothing else.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Content-free proposal → pre-inventory approval | Fixed scope must be approved before any real root metadata is observed. |
| Real historical roots → metadata inventory | Sensitive content-bearing stores are inspected only through allowlisted metadata/hash checks. |
| Live process/files → quiescence ticket | Writer/open-handle state determines whether a coherent backup may begin. |
| Private evidence → committed summary | Detailed paths/process/file facts must stay outside Git. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-07-01 | Information Disclosure | public inventory | mitigate | Phase 1 allowlist/private split plus adversarial privacy tests. |
| T-03-07-02 | Tampering / Repudiation | root/source-set evidence | mitigate | Exact aliases/roles, hashes, source-set digest, immutable Phase 1 hash comparison. |
| T-03-07-03 | Tampering / Denial of Service | quiescence | mitigate | Process/open-handle/space/path checks, short TTL, blocking human approval, no forced stop. |
| T-03-07-04 | Elevation / Information Disclosure | pre-inventory scope | mitigate | Synthetic-only readiness, observation-free proposal, and blocking approval before real stat/hash/open. |
| T-03-07-SC | Tampering | package supply chain | mitigate | Existing locked preservation CLI only; no dependency/install/sync/lock action. |
</threat_model>

<verification>
The plan completes only after the synthetic proposal verifier passes, Ty approves metadata inventory/preflight, the resulting current ticket passes its verifier, and Ty separately approves one exact backup. Any non-pass or expired evidence leaves the plan blocked and authorizes no copy/import/switch.
</verification>

<source_coverage>
## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Coverage | Status |
|---|---|---|---|---|
| GOAL | — | Recoverable truthful lifecycle | Pre-inventory and preservation gates | COVERED |
| REQ | DATA-01 | Enumerated ownership roots | Approved proposal then exact fresh root inventory | COVERED |
| REQ | DATA-02 | Backup preconditions | Separate metadata and quiescence/backup authorizations | COVERED |
| REQ | PRES-04 | Private evidence outside Git | Private/public split | COVERED |
| REQ | TEST-03 | Evidence/failure tests | Exact adversarial verifier | COVERED |
| CONTEXT | D-04 | Originals immutable until parity | Synthetic proposal plus two checkpoints | COVERED |
| RESEARCH | Refresh 14 roots + Memvid v2 | Approved exact 15-root declaration, conditional v2 root | COVERED |

Backup/restore occurs in Plan 03-08 and import/read switch in Plan 03-09. No deferred feature is implemented here.
</source_coverage>

<success_criteria>
- Ty authorizes the exact content-free inventory/preflight proposal before any real metadata observation.
- Fresh inventory/preflight evidence is exact, private-safe, and bound to unchanged Phase 1 evidence.
- Ty separately authorizes the exact source set while quiescent for one backup.
- No source, backup, archive, database, tracked artifact, or runtime setting is modified.
</success_criteria>

<output>
Create `.planning/phases/03-memory-integrity-and-data-lifecycle/03-07-SUMMARY.md` when done.
</output>
