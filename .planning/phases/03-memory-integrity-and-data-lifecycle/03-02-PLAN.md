---
phase: 03-memory-integrity-and-data-lifecycle
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - aura_backend/storage/benchmark.py
  - tests/storage/test_memory_benchmark.py
  - tests/fixtures/memory_eval/corpus.jsonl
  - tests/fixtures/memory_eval/manifest.json
autonomous: true
requirements: [TEST-03, DATA-05, DATA-09]
must_haves:
  truths:
    - "A versioned, sanitized corpus and immutable manifest exist before retrieval scoring or optimization is accepted."
    - "Positive, stale-correction, absent-fact, cross-scope-leak, provenance, duplicate, prompt-injection, temporal, and pagination controls can distinguish a faithful instrument from a broken one."
    - "Timeout, RESOURCE_LIMIT, truncated input/search, missing controls, or incomplete repetitions are inconclusive non-pass outcomes rather than wins."
    - "The neutral arm and Phase 4 constant-salience control are named independently; no dynamic affect changes eligibility or ranking in Phase 3."
  artifacts:
    - path: "tests/fixtures/memory_eval/corpus.jsonl"
      provides: "Invented fixed evaluation events/queries including a 10,000-event load slice"
    - path: "tests/fixtures/memory_eval/manifest.json"
      provides: "Pre-registered hashes, metrics, thresholds, controls, bounds, and runtime labels"
    - path: "aura_backend/storage/benchmark.py"
      provides: "Fail-closed corpus loader, instrument controller, metrics, and kill-gate outcomes"
  key_links:
    - from: "aura_backend/storage/benchmark.py"
      to: "tests/fixtures/memory_eval/manifest.json"
      via: "canonical corpus/manifest digest and exact control inventory"
      pattern: "corpus_sha256|control"
    - from: "tests/storage/test_memory_benchmark.py"
      to: ".planning/research/affective-memory/KILL-CRITERIA.md"
      via: "exact competence, containment, resource, and alternative-adoption gates"
      pattern: "Recall@5|cross-user|RESOURCE_LIMIT"
---

<objective>
Build the sanitized measurement instrument before any retrieval implementation can be optimized or promoted.

Purpose: Prevent attractive ranking output, a leaky evaluator, or a truncated/resource-limited run from becoming evidence.
Output: Versioned synthetic corpus, frozen manifest, benchmark runner, positive/negative controls, and pre-registered gate evaluation.
</objective>

## Artifacts this plan produces

- Invented direct/paraphrase/correction/temporal/distractor/absence/duplicate/provenance/cross-scope/injection/pagination cases.
- A deterministic 10,000-event load slice with fixed seed and three-repetition timing contract.
- Exact baseline and alternative kill-gate outcomes with `pass`, `fail`, and `inconclusive` kept distinct.
- No production ranking or performance claim; this plan validates the ruler before measuring the system.

<execution_context>
@/home/ty/.Codex/gsd-core/workflows/execute-plan.md
@/home/ty/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-memory-integrity-and-data-lifecycle/03-CONTEXT.md
@.planning/phases/03-memory-integrity-and-data-lifecycle/03-RESEARCH.md
@.planning/research/affective-memory/KILL-CRITERIA.md
@.planning/research/affective-memory/AFFECT-MEMORY-LOOP.md
@.planning/research/affective-memory/FINDINGS.md
@aura_backend/storage/models.py
@aura_backend/storage/repository.py
</context>

<dependencies>
Needs: Plan 03-01 typed scope/origin/provenance identities.
Creates: The independent instrument consumed by neutral retrieval and the final import/read-switch gate.
Wave rationale: Runs in parallel with projection work because it modifies only benchmark fixtures/harness files and calls a retriever through an injected protocol.
</dependencies>

<scope_fence>
- Implement D-08 exactly: measurement and instrument controls precede optimization. Use invented content only; never copy, paraphrase, inspect, or hash personal conversation text into these fixtures.
- Do not implement a graph, RLM reader, Memvid primary path, dynamic affect, non-zero salience, provider/model evaluation, or frontend work.
- Do not add/install/sync a package or modify any manifest/lock. GPU use is optional; CPU correctness is mandatory.
- Keep pre-registered thresholds exact; do not lower, raise, or omit a gate to turn a failed run into success.
</scope_fence>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-02-01: Freeze the sanitized corpus and manifest contract</name>
  <files>tests/fixtures/memory_eval/corpus.jsonl, tests/fixtures/memory_eval/manifest.json, aura_backend/storage/benchmark.py, tests/storage/test_memory_benchmark.py</files>
  <read_first>
    @.planning/research/affective-memory/KILL-CRITERIA.md
    @.planning/phases/03-memory-integrity-and-data-lifecycle/03-RESEARCH.md
    @aura_backend/storage/models.py
    @tests/preservation/test_manifest_privacy.py
  </read_first>
  <behavior>
    - The corpus contains only invented scope IDs, events, derived memories, corrections, queries, expected origin IDs, and distractors; a privacy sentinel scan rejects production-root/path/content markers.
    - The fixed case inventory covers direct and paraphrased recall, correction/stale exclusion, temporal questions, distractors, absent critical facts, duplicates, provenance, identical cross-scope content, stored prompt injection, and stable pagination.
    - A deterministic fixed-seed load slice contains at least 10,000 synthetic events without relying on runtime generation or network/model calls.
    - The manifest binds corpus hash/version, case IDs, expected origins, FTS tokenizer, cosine metric, embedding fingerprint slot, caps, RRF k=60, admission thresholds, page bounds, runtime/hardware label, fixed repetitions, and benchmark code commit field.
    - Any missing/duplicate/unknown case, digest mismatch, personal-data marker, unbound threshold, or incomplete control makes the instrument invalid.
  </behavior>
  <action>Run a complete tracer cycle. RED: write manifest/corpus validators and adversarial mutations before filling the complete fixtures, ensuring failures name only safe case IDs/codes. GREEN: populate the full invented corpus and canonical manifest until exact-set, hash, privacy, determinism, and 10,000-event assertions pass. REFACTOR only while the focused suite remains green, keeping JSONL deterministic and reviewable. Implement D-08 without measuring the unfinished retrieval service and without using production data.</action>
  <acceptance_criteria>
    - Corpus and manifest hashes are stable and exact-set validation is fail-closed.
    - Every required case class and the 10,000-event slice exist with invented content.
    - No real root is opened and no production text enters fixtures, reports, or logs.
    - The fixture loader is bounded and rejects malformed/truncated input.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/storage/test_memory_benchmark.py -k 'corpus or manifest or privacy'</automated>
  </verify>
  <done>A content-safe, deterministic, pre-registered corpus exists before system measurement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-02-02: Prove benchmark controls and truthful gate outcomes</name>
  <files>aura_backend/storage/benchmark.py, tests/storage/test_memory_benchmark.py, tests/fixtures/memory_eval/manifest.json</files>
  <read_first>
    @tests/fixtures/memory_eval/corpus.jsonl
    @tests/fixtures/memory_eval/manifest.json
    @.planning/research/affective-memory/KILL-CRITERIA.md
    @aura_backend/storage/benchmark.py
  </read_first>
  <behavior>
    - A unique exact fact positive control retrieves; a correction control excludes stale memory; an absent fact abstains.
    - A deliberately scope-free retriever triggers the leak detector, a provenance-dropping retriever triggers the provenance detector, and a stale-preferring retriever triggers the correction detector.
    - Baseline gates are Recall@5 at least 0.90 for direct/paraphrase, correction accuracy at least 0.90, critical-absent abstention exactly 1.00, cross-scope leaks zero, selected items without provenance zero, and warmed local p95 below 250 ms at 10,000 events.
    - Three fixed timing repetitions are required; missing control, timeout, resource limit, truncated corpus/search, or incomplete repetition produces `inconclusive`, never `pass`.
    - Alternative adoption requires at least five absolute quality points or 30 percent storage/latency improvement with no correctness, stale, abstention, provenance, privacy, or restore regression; bounded-cycle stopping is reported without implementing an alternative.
  </behavior>
  <action>Use RED, smallest complete GREEN, then optional REFACTOR. RED: drive the runner with faithful, leaky, stale, provenance-free, truncated, timeout, and resource-limited fake retrievers; require the negative controls to fail for the intended reason. GREEN: implement deterministic metric aggregation and typed outcome evaluation against the frozen manifest, preserving per-case evidence and content-free aggregate reports. REFACTOR only while all controls still distinguish faithful from broken instruments. Record neutral and constant-salience arms as separate named interfaces, both with salience contribution exactly zero in Phase 3; do not implement dynamic affect.</action>
  <acceptance_criteria>
    - All positive and negative controls produce their predeclared result.
    - Inconclusive conditions cannot satisfy any competence or adoption gate.
    - Metrics are derived from fixed expected origin IDs and scope/provenance facts, not model-authored labels.
    - Reports contain IDs, counts, timings, codes, and hashes only.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/storage/test_memory_benchmark.py</automated>
  </verify>
  <done>The benchmark is a faithful instrument with fixed competence, containment, and stopping semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Fixture authoring → committed corpus | Synthetic evaluation data must not contain personal/runtime content. |
| Retriever output → benchmark verdict | Candidate IDs/scores can be partial, stale, scoped incorrectly, or fabricated. |
| Resource state → result status | Timeout/truncation/resource limits must not become a positive result. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-02-01 | Information Disclosure | corpus/report | mitigate | Invented exact-set fixtures, privacy sentinel scan, content-free aggregates. |
| T-03-02-02 | Spoofing / Tampering | benchmark controls | mitigate | Positive plus deliberately leaky/stale/orphan ablations must produce exact expected verdicts. |
| T-03-02-03 | Repudiation | manifest/thresholds | mitigate | Canonical hashes and frozen case/metric/control inventory. |
| T-03-02-04 | Denial of Service | load/timing run | mitigate | Fixed bounds/repetitions and explicit inconclusive resource outcomes. |
| T-03-02-SC | Tampering | package supply chain | mitigate | Existing locked environment only; no package, manifest, lock, install, or sync change. |
</threat_model>

<verification>
- Capture each task's intended RED failure, then run its exact GREEN command.
- Run `uv run --locked --no-sync python -m pytest -q tests/storage/test_memory_benchmark.py tests/storage/test_atomic_ledger.py tests/storage/test_provenance_supersession.py`.
- At the wave boundary run `uv run --locked --no-sync python -m pytest tests -q -m 'not live'`.
</verification>

<source_coverage>
## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Coverage | Status |
|---|---|---|---|---|
| GOAL | — | Truthful memory behavior | Faithful, pre-registered instrument | COVERED |
| REQ | DATA-05 | Measured relevance/latency/duplicates | Fixed cases, load, repetitions, bounds | COVERED |
| REQ | DATA-09 | Neutral/contained retrieval evidence | Leak/stale/provenance/injection controls | COVERED |
| REQ | TEST-03 | Automated failure behavior | Adversarial instrument suite | COVERED |
| CONTEXT | D-08 | Sanitized corpus before optimization | Both tasks | COVERED |
| RESEARCH | Kill criteria/resource semantics | Exact thresholds and inconclusive states | COVERED |

Dynamic salience, graph/RLM, Memvid adoption, provider quality, real-data evaluation, and frontend work remain deferred and are not gaps.
</source_coverage>

<success_criteria>
- The fixture inventory is complete, sanitized, hash-bound, and deterministic.
- Broken instruments fail their intended controls and resource-limited work remains inconclusive.
- No production ranking claim is made before Plan 03-04 connects the real neutral retriever.
</success_criteria>

<output>
Create `.planning/phases/03-memory-integrity-and-data-lifecycle/03-02-SUMMARY.md` when done.
</output>
