---
phase: 03-memory-integrity-and-data-lifecycle
plan: 08
type: execute
wave: 6
depends_on: [03-07]
files_modified:
  - .planning/evidence/phase-03/restore-drill-summary.json
  - .planning/evidence/phase-03/migration-plan-summary.json
  - .planning/evidence/phase-03/migration-authorization-receipt.json
  - tests/storage/test_phase3_evidence.py
autonomous: false
requirements: [PRES-04, TEST-03, DATA-01, DATA-02]
must_haves:
  truths:
    - "The Ty-approved exact source set has a fresh immutable outside-Git copy with source-before/source-after/destination parity."
    - "A disposable restore proves hashes, SQLite integrity/FK parity, Chroma counts, opaque retrieval, and the known historical anomalies without opening source or durable-backup Chroma paths."
    - "A content-free migration plan names only disposable-copy aliases/fingerprints, both historical roots, target ledger/projection paths, required parity/benchmark/rollback gates, and projected space."
    - "No restored-copy content is opened for import and no read-owner switch occurs until Ty separately authorizes the exact plan."
    - "An immutable public-safe authorization receipt exists only after approval and binds the exact aliases/fingerprints, restore and migration-plan digests, fixed scope, issued/expiry times, approved result, and private authorization-summary digest."
  artifacts:
    - path: ".planning/evidence/phase-03/restore-drill-summary.json"
      provides: "Sanitized fresh backup/isolated-restore evidence bound to inventory/quiescence"
    - path: ".planning/evidence/phase-03/migration-plan-summary.json"
      provides: "Content-free exact disposable-copy import/read-switch proposal"
    - path: ".planning/evidence/phase-03/migration-authorization-receipt.json"
      provides: "Write-once public-safe authorization capability bound to the exact human-approved scope and private summary digest"
    - path: "/backup/aura-phase-03/phase-03-storage-gate-01/authorizations/migration-authorization-summary.private.json"
      provides: "Mode-0600 exact approval/path summary whose canonical digest is bound by the public receipt"
    - path: "tests/storage/test_phase3_evidence.py"
      provides: "Exact restore/migration-plan completeness, privacy, and binding checks"
  key_links:
    - from: ".planning/evidence/phase-03/restore-drill-summary.json"
      to: ".planning/evidence/phase-03/inventory-summary.json"
      via: "inventory, quiescence, source-set, and backup-result digests"
      pattern: "inventory_summary_sha256|source_set_sha256|backup_result_sha256"
    - from: ".planning/evidence/phase-03/migration-plan-summary.json"
      to: ".planning/evidence/phase-03/restore-drill-summary.json"
      via: "restore digest, approved aliases/fingerprints, and exact gate inventory"
      pattern: "restore_summary_sha256|source_alias"
    - from: ".planning/evidence/phase-03/migration-authorization-receipt.json"
      to: ".planning/evidence/phase-03/migration-plan-summary.json"
      via: "exact restore/plan digests, alias/fingerprint set, fixed scope, approved result, expiry, and private authorization-summary digest"
      pattern: "authorization_summary_sha256|migration_plan_summary_sha256|approval_result|expires_at"
    - from: ".planning/evidence/phase-03/migration-authorization-receipt.json"
      to: "/backup/aura-phase-03/phase-03-storage-gate-01/authorizations/migration-authorization-summary.private.json"
      via: "canonical private-summary digest without exposing its sensitive absolute paths"
      pattern: "authorization_summary_sha256"
---

<objective>
Create a fresh immutable backup, prove an isolated restore, then pause for explicit authorization to read disposable copies and switch reads.

Purpose: Convert preservation from an old snapshot into a current, exact safety gate before any historical content is imported.
Output: Outside-Git backup/restore evidence, a content-free migration plan, Ty's explicit authorization, and an immutable content-free authorization receipt or a truthful blocked result.
</objective>

## Artifacts this plan produces

- `/backup/aura-phase-03/phase-03-storage-gate-01/backup/` as the current immutable source-set copy.
- Private backup/restore attempts under `/backup/aura-phase-03/phase-03-storage-gate-01/`, mode restricted and never committed.
- A public restore summary with exact checks/counts/digests only.
- A public migration proposal that grants no authority until the checkpoint is approved.
- `/backup/aura-phase-03/phase-03-storage-gate-01/authorizations/migration-authorization-summary.private.json` as the mode-0600 exact authorization summary.
- `.planning/evidence/phase-03/migration-authorization-receipt.json` as the exclusive-create public-safe receipt containing no absolute source path or content.

<execution_context>
@/home/ty/.Codex/gsd-core/workflows/execute-plan.md
@/home/ty/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-memory-integrity-and-data-lifecycle/03-CONTEXT.md
@.planning/phases/03-memory-integrity-and-data-lifecycle/03-RESEARCH.md
@.planning/phases/03-memory-integrity-and-data-lifecycle/03-07-SUMMARY.md
@.planning/evidence/phase-03/inventory-summary.json
@.planning/evidence/phase-03/quiescence-summary.json
@.planning/evidence/phase-01/restore-drill-summary.json
@aura_backend/preservation/cli.py
@aura_backend/preservation/backup.py
@aura_backend/preservation/restore.py
@aura_backend/storage/cli.py
</context>

<dependencies>
Needs: Plan 03-07 current passing ticket and Ty's exact backup authorization.
Creates: Current preservation proof and a write-once, expiring, exact-scope authorization receipt consumed by Plan 03-09.
Wave rationale: No backup or real-data authorization can run before the prior blocking checkpoint.
</dependencies>

<scope_fence>
- This is a non-autonomous preservation and authorization plan. Run only against the exact source-set/ticket approved in Plan 03-07.
- The backup is a byte copy for preservation; Chroma opens only a unique disposable restore descendant, never originals or the durable backup. Retain all failure evidence.
- Do not import historical records, create the production ledger from them, switch reads, repair anomalies, delete/rotate/prune any copy, remove tracked data, or inspect conversation documents/metadata outside the privacy-safe restore fixtures.
- If the ticket is stale/blocked or any copy/restore/privacy/binding check fails, stop. Return to Plan 03-07 for a new preflight and human approval; do not self-authorize.
- Never infer approval from a resume, summary existence, or old receipt. Only Task 03-08-02's exact `approved` result may create Task 03-08-03's receipt; exclusive-create prevents overwrite/replay.
</scope_fence>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-08-01: Copy the approved set, prove restore, and prepare the exact migration proposal</name>
  <files>.planning/evidence/phase-03/restore-drill-summary.json, .planning/evidence/phase-03/migration-plan-summary.json, tests/storage/test_phase3_evidence.py</files>
  <read_first>
    @.planning/phases/03-memory-integrity-and-data-lifecycle/03-07-SUMMARY.md
    @.planning/evidence/phase-03/inventory-summary.json
    @.planning/evidence/phase-03/quiescence-summary.json
    @aura_backend/preservation/cli.py
    @aura_backend/storage/cli.py
    @tests/storage/test_phase3_evidence.py
  </read_first>
  <behavior>
    - Backup revalidates the still-current approved ticket, source-set digest, writers/open handles, disjoint new destination, free space, and every source before copying.
    - Source-before/source-after/destination manifests match exactly; a partial copy or changed source is non-pass and retained without finalizing success.
    - Restore copies the durable backup into one new disposable child, opens Chroma only there, and requires exact source/hash/SQLite/FK/count/opaque-retrieval parity with no vacuous/omitted check.
    - Both historical roots remain separate and the prior eight-row anomaly evidence is compared rather than repaired or described as clean.
    - Migration plan binds the exact fresh restore digest, both historical root aliases/fingerprints, disposable-work parent, new ignored ledger/projection target, no-source-mutation rule, import mapping/count/digest checks, neutral benchmark gates, snapshot/restore gate, rollback rehearsal, and read-owner switch condition.
  </behavior>
  <action>Use an evidence tracer. RED: extend `test_phase3_evidence.py` with absent/incomplete/forged restore and migration-plan cases, exact required checks, private/public digest bindings, Phase 1 hash invariance, and privacy allowlists. GREEN: run `uv run --locked --no-sync python -m aura_backend.preservation.cli backup-from-ticket` with the Phase 3 inventory/quiescence summaries, `/backup/aura-phase-03`, and destination name `backup`; then run the preservation `verify` command with the same evidence/root, restore parent `/backup/aura-phase-03/.restore-work`, and public output `.planning/evidence/phase-03/restore-drill-summary.json`. After exact restore validation passes, run `uv run --locked --no-sync python -m aura_backend.storage.cli prepare-migration-plan` using only the public inventory/restore evidence and explicit `/backup/aura-phase-03/phase-03-storage-gate-01/backup`, `/backup/aura-phase-03/.migration-work`, and repository-resolved `aura_data_v2` targets to write the public migration plan. GREEN requires the exact pytest verifier. Refactor verifier code only while green. Do not run an import, benchmark on real data, snapshot the new ledger, or write a read-owner marker.</action>
  <acceptance_criteria>
    - Outside-Git backup is complete and source before/after/destination parity is exact.
    - All restore checks and bindings pass; Chroma never receives an original/durable path.
    - Migration plan names both restored-source aliases and every gate with no content.
    - Phase 1 evidence, originals, backups, archives, runtime configuration, and read owner remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/storage/test_phase3_evidence.py -k 'backup or restore or migration_plan or phase1'</automated>
  </verify>
  <done>The approved source set is recoverably preserved and an exact, non-executed migration/read-switch proposal exists.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 03-08-02: Authorize disposable-copy data read, import, and gated read switch</name>
  <read_first>
    @.planning/evidence/phase-03/restore-drill-summary.json
    @.planning/evidence/phase-03/migration-plan-summary.json
    @.planning/evidence/phase-01/restore-drill-summary.json
  </read_first>
  <action>Run the exact automated evidence test immediately before pausing. Ty reviews only public-safe restore and migration-plan evidence: both root aliases/fingerprints, source unchanged, backup/restore exact checks, target ledger/projection paths, temporary restored-copy parent, required import/provenance/projection/benchmark/snapshot/rollback gates, and the rule that originals/durable backup remain immutable. Approval authorizes Plan 03-09 to copy the durable backup into a disposable migration workspace, read those copies through public APIs, import them once into the new SQLite ledger, build projections, and switch reads only after every declared gate passes. Approval does not authorize any source/backup/archive deletion, repair, purge, tracked-data removal, or Git-history change.</action>
  <what-built>A current outside-Git backup, exact disposable-restore proof, and content-free plan for importing both historical roots from new disposable copies.</what-built>
  <how-to-verify>1. Confirm restore status and every exact check are pass. 2. Confirm both historical root aliases remain separate and unchanged. 3. Confirm the import source is `/backup/aura-phase-03/.migration-work` disposable children, never originals/durable backup. 4. Confirm every parity, neutral benchmark, snapshot/restore, rollback, and switch gate is mandatory. 5. Approve only this import/read-switch scope.</how-to-verify>
  <acceptance_criteria>
    - Automated restore/migration-plan verifier passes with unchanged Phase 1 evidence.
    - Ty explicitly authorizes data read/import from disposable restored copies and the fail-closed read-switch gate.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/storage/test_phase3_evidence.py -k 'backup or restore or migration_plan or phase1'</automated>
    <human-check>Ty reviews the public evidence and replies `approved` for the exact disposable-copy import and gated read switch.</human-check>
  </verify>
  <resume-signal>Type `approved` to authorize only Plan 03-09's exact disposable-copy import/read-switch flow; otherwise describe the evidence or scope concern.</resume-signal>
  <done>The exact human approval result is available to the next task; it is not yet a reusable authorization capability.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-08-03: Record the immutable exact-scope authorization receipt</name>
  <files>.planning/evidence/phase-03/migration-authorization-receipt.json, tests/storage/test_phase3_evidence.py</files>
  <read_first>
    @.planning/evidence/phase-03/restore-drill-summary.json
    @.planning/evidence/phase-03/migration-plan-summary.json
    @aura_backend/storage/cli.py
    @tests/storage/test_phase3_evidence.py
  </read_first>
  <behavior>
    - Receipt creation requires Task 03-08-02's exact `approved` result; denied, absent, ambiguous, or wrong-result checkpoint data produces no receipt.
    - Private mode-0600 authorization summary binds the checkpoint result, exact approved absolute disposable/durable/target paths, source aliases/fingerprints, restore and migration-plan digests, fixed operation scope, issued-at UTC, and expiry no more than 24 hours later.
    - Public receipt contains authorization ID, exact public-safe aliases/HMAC fingerprints, restore/migration-plan digests, scope `disposable_copy_import_and_fail_closed_read_switch`, issued/expiry UTC, `approval_result: approved`, private `authorization_summary_sha256`, and its canonical receipt digest—never absolute source paths, content, raw metadata, secrets, or checkpoint transcript.
    - Private summary and public receipt use exclusive creation and reject overwrite, widened scope, reordered/substituted sources, stale timestamps, replayed authorization ID, digest drift, or self-reported approval.
  </behavior>
  <action>Use RED, complete GREEN, then conditional REFACTOR. RED: extend `test_phase3_evidence.py` with missing, non-approved, stale, expired, tampered, widened, source-substituted, replayed, wrong-result, digest-mismatched, non-exclusive, and public-path/content leakage authorization cases. GREEN: after and only after Task 03-08-02 returned exact `approved`, invoke `uv run --locked --no-sync python -m aura_backend.storage.cli record-authorization` with the restore summary, migration plan, exact checkpoint result, fixed scope, private outside-Git summary path, public receipt path, and maximum 24-hour expiry. Require exclusive creation, canonical JSON/digests, mode-0600 private summary, public schema allowlist, and the exact pytest verifier. REFACTOR only while green. If either receipt path already exists, the checkpoint result is not exactly approved, or any binding differs, stop without overwriting or emitting authority.</action>
  <acceptance_criteria>
    - Public receipt binds exact aliases/fingerprints, restore and migration-plan digests, scope, issued/expiry times, approved result, authorization summary digest, and canonical receipt digest.
    - Private summary is mode-0600 outside Git; public receipt contains no sensitive path, content, raw metadata, secret, or transcript.
    - Missing/stale/tampered/widened/replayed/wrong-result receipts fail adversarial validation.
    - Originals, durable backup, restored copies, read owner, and Phase 1 evidence remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/storage/test_phase3_evidence.py -k 'authorization_receipt or authorization_summary'</automated>
  </verify>
  <done>A write-once, current, content-free receipt grants only the exact disposable-copy import and fail-closed read-switch scope to Plan 03-09.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Quiesced real roots → immutable outside-Git backup | Personal data must be copied completely without source mutation. |
| Durable backup → disposable restore | Stateful Chroma checks are permitted only on a disposable child. |
| Public restore evidence → migration authorization | Incomplete/forged evidence must not license real-data reads or a switch. |
| Human approval → private summary → public receipt | Approval must become a narrow, expiring, tamper-evident capability without exposing sensitive paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-08-01 | Tampering / Denial of Service | backup copy | mitigate | Fresh ticket, disjoint new destination, source before/after/destination hashes, no prune/delete. |
| T-03-08-02 | Information Disclosure | restore/evidence | mitigate | Disposable-only Chroma, HMACed opaque fixtures, public allowlist/private mode-0600 artifacts. |
| T-03-08-03 | Spoofing / Repudiation | restore/migration plan | mitigate | Exact check set and inventory/ticket/backup/private/source fingerprints; adversarial verifier. |
| T-03-08-04 | Elevation / Tampering | import authorization | mitigate | Blocking approval plus exclusive-create receipt bound to exact sources, scope, result, digests, and expiry. |
| T-03-08-05 | Repudiation / Information Disclosure | authorization summary/receipt | mitigate | Private mode-0600 summary, public allowlist, canonical digests, replay/staleness/widening adversarial tests. |
| T-03-08-SC | Tampering | package supply chain | mitigate | Existing locked preservation/storage CLIs only; no install/sync/dependency mutation. |
</threat_model>

<verification>
The plan completes only after backup/restore evidence passes, Ty approves the named scope, and the immutable authorization receipt passes exact adversarial verification. Any stale/failed/incomplete/widened/replayed evidence stops without import or switch.
</verification>

<source_coverage>
## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Coverage | Status |
|---|---|---|---|---|
| GOAL | — | Recoverable data transition | Current backup/restore and immutable migration authorization | COVERED |
| REQ | DATA-01 | One owner migration boundary | Exact two-root import proposal | COVERED |
| REQ | DATA-02 | Consistent outside-Git backup/restore | Task 01 | COVERED |
| REQ | PRES-04 | Private artifacts outside Git | Public/private evidence split | COVERED |
| REQ | TEST-03 | Failure/evidence tests | Restore plus authorization receipt adversarial verifier | COVERED |
| CONTEXT | D-04 | Originals/archives immutable | All three tasks | COVERED |
| CONTEXT | D-05 | Isolated restore proof | Task 01 | COVERED |
| RESEARCH | Human gate before real import/read switch | Task 02 approval and Task 03 receipt | COVERED |

No source deletion, repair, retention purge, tracked-data cleanup, graph/RLM, affect, frontend, auth, or Git-history work is authorized.
</source_coverage>

<success_criteria>
- Fresh backup and isolated restore evidence pass exact integrity/FK/count/retrieval/privacy bindings.
- Ty explicitly authorizes only the disposable-copy import and gated read switch, recorded in a write-once current receipt.
- Originals, durable backup, archives, Phase 1 evidence, read owner, and source data remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/03-memory-integrity-and-data-lifecycle/03-08-SUMMARY.md` when done.
</output>
