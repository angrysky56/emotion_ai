---
phase: 03-memory-integrity-and-data-lifecycle
plan: 04
type: execute
wave: 3
depends_on: [03-02, 03-03]
files_modified:
  - aura_backend/storage/models.py
  - aura_backend/storage/retrieval.py
  - aura_backend/storage/benchmark.py
  - tests/storage/test_hybrid_retrieval.py
  - tests/storage/test_memory_benchmark.py
  - tests/memory/test_prompt_memory_boundary.py
autonomous: true
requirements: [TEST-03, DATA-05, DATA-06, DATA-09]
must_haves:
  truths:
    - "Affect-neutral FTS5 and cosine-vector candidates are bounded, fused through fixed normalized RRF, and sorted deterministically."
    - "Scope, origin existence, provenance, supersession/retraction, projection generation/hash, and neutral relevance are hard gates that no caller filter or later scorer can bypass."
    - "Every considered candidate has a content-free stable trace explaining admission/rejection, channel ranks/raw values, neutral score, contributors, and selected rank."
    - "History and retrieval pages are bounded and stable across a frozen run; forged, expired, wrong-scope/query/config/generation cursors fail closed."
    - "The Phase 4 salience seam returns exactly 0.0 for every candidate and cannot change admission or order."
  artifacts:
    - path: "aura_backend/storage/retrieval.py"
      provides: "Neutral hybrid admission, normalized RRF, trace, deduplication, and cursor service"
    - path: "tests/storage/test_hybrid_retrieval.py"
      provides: "Scope/provenance/staleness/injection/ranking/pagination adversarial proof"
    - path: "tests/storage/test_memory_benchmark.py"
      provides: "Connected neutral-arm competence/containment benchmark"
  key_links:
    - from: "aura_backend/storage/retrieval.py"
      to: "aura_backend/storage/repository.py"
      via: "FTS candidates plus authoritative origin/provenance/supersession revalidation"
      pattern: "query_fts|validate_origin"
    - from: "aura_backend/storage/retrieval.py"
      to: "aura_backend/storage/projection.py"
      via: "bounded cosine candidates carrying generation and content hashes"
      pattern: "query_candidates|projection_generation"
    - from: "aura_backend/storage/retrieval.py"
      to: "aura_backend/storage/benchmark.py"
      via: "public neutral retriever protocol measured by the frozen corpus"
      pattern: "RetrievalPage|BenchmarkRunner"
---

<objective>
Deliver deterministic, bounded, affect-neutral hybrid retrieval with hard evidence gates and auditable pages.

Purpose: Make recalled data useful without allowing mood, stale projections, caller filters, or retrieved instructions to manufacture authority or evidence.
Output: Hybrid retriever, traces/cursors, exact zero-salience seam, prompt-boundary preservation, and connected benchmark evidence.
</objective>

## Artifacts this plan produces

- Fixed 50 lexical + 50 vector candidate caps, normalized RRF with `k=60`, and deterministic tie-breaking.
- Hard eligibility reasons and traces for accepted/rejected/deduplicated candidates.
- Stored-run cursors with default 20, hard maximum 100, expiry and scope/query/config/generation binding.
- A neutral benchmark run whose instrument controls must pass before competence numbers count.

<execution_context>
@/home/ty/.Codex/gsd-core/workflows/execute-plan.md
@/home/ty/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-memory-integrity-and-data-lifecycle/03-CONTEXT.md
@.planning/phases/03-memory-integrity-and-data-lifecycle/03-RESEARCH.md
@.planning/research/affective-memory/AFFECT-MEMORY-LOOP.md
@.planning/research/affective-memory/KILL-CRITERIA.md
@aura_backend/storage/models.py
@aura_backend/storage/repository.py
@aura_backend/storage/projection.py
@aura_backend/storage/benchmark.py
@aura_backend/main.py
@tests/memory/test_prompt_memory_boundary.py
</context>

<dependencies>
Needs: Plan 03-02's faithful instrument and Plan 03-03's bounded cosine projection.
Creates: Neutral retrieval/history contracts consumed by runtime/API wiring and final migration/read-switch gates.
Wave rationale: Runs beside lifecycle work with no overlapping files.
</dependencies>

<scope_fence>
- Implement D-03 exactly. Affect is absent from candidate discovery/admission/rank; the only Phase 4 seam is an injected scorer whose shipped implementation returns `0.0` exactly.
- Retrieved text remains untrusted historical data inside the existing delimiters and never becomes a system message, tool authority, state update, or new evidence merely because it was selected.
- Do not add graph traversal, RLM, Memvid primary retrieval, dynamic affect, relationship update, frontend redesign, auth, or dependency changes.
- Use synthetic temporary SQLite/Chroma only. Do not open real historical roots or run a benchmark on real conversations.
</scope_fence>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-04-01: Enforce neutral candidate gates and normalized RRF</name>
  <files>aura_backend/storage/models.py, aura_backend/storage/retrieval.py, tests/storage/test_hybrid_retrieval.py</files>
  <read_first>
    @aura_backend/storage/models.py
    @aura_backend/storage/repository.py
    @aura_backend/storage/projection.py
    @.planning/phases/03-memory-integrity-and-data-lifecycle/03-RESEARCH.md
  </read_first>
  <behavior>
    - The service requests at most 50 FTS and 50 vector candidates using configuration-owned caps; client input cannot raise them.
    - Every vector result is re-resolved in SQLite and rejected for missing origin, wrong scope, absent provenance, supersession/retraction, stale generation/hash, or failed neutral relevance admission.
    - Caller metadata/filter input is never merged over required scope; identical content in another scope cannot appear even when Chroma returns it adversarially.
    - Lexical and vector ranks use the fixed normalized reciprocal-rank formula with `k=60`; raw BM25 and cosine distance are trace facts, never directly added.
    - Eligible duplicates collapse by canonical origin/content hash while every lexical/vector contributor remains in the trace.
    - Ordering is neutral score descending, exact match descending, observed time descending, origin ID ascending; a timestamp cannot override stronger relevance.
  </behavior>
  <action>Run RED, smallest complete GREEN, then optional REFACTOR. RED: build two-scope synthetic ledger/projection cases plus forged scope filters, orphan provenance, superseded memories, stale hashes/generations, raw-score incompatibility, duplicates, and deterministic ties. Assert exact rejection codes and the normalized RRF arithmetic. GREEN: implement affect-blind candidate materialization, authoritative SQLite revalidation, deduplication, fixed fusion, and deterministic ordering. REFACTOR only with the focused suite green. Do not expose an extension point that can modify eligibility; the later salience seam receives only the already eligible ordered set.</action>
  <acceptance_criteria>
    - All hard gates reject the intended candidates with stable content-free reasons.
    - Scope cannot be overwritten by any caller filter or projection metadata.
    - Neutral scores are bounded `[0,1]`, deterministic, and independent of page-local min/max or affect.
    - Deduplication preserves trace contributors and loses no provenance.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/storage/test_hybrid_retrieval.py -k 'gate or scope or rrf or duplicate or order'</automated>
  </verify>
  <done>The eligible set is affect-neutral, scope/provenance safe, and deterministically fused.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-04-02: Add stable traces/cursors and prove the neutral benchmark</name>
  <files>aura_backend/storage/retrieval.py, aura_backend/storage/benchmark.py, tests/storage/test_hybrid_retrieval.py, tests/storage/test_memory_benchmark.py, tests/memory/test_prompt_memory_boundary.py</files>
  <read_first>
    @aura_backend/storage/retrieval.py
    @aura_backend/storage/benchmark.py
    @tests/fixtures/memory_eval/manifest.json
    @tests/memory/test_prompt_memory_boundary.py
    @aura_backend/main.py
  </read_first>
  <behavior>
    - Every considered candidate records run/config/query hashes, SQLite schema, projection generation, origin/type, raw/rank channel facts, normalized components, each gate result/reason, neutral score, contributor IDs, and selected rank without remembered text.
    - Retrieval returns `items`, `next_cursor`, `has_more`, and trace/run ID; default page size is 20 and maximum is 100.
    - Cursor tokens are opaque and integrity-bound to scope, query hash, config, generation, frozen run, expiry, and last stable sort tuple; forgery/replay across any binding is rejected.
    - Ledger history uses `(observed_at,event_id)` keyset pagination; retrieval pages use the stored frozen run and have no gaps/duplicates when the live projection changes.
    - Stored prompt injection remains inside `<untrusted_memory_context>` and cannot alter system/tool authority or create an event/derivation.
    - `ZeroSalienceScorer` returns exactly `0.0` for every candidate; trace salience is zero and final order equals neutral order.
    - The connected frozen benchmark must pass every instrument control before competence gates can be reported; failures remain explicit rather than tuned away.
  </behavior>
  <action>Use the full tracer cycle. RED: add cursor tampering/cross-scope/query/config/generation/expiry tests, mutation-between-pages tests, content-free trace sentinels, stored-instruction boundary tests, and a fake attempted non-zero salience injection that cannot alter the shipped Phase 3 order. Connect the frozen benchmark to the public retriever and first prove its positive/negative controls. GREEN: implement stored retrieval runs/candidates, integrity-bound cursors, bounded retention metadata, keyset history, complete trace construction, and the exact-zero scorer. Make only the implementation changes needed by the fixed benchmark; do not change the corpus, thresholds, caps, RRF k, expected origins, or controls to obtain green. REFACTOR only while retrieval, prompt-boundary, and instrument suites remain green.</action>
  <acceptance_criteria>
    - Stable pages have no gaps/duplicates and forged/mismatched/expired cursors fail closed.
    - Trace rows explain both selected and rejected candidates without query/content leakage.
    - Prompt delimiters and instruction warning remain exact compatibility contracts.
    - Phase 4 salience is exactly zero and cannot change eligibility/order.
    - Benchmark result is accepted only if all controls are faithful; any missed threshold is reported as a failed baseline gate.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/storage/test_hybrid_retrieval.py tests/storage/test_memory_benchmark.py tests/memory/test_prompt_memory_boundary.py</automated>
  </verify>
  <done>Neutral retrieval is bounded, traceable, cursor-stable, prompt-safe, and honestly measured with the salience seam fixed at zero.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Query/caller filters → candidate services | Caller data must not overwrite mandatory scope/bounds. |
| Chroma/FTS candidates → eligible memory | Stale, orphaned, superseded, irrelevant, or cross-scope candidates are untrusted. |
| Stored historical text → provider prompt | Recalled instructions must remain data, never authority. |
| Cursor → stored retrieval run | Client-controlled tokens must not reveal or access another run/scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-04-01 | Information Disclosure / Elevation | scope admission | mitigate | Mandatory conjunct, SQLite origin revalidation, two-scope collision/override tests. |
| T-03-04-02 | Tampering | projection/provenance gates | mitigate | Generation/hash/origin/provenance/supersession checks with stable rejection codes. |
| T-03-04-03 | Spoofing / Elevation | stored prompt injection | mitigate | Existing delimiters, no system/tool/evidence promotion, adversarial corpus/tests. |
| T-03-04-04 | Tampering / Disclosure | cursor | mitigate | Integrity-bound opaque token, expiry and exact scope/query/config/generation/run binding. |
| T-03-04-05 | Repudiation | trace/rank | mitigate | Persist every considered candidate's raw ranks/gates/neutral score without text. |
| T-03-04-SC | Tampering | package supply chain | mitigate | Existing SQLite/Chroma stack only; no dependency, install, sync, manifest, or lock mutation. |
</threat_model>

<verification>
- Capture intended RED then exact GREEN commands for both tasks.
- Run `uv run --locked --no-sync python -m pytest -q tests/storage/test_hybrid_retrieval.py tests/storage/test_memory_benchmark.py tests/memory/test_prompt_memory_boundary.py tests/storage/test_projection_rebuild.py tests/storage/test_provenance_supersession.py`.
- At the wave boundary run `uv run --locked --no-sync python -m pytest tests -q -m 'not live'`.
</verification>

<source_coverage>
## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Coverage | Status |
|---|---|---|---|---|
| GOAL | — | Truthful bounded conversation memory | Neutral hybrid retriever and stable pages | COVERED |
| REQ | DATA-05 | Pagination/relevance/latency/duplicates | Bounds, cursor, benchmark, dedup trace | COVERED |
| REQ | DATA-06 | Provenance/supersession | Hard admission gates | COVERED |
| REQ | DATA-09 | Affect-neutral eligible set and trace | Both tasks; zero salience seam | COVERED |
| REQ | TEST-03 | Automated failure behavior | Scope, stale, injection, cursor adversarial tests | COVERED |
| CONTEXT | D-03 | FTS+Chroma, gates, RRF, trace, zero seam | Both tasks | COVERED |
| RESEARCH | Candidate caps, k=60, stable sort/pages | Both tasks | COVERED |

Dynamic affect and relationship state remain Phase 4; graph/RLM/Memvid primary retrieval and frontend work remain excluded and are not gaps.
</source_coverage>

<success_criteria>
- Hybrid retrieval, prompt-boundary, and benchmark suites pass with exact fixed controls.
- Every eligible item is scope/provenance/current/hash/relevance valid and every rejected item is traceable.
- Full deterministic regression stays green without any real-store access.
</success_criteria>

<output>
Create `.planning/phases/03-memory-integrity-and-data-lifecycle/03-04-SUMMARY.md` when done.
</output>
