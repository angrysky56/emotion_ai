---
phase: 03-memory-integrity-and-data-lifecycle
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - aura_backend/storage/projection.py
  - aura_backend/storage/migration.py
  - tests/storage/test_projection_rebuild.py
  - tests/storage/test_legacy_migration.py
  - tests/storage/test_storage_owner.py
autonomous: true
requirements: [TEST-03, DATA-01, DATA-06]
must_haves:
  truths:
    - "Chroma contains only stable-ID cosine projections of committed SQLite origins and can be discarded/rebuilt without losing truth."
    - "Projection failure after commit leaves one durable turn plus traceable reconciliation work, never a second authority or duplicated turn."
    - "Historical import code can read only explicitly authorized disposable restored copies, preserves both legacy roots/origins/duplicates/fragments/anomalies, and never mutates or repairs a source."
    - "No active code reaches Chroma's internal SQLite schema for repair, migration, backup, or truth decisions."
  artifacts:
    - path: "aura_backend/storage/projection.py"
      provides: "Stable cosine Chroma projection, reconciliation, and generation rebuild"
    - path: "aura_backend/storage/migration.py"
      provides: "Bounded idempotent legacy reader/importer requiring disposable restore evidence"
    - path: "tests/storage/test_storage_owner.py"
      provides: "Single-authority and no-third-party-internals architecture gate"
  key_links:
    - from: "aura_backend/storage/projection.py"
      to: "aura_backend/storage/repository.py"
      via: "SQLite origins, hashes, watermarks, and projection generation records"
      pattern: "origin_id|content_sha256|generation"
    - from: "aura_backend/storage/migration.py"
      to: "aura_backend/storage/repository.py"
      via: "legacy source triple and typed fragment/event import"
      pattern: "root_fingerprint|collection_name|legacy_id"
---

<objective>
Turn Chroma into a disposable, verifiable projection and provide a source-safe historical import engine.

Purpose: Preserve semantic compatibility without dual truth, direct third-party database repair, or destructive/guess-based migration.
Output: Projection adapter, reconciliation/rebuild flow, authorized-copy migration engine, and architecture/failure tests.
</objective>

## Artifacts this plan produces

- An ordinary injected Chroma adapter with explicit cosine configuration and stable event/memory IDs.
- Generation/config/model/hash metadata, stale-result rejection inputs, post-commit reconciliation, and fresh-directory rebuild parity.
- An idempotent legacy import engine that accepts only disposable restored copies and records exact origin provenance.
- Synthetic public-API Chroma tests; no real historical store is opened in this plan.

<execution_context>
@/home/ty/.Codex/gsd-core/workflows/execute-plan.md
@/home/ty/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-memory-integrity-and-data-lifecycle/03-CONTEXT.md
@.planning/phases/03-memory-integrity-and-data-lifecycle/03-RESEARCH.md
@.planning/phases/01-preservation-and-trusted-baseline/01-VERIFICATION.md
@.planning/phases/01-preservation-and-trusted-baseline/01-10-SUMMARY.md
@aura_backend/storage/models.py
@aura_backend/storage/repository.py
@aura_backend/robust_vector_db.py
@aura_backend/shared_embedding_service.py
</context>

<dependencies>
Needs: Plan 03-01 stable SQLite origin, provenance, source-triple, and generation contracts.
Creates: Vector candidates and safe historical-copy import used by neutral retrieval and the final authorized migration.
Wave rationale: Can run beside the benchmark plan because it owns only projection/migration modules and their tests.
</dependencies>

<scope_fence>
- Implement D-03's disposable Chroma role and D-04's immutable-history boundary. Never open either real `aura_chroma_db` root, the Phase 1 durable backup, or any `.mv2` archive in this ordinary implementation plan.
- Migration tests use synthetic stores copied into temporary disposable restore descendants. Source before/after hashes must match.
- Preserve both historical root identities; never pick a canonical root by size/mtime, fabricate pairs, merge equal content, repair eight-row FK anomalies, or run PRAGMA/REINDEX against Chroma internals.
- Retain `all-MiniLM-L6-v2`, fingerprint its config, and add no dependency or lock change. Memvid remains copy-only and outside the primary read/write path.
</scope_fence>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-03-01: Make cosine Chroma a rebuildable projection</name>
  <files>aura_backend/storage/projection.py, tests/storage/test_projection_rebuild.py, tests/storage/test_storage_owner.py</files>
  <read_first>
    @aura_backend/storage/repository.py
    @aura_backend/robust_vector_db.py
    @aura_backend/shared_embedding_service.py
    @.planning/phases/03-memory-integrity-and-data-lifecycle/03-RESEARCH.md
  </read_first>
  <behavior>
    - Adapter construction receives explicit directories/repository/embedding service and performs no singleton or import-time I/O.
    - Collections explicitly freeze cosine distance under locked Chroma 1.5.9; raw distance and clamped cosine similarity remain distinct.
    - Stable IDs are `event:{event_id}` and `memory:{memory_id}` with scope, origin kind/ID, content hash, SQLite schema, generation, and embedding model/config hash metadata.
    - Only committed source events and active provenance-bearing derived memories can project; orphan/stale/superseded origins are rejected.
    - Failure after commit/before projection, during any upsert, or before retry converges through reconciliation to exactly one projection per origin.
    - Rebuild creates a fresh generation/directory, pages SQLite deterministically, verifies IDs/counts/hashes/config/fixtures, and switches only after parity; a failed generation never becomes current.
  </behavior>
  <action>Apply RED, complete GREEN, and conditional REFACTOR. RED: specify locked-version cosine construction, stable metadata/IDs, post-commit fault points, tampered hash/generation rejection, idempotent reconciliation, and discard/rebuild parity with deterministic fake embeddings and temporary Chroma roots. GREEN: implement `upsert_committed`, `delete_origins`, `query_candidates`, `reconcile`, and `rebuild` using only documented public Chroma APIs and the existing embedding service seam. Store generation truth/watermarks in SQLite, never Chroma. REFACTOR only with both tests green. Architecture tests must reject direct writes that bypass the repository, Chroma-as-truth fallbacks, singleton path capture, and imports of third-party internal SQLite repair logic.</action>
  <acceptance_criteria>
    - Explicit cosine behavior is verified against the locked installed API.
    - Every failure/retry path converges to one stable projection per active SQLite origin.
    - Full discard/rebuild reaches exact origin/count/hash/config fixture parity before generation switch.
    - Projection loss or corruption cannot lose or alter SQLite truth.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/storage/test_projection_rebuild.py tests/storage/test_storage_owner.py</automated>
  </verify>
  <done>Chroma is a cosine, stable-ID, fully disposable projection with deterministic reconciliation and rebuild proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-03-02: Make historical import idempotent and source-immutable</name>
  <files>aura_backend/storage/migration.py, tests/storage/test_legacy_migration.py, tests/storage/test_storage_owner.py</files>
  <read_first>
    @.planning/evidence/phase-01/inventory-summary.json
    @.planning/evidence/phase-01/restore-drill-summary.json
    @aura_backend/preservation/restore.py
    @aura_backend/storage/repository.py
    @aura_backend/robust_vector_db.py
  </read_first>
  <behavior>
    - Import requires an allowlisted source alias/fingerprint plus evidence that the path is a disposable descendant of an authorized restore workspace; original and durable-backup paths are rejected.
    - Both active-root aliases are imported independently in bounded deterministic pages through public Chroma collection APIs.
    - `(root_fingerprint, collection_name, legacy_id)` is unique and reruns produce no duplicate ledger rows.
    - Identical content from distinct origins remains separate evidence; retrieval may later collapse display duplicates while retaining contributors.
    - Missing pairs/timestamps become typed legacy fragments with canonical raw metadata and uncertainty; no pair, fact, relationship, or timestamp is guessed.
    - Known FK anomalies and unmapped fields are recorded as content-free mapping/anomaly evidence; source trees are byte-identical before/after.
  </behavior>
  <action>Use the tracer sequence. RED: create two synthetic divergent Chroma roots plus duplicates, unmatched fragments, malformed metadata, reruns, path-forgery attempts, and source-mutation spies; copy them into authorized temporary restore descendants before invoking the importer. GREEN: implement bounded public-API discovery and repository insertion keyed by the exact legacy source triple, preserving canonical raw metadata, root/collection/legacy IDs, and typed fragment status. REFACTOR only while the import and storage-owner suites stay green. The implementation must require an authorization/evidence object supplied by the later checkpoint flow; it must have no convenience path that accepts a real root directly.</action>
  <acceptance_criteria>
    - Both synthetic roots import with exact independent origin mappings and rerun safety.
    - Duplicates/fragments/anomalies remain explicit and no source byte changes.
    - Original-root, durable-backup, outside-workspace, symlink, and missing-evidence paths fail before Chroma opens.
    - No import executes in this plan against real Aura data.
  </acceptance_criteria>
  <verify>
    <automated>uv run --locked --no-sync python -m pytest -q tests/storage/test_legacy_migration.py tests/storage/test_storage_owner.py</automated>
  </verify>
  <done>The import engine is bounded, idempotent, provenance-preserving, and incapable of opening an unauthorized original source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| SQLite truth → Chroma projection | Derived vector state must not become authoritative or stale silently. |
| Restore authorization → legacy importer | Personal historical data may be read only from an approved disposable copy. |
| Legacy metadata → typed ledger | Malformed/ambiguous values must not fabricate evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-03-01 | Tampering | projection generation | mitigate | Stable IDs/hashes/config/watermark, fresh generation, exact parity before atomic switch. |
| T-03-03-02 | Spoofing / Elevation | import path/evidence | mitigate | Restore-descendant containment, alias/fingerprint binding, symlink rejection, original/durable path deny tests. |
| T-03-03-03 | Tampering / Repudiation | legacy mapping | mitigate | Exact source triple, canonical raw metadata, idempotent reruns, no inferred pair/repair. |
| T-03-03-04 | Information Disclosure | migration evidence | mitigate | IDs/counts/hashes/reason codes only; no documents/metadata in public reports. |
| T-03-03-SC | Tampering | package supply chain | mitigate | Existing locked Chroma/embedding packages only; no dependency authority change. |
</threat_model>

<verification>
- Capture RED then GREEN for each task and run both exact task commands.
- Run `uv run --locked --no-sync python -m pytest -q tests/storage/test_projection_rebuild.py tests/storage/test_legacy_migration.py tests/storage/test_storage_owner.py tests/storage/test_atomic_ledger.py tests/storage/test_provenance_supersession.py`.
- At the wave boundary run `uv run --locked --no-sync python -m pytest tests -q -m 'not live'` and confirm no real data-root timestamp/hash changed.
</verification>

<source_coverage>
## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Coverage | Status |
|---|---|---|---|---|
| GOAL | — | Consolidate persistence behind one truth owner | Projection/import consume SQLite repository | COVERED |
| REQ | DATA-01 | One owner; indexes rebuildable | Chroma adapter and storage-owner test | COVERED |
| REQ | DATA-06 | Historical provenance and fragments | Exact legacy source triple and typed ambiguity | COVERED |
| REQ | TEST-03 | Failure/migration behavior tests | Reconcile, corrupt, rebuild, path, rerun tests | COVERED |
| CONTEXT | D-03 | Existing Chroma as disposable projection | Task 01 | COVERED |
| CONTEXT | D-04 | Immutable historical roots; no repair | Task 02 | COVERED |
| RESEARCH | Both roots, cosine, stable IDs, public API only | Both tasks | COVERED |

Actual real-data import remains gated to Plan 03-09; graph/RLM, Memvid adoption, old-root repair/deletion, and embedding replacement are excluded and are not gaps.
</source_coverage>

<success_criteria>
- Projection and import focused suites pass entirely on disposable synthetic stores.
- No real Aura Chroma/Memvid/backup root is opened or changed.
- The full deterministic suite remains green with no new package or authority.
</success_criteria>

<output>
Create `.planning/phases/03-memory-integrity-and-data-lifecycle/03-03-SUMMARY.md` when done.
</output>
