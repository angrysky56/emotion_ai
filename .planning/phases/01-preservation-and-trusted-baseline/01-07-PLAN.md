---
phase: 01-preservation-and-trusted-baseline
plan: 07
type: tdd
wave: 2
depends_on:
  - 01-05
files_modified:
  - tests/support/main_subprocess_probe.py
  - tests/characterization/test_persistence_contract.py
  - tests/characterization/test_companion_contract.py
autonomous: true
requirements:
  - PRES-03
must_haves:
  truths:
    - "A deterministic fake-provider conversation captures the current user-visible response schema without Ollama."
    - "The route passes the expected user/Aura exchange and session shape to persistence, and persistence failure behavior is explicit."
    - "Volatile timestamps, UUIDs, model prose, and hidden reasoning are normalized or excluded rather than frozen as false contracts."
  artifacts:
    - path: "tests/characterization/test_persistence_contract.py"
      provides: "Persistence call-shape and failure characterization"
    - path: "tests/characterization/test_companion_contract.py"
      provides: "Stable user-visible conversation response characterization"
  key_links:
    - from: "tests/characterization/test_companion_contract.py"
      to: "tests/support/main_subprocess_probe.py"
      via: "fake-provider conversation scenarios"
      pattern: "conversation"
    - from: "tests/support/main_subprocess_probe.py"
      to: "aura_backend/main.py"
      via: "real `/conversation` route with fake globals/functions"
      pattern: "process_conversation"
---

<objective>
Capture representative companion and persistence behavior before structural change, implementing D-04 and D-05 with deterministic fake collaborators.

Purpose: Preserve the user-visible and storage-call contracts that later refactoring must keep, while refusing to freeze volatile or unverifiable internals.
Output: Request-level companion and persistence characterization tests.
</objective>

## Artifacts this phase produces

- `tests/characterization/test_companion_contract.py` — stable response schema, visible usefulness, and provider-error behavior.
- `tests/characterization/test_persistence_contract.py` — exchange/session/write-call and persistence-degradation behavior.
- Extended `tests/support/main_subprocess_probe.py` scenarios using only deterministic fakes.

<execution_context>
@/home/ty/.Codex/gsd-core/workflows/execute-plan.md
@/home/ty/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-preservation-and-trusted-baseline/01-CONTEXT.md
@.planning/phases/01-preservation-and-trusted-baseline/01-RESEARCH.md
@.planning/phases/01-preservation-and-trusted-baseline/01-05-SUMMARY.md
@aura_backend/main.py
@aura_backend/providers/base.py
@aura_backend/conversation_persistence_service.py
@tests/support/main_subprocess_probe.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Characterize the persistence exchange and failure boundary</name>
  <files>tests/support/main_subprocess_probe.py, tests/characterization/test_persistence_contract.py</files>
  <read_first>
    @aura_backend/main.py
    @aura_backend/conversation_persistence_service.py
    @tests/support/main_subprocess_probe.py
  </read_first>
  <behavior>
    - One valid conversation produces one exchange containing user and Aura memories with the same normalized session ID and expected sender/user fields.
    - Immediate persistence receives `update_profile=True` and the configured timeout; its structured success result is observed.
    - A deterministic persistence failure follows the current user-visible route behavior and is recorded distinctly, without writing a fallback file or touching real storage.
  </behavior>
  <action>Follow RED→GREEN→REFACTOR: run the absent focused path for RED, then extend the child probe with a recording fake persistence service and patch the emotion/cognition functions to deterministic typed values. Send a real `/conversation` request through the app, return only sanitized call-shape/status evidence, and assert the production route's present success and failure behavior. Use `IMMEDIATE_PERSISTENCE_ENABLED=true` and zero emergency retries in the child to keep the contract bounded. Do not instantiate `ConversationPersistenceService`, Chroma, embeddings, filesystem fallbacks, or provider services.</action>
  <acceptance_criteria>
    - Assertions cover exchange senders, user/session linkage, method name, update-profile flag, and persistence result status.
    - Failure is reported as the observed contract, never swallowed into an invented pass.
    - No production data directory mtime/hash changes during the focused command.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m pytest -q tests/characterization/test_persistence_contract.py</automated>
  </verify>
  <done>The route-to-persistence contract is pinned with synthetic inputs and no storage side effects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Characterize the user-visible companion response and provider failure</name>
  <files>tests/support/main_subprocess_probe.py, tests/characterization/test_companion_contract.py</files>
  <read_first>
    @aura_backend/main.py
    @aura_backend/providers/base.py
    @tests/support/main_subprocess_probe.py
  </read_first>
  <behavior>
    - A fake provider returns a concise visible answer and the real route emits the stable ConversationResponse keys/types plus normalized emotion/cognition/session fields.
    - Provider messages contain the user's message and the system instruction is nonempty, while exact prose is not snapshotted.
    - Provider error and empty-content cases produce the current explicit HTTP failure behavior.
    - No raw hidden reasoning/thought text, timestamp, generated UUID, or exact model prose becomes a golden fixture.
  </behavior>
  <action>Start from RED on the absent focused path, add fake-provider success/error/empty scenarios to the child probe, and assert stable schema/types and visible response usefulness through the real route. Normalize UUID/timestamp fields and record only booleans/counts for provider inputs. Set raw response/thinking to absent synthetic state so no hidden reasoning contract is introduced. Per D-04, no Ollama process/model call is permitted; per D-05, do not refactor route orchestration while capturing it.</action>
  <acceptance_criteria>
    - Success asserts stable keys/types and a nonempty visible answer without exact-model-output snapshots.
    - Error/empty cases assert non-2xx statuses and safe structured error meaning.
    - Focused tests pass with Ollama unavailable and network disabled.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m pytest -q tests/characterization/test_companion_contract.py</automated>
  </verify>
  <done>The representative companion contract is preserved without model dependence or hidden-reasoning capture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fake provider/persistence → production route | Test doubles must expose route contracts without replacing route logic. |
| Production route → committed fixture | Sensitive/volatile outputs must be excluded or normalized. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-FT | Repudiation | characterization outcomes | mitigate | Assert success and failure paths; child error/timeout is non-pass. |
| T-01-CL | Information Disclosure | probe output and fixtures | mitigate | Synthetic content only; omit hidden reasoning, raw errors, IDs, and timestamps. |
| T-01-SM | Tampering | persistence characterization | mitigate | Recording fake only, temp cwd, and unchanged production-root checks. |
| T-01-LH | Spoofing / Tampering | no-sign-in route | mitigate | Inherit explicit-origin/strict-JSON/loopback controls from Plan 01-05; no auth added. |
| T-01-SC | Tampering | package supply chain | mitigate | Existing dependencies and lock only. |
</threat_model>

<verification>
Run both focused characterization files, `tests/api/test_local_boundary.py`, the full deterministic suite, and `git diff --check`. The command must make no Ollama, network, MCP, GPU, Chroma, or production-filesystem call.
</verification>

<success_criteria>
- PRES-03 covers representative companion response and persistence behavior before refactoring.
- D-04 is enforced by deterministic fakes and no live model requirement.
- D-05 is enforced by characterization-only changes to production behavior.
- Test evidence distinguishes route failure from infrastructure/test failure.
</success_criteria>

<output>
Create `.planning/phases/01-preservation-and-trusted-baseline/01-07-SUMMARY.md` when done.
</output>
