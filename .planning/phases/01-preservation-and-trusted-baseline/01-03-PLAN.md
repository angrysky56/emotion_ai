---
phase: 01-preservation-and-trusted-baseline
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .gitignore
  - .planning/evidence/phase-01/tracked-runtime-baseline.json
  - .planning/evidence/phase-01/legacy-test-classification.json
  - tests/test_repository_hygiene.py
  - tests/test_legacy_classification.py
autonomous: true
requirements:
  - PRES-04
  - TEST-01
  - TEST-02
must_haves:
  truths:
    - "One root command collects and runs only deterministic assertion-based tests with an unambiguous exit status."
    - "Every legacy test-like script is classified exactly once without broadening pytest discovery."
    - "New runtime databases, backups, exports, profiles, logs, or secrets cannot silently enter a commit, while existing tracked anomalies remain untouched."
  artifacts:
    - path: "pyproject.toml"
      provides: "Strict deterministic discovery and registered environment-dependent markers"
    - path: ".planning/evidence/phase-01/legacy-test-classification.json"
      provides: "Complete ownership/disposition record for legacy scripts"
    - path: "tests/test_repository_hygiene.py"
      provides: "Regression gate for new tracked runtime artifacts"
  key_links:
    - from: "tests/test_legacy_classification.py"
      to: ".planning/evidence/phase-01/legacy-test-classification.json"
      via: "exact set comparison against repository script discovery"
      pattern: "legacy-test-classification"
    - from: "tests/test_repository_hygiene.py"
      to: ".planning/evidence/phase-01/tracked-runtime-baseline.json"
      via: "grandfathered tracked-path comparison"
      pattern: "tracked-runtime-baseline"
---

<objective>
Establish the truthful deterministic test and Git-hygiene boundaries required by D-03 and D-04, without changing, moving, or deleting legacy scripts or existing tracked data.

Purpose: Prevent partial collection, printed booleans, unavailable services, and repository artifacts from being mistaken for engineering success.
Output: Strict pytest configuration, complete legacy classification, expanded ignore coverage, and executable hygiene gates.
</objective>

## Artifacts this phase produces

- `pyproject.toml` — `tests/`-only discovery, strict async/marker semantics, and registered `live`, `ollama`, and `gpu` lanes.
- `.planning/evidence/phase-01/legacy-test-classification.json` — one classification per test-like legacy script.
- `.planning/evidence/phase-01/tracked-runtime-baseline.json` — content-free path/size/blob baseline for already tracked runtime artifacts.
- `tests/test_repository_hygiene.py` and `tests/test_legacy_classification.py` — automated truthfulness gates.

<execution_context>
@/home/ty/.Codex/gsd-core/workflows/execute-plan.md
@/home/ty/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-preservation-and-trusted-baseline/01-CONTEXT.md
@.planning/phases/01-preservation-and-trusted-baseline/01-RESEARCH.md
@.planning/codebase/TESTING.md
@pyproject.toml
@.gitignore
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make deterministic discovery and repository hygiene executable contracts</name>
  <files>pyproject.toml, .gitignore, .planning/evidence/phase-01/tracked-runtime-baseline.json, tests/test_repository_hygiene.py</files>
  <read_first>
    @pyproject.toml
    @.gitignore
    @.planning/phases/01-preservation-and-trusted-baseline/01-RESEARCH.md
    @.planning/codebase/TESTING.md
  </read_first>
  <behavior>
    - `uv run python -m pytest -q` collects only `tests/` and returns a nonzero status for assertion, collection, or configuration failure.
    - Async behavior is strict; `live`, `ollama`, and `gpu` markers are registered but no marked external check is required by the root command.
    - Existing tracked runtime artifacts are recorded by path, byte size, and Git blob OID, but a newly tracked runtime/secret pattern fails the test.
    - Ignore coverage includes active/root Chroma data, backup roots, exports, sessions, profiles/users, logs/profiles/traces, SQLite sidecars, and environment-secret variants without untracking anything.
  </behavior>
  <action>Use RED→GREEN→REFACTOR. Add the repository-hygiene test first so missing baseline/ignore coverage fails. Keep `testpaths = ["tests"]`; add strict asyncio mode and environment markers without pointing discovery at `aura_backend/tests`, archives, or scratch. Generate `tracked-runtime-baseline.json` from `git ls-files` metadata only, with exact grandfathered paths and no file contents. Expand `.gitignore` for verified generated roots and secret/runtime patterns, preserving `.gitkeep` exceptions. The test must compare current tracked candidates against the grandfathered set so Phase 1 does not delete or untrack existing artifacts but does block new additions, implementing PRES-04 and D-03.</action>
  <acceptance_criteria>
    - The root module-invocation command collects only deterministic tests and requires neither Ollama nor another live service.
    - The hygiene test fails for a synthetic newly tracked candidate but permits the exact pre-existing baseline.
    - `git status --short` shows no database, backup, export, profile, log, secret, or generated-data deletion.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m pytest -q tests/test_repository_hygiene.py</automated>
  </verify>
  <done>The repository has an enforceable no-new-runtime-data gate and one honest deterministic command.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classify every legacy test-like script exactly once</name>
  <files>.planning/evidence/phase-01/legacy-test-classification.json, tests/test_legacy_classification.py</files>
  <read_first>
    @.planning/codebase/TESTING.md
    @.planning/phases/01-preservation-and-trusted-baseline/01-RESEARCH.md
    @aura_backend/tests/test_aura_parameter_fix.py
    @aura_backend/tests/test_mcp_bridge_fix.py
    @aura_backend/tests/test_numpy_serialization.py
  </read_first>
  <behavior>
    - The discovered set of `test_*.py`, `*_test.py`, and `quick_test.py` under `aura_backend/tests`, `aura_backend/archive_unused`, and `aura_backend/scratch` exactly equals the manifest path set.
    - Each entry has one approved category, evidence-based reason, external dependencies, write behavior, production symbol if any, disposition, and replacement test path when migrated.
    - Boolean returns, printed banners, import-time exits, missing services, and unavailable models are never represented as deterministic passes.
  </behavior>
  <action>Start with a failing completeness/schema test, then build the classification manifest from the current repository. Use categories that distinguish deterministic candidates, live service/MCP checks, optional model/GPU diagnostics, destructive/concurrency diagnostics, migration/recovery tools, source-text/demo checks, vacuous diagnostics, and immutable archives. Record the three production-backed migration groups owned by Plan 01-04: smart parameter handling, MCP/large-result serialization, and pure NumPy conversion. Preserve every legacy file in place per D-05; classification changes ownership and result semantics, not stored files or discovery scope.</action>
  <acceptance_criteria>
    - Every currently discovered legacy path appears exactly once and an added/unclassified script makes the test fail.
    - No entry calls a printed success, returned boolean, import success, or environment block a passing test.
    - Migration and live lanes name exact commands/markers and never enter default collection.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m pytest -q tests/test_legacy_classification.py</automated>
  </verify>
  <done>Every legacy check has an explicit, audited lane and none can inflate the deterministic pass count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Legacy script → trusted runner | Test-shaped code may exit, write real data, or depend on ambient services. |
| Working tree → Git commit | Generated or sensitive paths may become newly tracked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-FT | Repudiation | pytest result semantics | mitigate | Strict collection/configuration, native assertions, and separate external lanes. |
| T-01-CL | Information Disclosure | tracked runtime artifacts | mitigate | Content-free baseline, expanded ignore rules, and no-new-candidate regression test. |
| T-01-SM | Tampering | legacy diagnostics | mitigate | Keep them outside discovery; classify destructive/writing scripts explicitly. |
| T-01-SC | Tampering | package supply chain | mitigate | No dependency installation or re-lock; use current pytest stack only. |
</threat_model>

<verification>
Run `uv run python -m pytest --collect-only -q`, `uv run python -m pytest -q`, and `git diff --check`. Confirm collection contains no `aura_backend/tests`, archive, scratch, `live`, `ollama`, or `gpu` execution.
</verification>

<success_criteria>
- TEST-01 has one verified root command: `uv run python -m pytest -q`.
- TEST-02 has exact legacy classification and no broadened discovery.
- PRES-04 blocks new generated/secret/runtime data without removing any existing tracked artifact.
- D-03 and D-04 keep environment/model checks distinct from deterministic success.
</success_criteria>

<output>
Create `.planning/phases/01-preservation-and-trusted-baseline/01-03-SUMMARY.md` when done.
</output>
