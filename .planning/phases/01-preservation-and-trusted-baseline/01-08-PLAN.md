---
phase: 01-preservation-and-trusted-baseline
plan: 08
type: execute
wave: 3
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - .planning/evidence/phase-01/inventory-summary.json
autonomous: true
requirements: [PRES-01, PRES-04]
must_haves:
  truths:
    - "Every declared active, backup, test, and archive root is represented in a content-safe inventory."
    - "Both divergent likely-active Chroma roots remain preserved and neither is declared canonical."
    - "Private per-file evidence is outside Git and the committable summary passes its privacy schema."
  artifacts:
    - path: ".planning/evidence/phase-01/inventory-summary.json"
      provides: "Sanitized real-root totals, roles, hashes, integrity results, and anomaly counts"
  key_links:
    - from: ".planning/evidence/phase-01/inventory-summary.json"
      to: "aura_backend/preservation/cli.py"
      via: "real inventory command"
      pattern: "schema_version"
---

<objective>
Run the tested inventory autonomously against Aura's real roots and produce the safe preservation input required by D-02.

Purpose: Replace estimates with current, content-free evidence before the human quiescence gate.
Output: A private immutable manifest under `/backup` and a safe-to-commit inventory summary.
</objective>

## Artifacts this phase produces

- `/backup/aura-preservation/.../inventory.private.json` — immutable private per-file evidence.
- `.planning/evidence/phase-01/inventory-summary.json` — committable aggregate evidence only.

<execution_context>
@/home/ty/.Codex/gsd-core/workflows/execute-plan.md
@/home/ty/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-preservation-and-trusted-baseline/01-CONTEXT.md
@.planning/phases/01-preservation-and-trusted-baseline/01-RESEARCH.md
@.planning/phases/01-preservation-and-trusted-baseline/01-01-SUMMARY.md
@.planning/phases/01-preservation-and-trusted-baseline/01-02-SUMMARY.md
@.planning/evidence/phase-01/tracked-runtime-baseline.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory all declared real data roots without following links</name>
  <files>.planning/evidence/phase-01/inventory-summary.json, /backup/aura-preservation/</files>
  <read_first>
    @.planning/phases/01-preservation-and-trusted-baseline/01-RESEARCH.md
    @aura_backend/preservation/cli.py
    @.planning/evidence/phase-01/tracked-runtime-baseline.json
  </read_first>
  <action>From the repository root, run exactly `PRES_RUN_ID="$(date -u +phase-01-%Y%m%dT%H%M%SZ)"; PRES_RUN_DIR="/backup/aura-preservation/$PRES_RUN_ID"; uv run python -m aura_backend.preservation.cli inventory --repository-root . --backup-root /backup/aura-preservation --run-id "$PRES_RUN_ID" --private-manifest "$PRES_RUN_DIR/inventory.private.json" --public-summary .planning/evidence/phase-01/inventory-summary.json --root active=aura_chroma_db --root active=aura_backend/aura_chroma_db --root active=aura_data --root active=aura_backend/aura_data --root active=memvid_data --root active=aura_backend/memvid_data --root backup=auto_backups --root backup=aura_backend/auto_backups --root backup=aura_backend/chromadb_backups --root test=aura_backend/test_chroma_db --root test=aura_backend/tests/test_aura_chroma_db --root archive=archive --root archive=aura_backend/archive_unused --root archive=aura_backend/aura_archives --require-role active --require-role backup --require-role test --require-role archive`. This is the complete real inventory invocation under the Plan 01-02 contract; exit `0` is required. Write detailed evidence only to the named outside-Git private manifest and the allowlisted public schema to the named repository path. A suffix-matched file under an explicitly declared archive root that SQLite identifies as `SQLITE_NOTADB` remains hashed and counted, and records both database checks as `not_applicable` with the fixed public-safe reason `preserved_non_sqlite_archive`; it remains an anomaly rather than a successful integrity check. The same result in active, backup, or test roles fails. Per D-02 and the resolved research decision, retain both active Chroma candidates and do not select a canonical root, open originals through Chroma, copy data into the repository, or delete/migrate anything.</action>
  <acceptance_criteria>
    - Active, backup, test, and archive roles are all present; both likely-active roots have separate entries.
    - All regular files are counted/hashed or the run is explicitly non-passing.
    - Preserved non-database archive artifacts are explicit `not_applicable` anomalies; this classification cannot apply to active, backup, or test roots.
    - Private evidence is outside Git and the repository summary contains no personal sentinel/raw content fields.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m aura_backend.preservation.cli validate-summary --summary .planning/evidence/phase-01/inventory-summary.json --require-role active --require-role backup --require-role test --require-role archive</automated>
  </verify>
  <done>A complete, current, content-safe inventory exists without choosing or changing any source root.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile inventory, Git baseline, and known anomalies</name>
  <files>.planning/evidence/phase-01/inventory-summary.json</files>
  <read_first>
    @.planning/evidence/phase-01/inventory-summary.json
    @.planning/evidence/phase-01/tracked-runtime-baseline.json
    @.planning/phases/01-preservation-and-trusted-baseline/01-RESEARCH.md
  </read_first>
  <action>Run exactly `uv run python -m aura_backend.preservation.cli validate-summary --summary .planning/evidence/phase-01/inventory-summary.json --require-role active --require-role backup --require-role test --require-role archive`, then reconcile tracked runtime paths against the inventory and require each likely-active SQLite database to record separate integrity and FK results. Preserve the observed eight-row FK count/fingerprint as a Phase 3 finding; an `ok` structural result does not close it. Update only the generated aggregate summary if the validator adds deterministic cross-check fields.</action>
  <acceptance_criteria>
    - Every tracked runtime baseline path is accounted for or explicitly flagged.
    - The two likely-active roots record integrity `ok` and the observed eight-row FK anomaly separately.
    - Archive `not_applicable` evidence is role-bound through later restore parity and cannot license active corruption or missing checks.
    - No overall pass hides missing, blocked, unstable, or unclassified entries.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m pytest -q tests/preservation/test_inventory.py tests/preservation/test_manifest_privacy.py tests/test_repository_hygiene.py</automated>
  </verify>
  <done>The inventory is a trustworthy input to backup preflight and retains the anomaly as unresolved parity evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Real personal roots → public evidence | Real metadata is summarized for Git. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-CL | Information Disclosure | inventory summary | mitigate | Field allowlist/privacy validator; private evidence stays under `/backup`. |
| T-01-SM | Tampering | real roots | mitigate | Read/hash only, read-only SQLite, no Chroma client, before/after stability checks. |
| T-01-PE | Tampering | root traversal | mitigate | Explicit roots, no-follow walker, blocked symlinks/special files. |
| T-01-FB | Repudiation | inventory status | mitigate | Required roles/checks and explicit anomaly/non-pass states. |
| T-01-SC | Tampering | packages | mitigate | No installation or lock change. |
</threat_model>

<verification>
Run both task commands, `uv run python -m pytest -q`, and `git diff --check`; inspect `git status --short` for no new runtime data.
</verification>

<success_criteria>
- PRES-01 has current evidence for every role without personal content.
- PRES-04 is checked against the real inventory and tracked baseline.
- D-02 preserves both active candidates and the FK finding without mutation.
</success_criteria>

<output>Create `.planning/phases/01-preservation-and-trusted-baseline/01-08-SUMMARY.md` when done.</output>
