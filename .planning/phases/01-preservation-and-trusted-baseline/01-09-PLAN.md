---
phase: 01-preservation-and-trusted-baseline
plan: 09
type: execute
wave: 4
depends_on: [01-08]
files_modified:
  - .planning/evidence/phase-01/quiescence-summary.json
autonomous: false
requirements: [PRES-01, PRES-02]
must_haves:
  truths:
    - "The real backup cannot begin until Aura/Chroma activity and open handles are absent and Ty confirms quiescence."
    - "The approved source set includes both likely-active roots and all inventoried backup/test/archive roots."
  artifacts:
    - path: ".planning/evidence/phase-01/quiescence-summary.json"
      provides: "Sanitized pointer to the private preflight ticket and approved source-set hash"
  key_links:
    - from: ".planning/evidence/phase-01/quiescence-summary.json"
      to: ".planning/evidence/phase-01/inventory-summary.json"
      via: "inventory/source-set digest"
      pattern: "source_set_hash"
---

<objective>
Create the real-backup preflight ticket and pause for Ty's explicit quiescence approval.

Purpose: Make the only human-dependent safety boundary visible and separate from implementation and copy execution.
Output: A sanitized quiescence summary plus a blocking human verification.
</objective>

## Artifacts this phase produces

- `/backup/aura-preservation/.../quiescence.private.json` — private process/open-handle evidence.
- `.planning/evidence/phase-01/quiescence-summary.json` — safe ticket pointer and source-set digest.

<execution_context>
@/home/ty/.Codex/gsd-core/workflows/execute-plan.md
@/home/ty/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-preservation-and-trusted-baseline/01-CONTEXT.md
@.planning/evidence/phase-01/inventory-summary.json
@.planning/phases/01-preservation-and-trusted-baseline/01-08-SUMMARY.md
@aura_backend/preservation/cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the real-backup preflight ticket</name>
  <files>.planning/evidence/phase-01/quiescence-summary.json, /backup/aura-preservation/</files>
  <read_first>
    @.planning/evidence/phase-01/inventory-summary.json
    @aura_backend/preservation/cli.py
    @.planning/phases/01-preservation-and-trusted-baseline/01-CONTEXT.md
  </read_first>
  <action>Run exactly `uv run python -m aura_backend.preservation.cli preflight --inventory-summary .planning/evidence/phase-01/inventory-summary.json --backup-root /backup/aura-preservation --public-summary .planning/evidence/phase-01/quiescence-summary.json --ticket-ttl-seconds 900`. The command resolves and re-hashes the inventory's private artifact, binds a new versioned private ticket to its `run_id`, inventory digest, and `source_set_sha256`, and records mount/free space, proposed `backup` destination, process scan, and `lsof` results for every active root. Exit `0` is required before the checkpoint. Exit `3` leaves blocked evidence; after Aura is stopped normally, rerun this same command so a new private ticket is issued and the public pointer advances without overwriting older tickets. Keep process details private under `/backup`; write only the Plan 01-02 public fields to the committable summary. Do not terminate processes, copy source data, or call the existing backup service.</action>
  <acceptance_criteria>
    - The ticket names the exact inventory/source-set digest and a new outside-Git destination.
    - Any detected writer/open handle yields blocked, not pass.
    - No source path or content is changed.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m aura_backend.preservation.cli validate-quiescence --summary .planning/evidence/phase-01/quiescence-summary.json --inventory .planning/evidence/phase-01/inventory-summary.json</automated>
  </verify>
  <done>A current, content-safe preflight exists and the copy has not started.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm Aura is quiescent and approve the complete source set</name>
  <read_first>
    @.planning/evidence/phase-01/quiescence-summary.json
    @.planning/evidence/phase-01/inventory-summary.json
  </read_first>
  <action>First run exactly `uv run python -m aura_backend.preservation.cli validate-quiescence --summary .planning/evidence/phase-01/quiescence-summary.json --inventory .planning/evidence/phase-01/inventory-summary.json --require-pass`; then pause. Ty confirms Aura is not in use, all Aura/FastAPI/Chroma writers are stopped, and both divergent active roots plus all inventory roles remain in the ticket-bound backup set. If validation reports a stale/blocked ticket, stop the relevant Aura process normally, rerun Task 1 to issue a fresh ticket, and rerun this validator before requesting approval; do not force-kill, delete, or omit a root to obtain approval.</action>
  <what-built>A read-only preflight with exact source-set and destination evidence; no backup bytes have been copied.</what-built>
  <how-to-verify>1. Confirm no conversation or maintenance operation is running. 2. Review only the sanitized root-role list. 3. Confirm both likely-active roots remain included. 4. Confirm the summary reports no open handles/writers and `/backup` is the target.</how-to-verify>
  <acceptance_criteria>
    - Ty explicitly approves quiescence and the complete source set.
    - The automated preflight is pass and still current.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m aura_backend.preservation.cli validate-quiescence --summary .planning/evidence/phase-01/quiescence-summary.json --inventory .planning/evidence/phase-01/inventory-summary.json --require-pass</automated>
    <human-check>Ty confirms Aura is stopped and replies `approved`.</human-check>
  </verify>
  <resume-signal>Type `approved` only after the command passes; otherwise describe the active process/root discrepancy.</resume-signal>
  <done>Human and automated quiescence gates both approve the exact complete source set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live runtime → backup preflight | Process/open-handle state determines whether copying is safe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SM | Tampering | live source | mitigate | Human stop confirmation plus automated process/open-handle scan; no copy in this plan. |
| T-01-FB | Repudiation | approval ticket | mitigate | Timestamped private evidence, source-set digest, explicit blocking checkpoint. |
| T-01-CL | Information Disclosure | process evidence | mitigate | Private details outside Git; public summary contains statuses/digests only. |
| T-01-SC | Tampering | packages | mitigate | No installation or dependency change. |
</threat_model>

<verification>The plan ends only after the automated `--require-pass` command succeeds and Ty replies `approved`; otherwise it remains blocked without backup execution.</verification>

<success_criteria>
- PRES-02's quiescence precondition is explicit and separately approved.
- No source is omitted, copied, mutated, migrated, or deleted in this plan.
</success_criteria>

<output>Create `.planning/phases/01-preservation-and-trusted-baseline/01-09-SUMMARY.md` when done.</output>
