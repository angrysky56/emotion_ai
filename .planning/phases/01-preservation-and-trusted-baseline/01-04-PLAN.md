---
phase: 01-preservation-and-trusted-baseline
plan: 04
type: execute
wave: 2
depends_on:
  - 01-03
files_modified:
  - tests/characterization/test_mcp_parameters.py
  - tests/characterization/test_mcp_result_formatting.py
  - tests/characterization/test_numpy_serialization.py
autonomous: true
requirements:
  - PRES-03
  - TEST-02
must_haves:
  truths:
    - "Valuable pure legacy cases run as native assertions against production symbols."
    - "The migrated cases are deterministic and require no server, model, GPU, network, or persistent database."
    - "The corresponding legacy scripts remain preserved and outside default discovery."
  artifacts:
    - path: "tests/characterization/test_mcp_parameters.py"
      provides: "Production SmartMCPParameterHandler characterization"
    - path: "tests/characterization/test_mcp_result_formatting.py"
      provides: "Production bridge serialization and result-bound characterization"
    - path: "tests/characterization/test_numpy_serialization.py"
      provides: "Production NumPy conversion characterization"
  key_links:
    - from: "tests/characterization/test_mcp_parameters.py"
      to: "aura_backend/smart_mcp_parameter_handler.py"
      via: "direct package import and `format_parameters` calls"
      pattern: "SmartMCPParameterHandler"
    - from: "tests/characterization/test_numpy_serialization.py"
      to: "aura_backend/scripts/json_serialization_fix.py"
      via: "direct conversion-helper imports"
      pattern: "convert_numpy_to_python"
---

<objective>
Convert the highest-value deterministic legacy checks into real characterization tests, implementing D-03 and D-05 without refactoring their production behavior.

Purpose: Preserve useful behavior while removing boolean/print semantics from the trusted signal.
Output: Three assertion-based production-bound characterization modules.
</objective>

## Artifacts this phase produces

- `tests/characterization/test_mcp_parameters.py` — schema/heuristic/direct/wrapped/FastMCP parameter behavior.
- `tests/characterization/test_mcp_result_formatting.py` — JSON-safe tool output and result-size behavior.
- `tests/characterization/test_numpy_serialization.py` — scalar/array/nested NumPy conversion behavior.

<execution_context>
@/home/ty/.Codex/gsd-core/workflows/execute-plan.md
@/home/ty/.Codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-preservation-and-trusted-baseline/01-CONTEXT.md
@.planning/phases/01-preservation-and-trusted-baseline/01-RESEARCH.md
@.planning/evidence/phase-01/legacy-test-classification.json
@aura_backend/smart_mcp_parameter_handler.py
@aura_backend/mcp_to_gemini_bridge.py
@aura_backend/scripts/json_serialization_fix.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate smart MCP parameter behavior to native assertions</name>
  <files>tests/characterization/test_mcp_parameters.py</files>
  <read_first>
    @aura_backend/tests/test_aura_parameter_fix.py
    @aura_backend/smart_mcp_parameter_handler.py
    @.planning/evidence/phase-01/legacy-test-classification.json
  </read_first>
  <behavior>
    - `SmartMCPParameterHandler.format_parameters` preserves direct arguments and handles schema-selected wrapped/FastMCP forms.
    - JSON-string, already-wrapped, empty, malformed, and cached-format cases have explicit expected native values or exceptions/fallbacks.
    - Tests instantiate a fresh handler per case and assert production return values, not copied formatting logic.
  </behavior>
  <action>Run the focused path once before creation to record the RED missing-test signal, then port only evidence-backed cases from the legacy script as parameterized pytest assertions against the package import. Make case names describe inputs/outputs, isolate cache state, and retain current fallback behavior as characterization rather than redesigning it. Finish GREEN and refactor repeated schemas into fixtures. Do not edit or delete the legacy script.</action>
  <acceptance_criteria>
    - Direct, wrapped, FastMCP, JSON-string, cache, and malformed-input cases invoke the production handler.
    - No test returns a boolean, prints its own pass banner, edits `sys.path`, or contacts MCP.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m pytest -q tests/characterization/test_mcp_parameters.py</automated>
  </verify>
  <done>The parameter-formatting behavior has deterministic native assertions and a trustworthy exit status.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate MCP result serialization and bounds</name>
  <files>tests/characterization/test_mcp_result_formatting.py</files>
  <read_first>
    @aura_backend/tests/test_mcp_bridge_fix.py
    @aura_backend/mcp_to_gemini_bridge.py
    @aura_backend/providers/base.py
  </read_first>
  <behavior>
    - NumPy-containing bridge results become JSON-safe native values.
    - `format_function_call_result_for_model` preserves success/failure meaning and unwraps the current MCP content/result shapes.
    - Large list/string result handling stays within the production bounds and indicates truncation deterministically.
  </behavior>
  <action>Use the same RED→GREEN→REFACTOR sequence for this absent test module. Import and exercise `ToolExecutionResult`, `format_function_call_result_for_model`, and the bridge's production JSON/large-result helpers with a fake client object only. Assert shape, success/error meaning, and documented size bounds; do not reproduce formatter logic inside tests, start an MCP server, or depend on heartbeat timing.</action>
  <acceptance_criteria>
    - The tests directly call production bridge symbols with deterministic fake inputs.
    - Result bounds and error meaning are asserted without wall-clock sleeps or external processes.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m pytest -q tests/characterization/test_mcp_result_formatting.py</automated>
  </verify>
  <done>The useful MCP bridge checks now fail through pytest assertions rather than printed diagnostics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate pure NumPy serialization behavior</name>
  <files>tests/characterization/test_numpy_serialization.py</files>
  <read_first>
    @aura_backend/tests/test_numpy_serialization.py
    @aura_backend/scripts/json_serialization_fix.py
  </read_first>
  <behavior>
    - NumPy integer, floating, boolean, complex, array, nested dict/list, and tuple values convert to JSON-safe native structures.
    - `safe_json_dumps` emits parseable JSON and `clean_tool_result` preserves ordinary values while converting arrays.
    - No embedding model, SentenceTransformer, GPU, Chroma, or live tool is loaded.
  </behavior>
  <action>Record RED from the absent focused test path, then port only the pure conversion cases against `aura_backend.scripts.json_serialization_fix`. Parameterize scalar/array/nested boundaries, parse serialized output back with stdlib `json`, and assert types as well as values. Exclude the legacy embedding and live-tool portions per D-04. Complete GREEN and refactor fixture construction without changing production conversion behavior.</action>
  <acceptance_criteria>
    - All listed NumPy types and nested containers are covered against production functions.
    - The test module imports no model, embedding, database, network, or GPU subsystem.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -m pytest -q tests/characterization/test_numpy_serialization.py</automated>
  </verify>
  <done>The pure NumPy behavior is preserved in the deterministic suite and the environment-heavy remainder stays classified outside it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Legacy diagnostic → deterministic test | Script behavior must become assertion-owned evidence without ambient services. |
| Model/tool-shaped data → formatter | Untrusted nested values may be malformed or oversized. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-FT | Repudiation | migrated tests | mitigate | Native assertions against production imports and no boolean/print success semantics. |
| T-01-DOS | Denial of Service | large tool results | mitigate | Characterize production bounds with deterministic in-memory data. |
| T-01-CL | Information Disclosure | fixtures | mitigate | Use synthetic values only; no stored conversations or live service output. |
| T-01-SC | Tampering | package supply chain | mitigate | Add no packages and preserve the current lock. |
</threat_model>

<verification>
Run all three focused files, then `uv run python -m pytest -q` and `git diff --check`. Collection must not execute the original legacy scripts or any `live`, `ollama`, or `gpu` lane.
</verification>

<success_criteria>
- Three high-value legacy groups are represented by native assertions against production symbols.
- PRES-03 captures these current behaviors before refactoring.
- TEST-02 dispositions are realized without broadening discovery or deleting history.
- D-04 is visible in the absence of model/GPU/service dependencies from the focused command.
</success_criteria>

<output>
Create `.planning/phases/01-preservation-and-trusted-baseline/01-04-SUMMARY.md` when done.
</output>
